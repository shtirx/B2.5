!        Generated by TAPENADE     (INRIA, Ecuador team)
!  Tapenade 3.16 (develop) - 23 Jul 2024 17:41
!
!  Differentiation of gradc_div in forward (tangent) mode (with options multiDirectional context noISIZE r8):
!   variations   of useful results: funf gfun
!   with respect to varying inputs: funf fun
!   Plus diff mem management of: geo.cvvol:in geo.fcs:in geo.fcvol:in
!                geo.fcqalf:in
!
!
!
!
!
!
!
!
!
!
!
!-----------------------------------------------------------------------
!.specification
!
SUBROUTINE GRADC_DIV_DV(ncv, nfc, nvx, mode, geo, geod, mpg, mpgd, fun, &
& fund, funf, funfd, gfun, gfund, nbdirs)
  USE B2MOD_TYPES
  USE B2US_GEO_DIFFV
  USE B2US_MAP_DIFFV
!  Hint: nbdirsmax should be the maximum number of differentiation directions
  USE B2MOD_DIFFSIZES
  IMPLICIT NONE
!   ..input arguments
  INTEGER, INTENT(IN) :: ncv, nfc, nvx, mode
  TYPE(GEOMETRY), INTENT(IN) :: geo
  TYPE(GEOMETRY_DIFFV), INTENT(IN) :: geod
  TYPE(MAPPING), INTENT(IN) :: mpg
  TYPE(MAPPING_DIFFV), INTENT(IN) :: mpgd
  REAL(kind=r8) :: fun(ncv), funf(nfc)
  REAL(kind=r8) :: fund(nbdirsmax, ncv), funfd(nbdirsmax, nfc)
!   ..output arguments
  REAL(kind=r8) :: gfun(ncv, 0:1)
  REAL(kind=r8) :: gfund(nbdirsmax, ncv, 0:1)
!-----------------------------------------------------------------------
!.documentation
!
!     GRADC_DIV computes gradients of a cell centered quantity fun in
!     cell centers.  Computation is based on the divergence theorem.
!     dfun(:,:,0) will hold the gradient in the poloidal direction
!     dfun(:,:,1) will hold the gradient in the radial direction
!
!     The routine is a simple interface to GRADC_DIV_P and GRADC_DIV_R, 
!     that compute the actual poloidal and radial gradients.
!
!-----------------------------------------------------------------------
!.declarations
!
!   ..local variables
!
!   ..procedures
  EXTERNAL GRADC_DIV_P_NODIFF, GRADC_DIV_R_NODIFF
  EXTERNAL GRADC_DIV_P_DV, GRADC_DIV_R_DV
  INTEGER :: nbdirs
!
!-----------------------------------------------------------------------
!.computation      
!
!   ..test mode
  CALL XERTST(0 .LE. mode .AND. mode .LE. 1, &
&       'gradc_div--faulty argument mode')
!
!   ..interpolate fun to cell faces, using volume weighted averaging
  IF (mode .EQ. 0) CALL INTFACE_DV(ncv, nfc, mpg%fccv, geo%fcvol, fun, &
&                            fund, funf, funfd, nbdirs)
!
!   ..compute poloidal and radial gradients
  CALL GRADC_DIV_P_DV(ncv, nfc, nvx, 1, geo, mpg, mpgd, fun, fund, funf&
&               , funfd, gfun(:, 0), gfund(:, :, 0), nbdirs)
  CALL GRADC_DIV_R_DV(ncv, nfc, nvx, 1, geo, mpg, mpgd, fun, fund, funf&
&               , funfd, gfun(:, 1), gfund(:, :, 1), nbdirs)
!
!   ..return
  RETURN
!
!-----------------------------------------------------------------------
!.end gradc_div
!
END SUBROUTINE GRADC_DIV_DV

!
!
!
!
!
!
!
!
!
!
!
!-----------------------------------------------------------------------
!.specification
!
SUBROUTINE GRADC_DIV_NODIFF(ncv, nfc, nvx, mode, geo, mpg, fun, funf, &
& gfun)
  USE B2MOD_TYPES
  USE B2US_GEO_DIFFV
  USE B2US_MAP_DIFFV
  USE B2MOD_DIFFSIZES
  IMPLICIT NONE
!   ..input arguments
  INTEGER, INTENT(IN) :: ncv, nfc, nvx, mode
  TYPE(GEOMETRY), INTENT(IN) :: geo
  TYPE(MAPPING), INTENT(IN) :: mpg
  REAL(kind=r8) :: fun(ncv), funf(nfc)
!   ..output arguments
  REAL(kind=r8) :: gfun(ncv, 0:1)
!-----------------------------------------------------------------------
!.documentation
!
!     GRADC_DIV computes gradients of a cell centered quantity fun in
!     cell centers.  Computation is based on the divergence theorem.
!     dfun(:,:,0) will hold the gradient in the poloidal direction
!     dfun(:,:,1) will hold the gradient in the radial direction
!
!     The routine is a simple interface to GRADC_DIV_P and GRADC_DIV_R, 
!     that compute the actual poloidal and radial gradients.
!
!-----------------------------------------------------------------------
!.declarations
!
!   ..local variables
!
!   ..procedures
  EXTERNAL GRADC_DIV_P_NODIFF, GRADC_DIV_R_NODIFF
!
!-----------------------------------------------------------------------
!.computation      
!
!   ..test mode
  CALL XERTST(0 .LE. mode .AND. mode .LE. 1, &
&       'gradc_div--faulty argument mode')
!
!   ..interpolate fun to cell faces, using volume weighted averaging
  IF (mode .EQ. 0) CALL INTFACE(ncv, nfc, mpg%fccv, geo%fcvol, fun, funf&
&                        )
!
!   ..compute poloidal and radial gradients
  CALL GRADC_DIV_P_NODIFF(ncv, nfc, nvx, 1, geo, mpg, fun, funf, gfun(:&
&                   , 0))
  CALL GRADC_DIV_R_NODIFF(ncv, nfc, nvx, 1, geo, mpg, fun, funf, gfun(:&
&                   , 1))
!
!   ..return
  RETURN
!
!-----------------------------------------------------------------------
!.end gradc_div
!
END SUBROUTINE GRADC_DIV_NODIFF

