!        Generated by TAPENADE     (INRIA, Ecuador team)
!  Tapenade 3.16 (develop) - 23 Jul 2024 17:41
!
!  Differentiation of b2xpnm in forward (tangent) mode (with options multiDirectional context noISIZE r8):
!   variations   of useful results: ne2m
!   with respect to varying inputs: na ne2m rz2
!
!
!
!
!
!
!
!
!
!
!
SUBROUTINE B2XPNM_DV(ncv, ns, rz2, rz2d, am, na, nad, ne2m, ne2md, &
& nbdirs)
  USE B2MOD_TYPES
!  Hint: nbdirsmax should be the maximum number of differentiation directions
  USE B2MOD_DIFFSIZES
  IMPLICIT NONE
  INTEGER :: ncv, ns
  REAL(kind=r8) :: rz2(ncv, 0:ns-1), am(0:ns-1), na(ncv, 0:ns-1), ne2m(&
& ncv)
  REAL(kind=r8) :: rz2d(nbdirsmax, ncv, 0:ns-1), nad(nbdirsmax, ncv, 0:&
& ns-1), ne2md(nbdirsmax, ncv)
!     ------------------------------------------------------------------
!     B2XPNM computes the atom density weighted by rz2/am, ne2m:
!       ne2m() = (sum is :: (rz2(,is)/am(is))*na(,is)) .
!     ------------------------------------------------------------------
  INTEGER :: is
  EXTERNAL SFILL_NODIFF
  EXTERNAL SFILL_DV
  REAL(r8), DIMENSION(nbdirsmax) :: dummyzerodiffd
  INTEGER :: nd
  REAL(kind=r8), DIMENSION(ncv) :: temp
  INTEGER :: nbdirs
!     ------------------------------------------------------------------
!$$$  call subini ('b2xpnm')
  dummyzerodiffd = 0.D0
  CALL SFILL_DV(ncv, 0.0_R8, dummyzerodiffd, ne2m, ne2md, 1, nbdirs)
  DO is=0,ns-1
    temp = rz2(:, is)/am(is)
    DO nd=1,nbdirs
      ne2md(nd, :) = ne2md(nd, :) + na(:, is)*rz2d(nd, :, is)/am(is) + &
&       temp*nad(nd, :, is)
    END DO
    ne2m = ne2m + temp*na(:, is)
  END DO
!$$$  call subend ()
  RETURN
!     ------------------------------------------------------------------
END SUBROUTINE B2XPNM_DV

!
!
!
!
!
!
!
!
!
!
!
SUBROUTINE B2XPNM_NODIFF(ncv, ns, rz2, am, na, ne2m)
  USE B2MOD_TYPES
  USE B2MOD_DIFFSIZES
  IMPLICIT NONE
  INTEGER :: ncv, ns
  REAL(kind=r8) :: rz2(ncv, 0:ns-1), am(0:ns-1), na(ncv, 0:ns-1), ne2m(&
& ncv)
!     ------------------------------------------------------------------
!     B2XPNM computes the atom density weighted by rz2/am, ne2m:
!       ne2m() = (sum is :: (rz2(,is)/am(is))*na(,is)) .
!     ------------------------------------------------------------------
  INTEGER :: is
  EXTERNAL SFILL_NODIFF
!     ------------------------------------------------------------------
!$$$  call subini ('b2xpnm')
  CALL SFILL_NODIFF(ncv, 0.0_R8, ne2m, 1)
  DO is=0,ns-1
    ne2m = ne2m + rz2(:, is)/am(is)*na(:, is)
  END DO
!$$$  call subend ()
  RETURN
!     ------------------------------------------------------------------
END SUBROUTINE B2XPNM_NODIFF

