!        Generated by TAPENADE     (INRIA, Ecuador team)
!  Tapenade 3.16 (develop) - 23 Jul 2024 17:41
!
!  Differentiation of b2xzdd in forward (tangent) mode (with options multiDirectional context noISIZE r8):
!   variations   of useful results: src
!   with respect to varying inputs: src
!
!
!
!
!
!
!
!
!
!
!
SUBROUTINE B2XZDD_DV(ncv, nd, switch, mpg, src, srcd, nbdirs)
  USE B2MOD_SWITCHES_DIFFV
  USE B2MOD_TYPES
  USE B2US_MAP_DIFFV
!  Hint: nbdirsmax should be the maximum number of differentiation directions
  USE B2MOD_DIFFSIZES
  IMPLICIT NONE
  INTEGER :: ncv, nd
  TYPE(SWITCHES), INTENT(IN) :: switch
  TYPE(MAPPING), INTENT(IN) :: mpg
  REAL(kind=r8) :: src(ncv, 0:nd)
  REAL(kind=r8) :: srcd(nbdirsmax, ncv, 0:nd)
!     ------------------------------------------------------------------
!      B2XZDD zeroes out the array elements corresponding to dead cells
!      and core boundary cells.
!     ------------------------------------------------------------------
  INTEGER :: id, icv
  INTEGER :: nd0
  INTEGER :: nbdirs
!     ------------------------------------------------------------------
  DO icv=1,ncv
    IF (mpg%cvreg(icv) .EQ. 0 .AND. switch%zero_dead_and_core .GE. 1) &
&   THEN
      DO id=0,nd
        DO nd0=1,nbdirs
          srcd(nd0, icv, id) = 0.D0
        END DO
        src(icv, id) = 0.0_R8
      END DO
    ELSE IF (icv .GT. mpg%nci .AND. mpg%cvonclosedsurface(icv) .AND. &
&       switch%zero_dead_and_core .GE. 2) THEN
      DO id=0,nd
        DO nd0=1,nbdirs
          srcd(nd0, icv, id) = 0.D0
        END DO
        src(icv, id) = 0.0_R8
      END DO
    END IF
  END DO
  RETURN
!     ------------------------------------------------------------------
END SUBROUTINE B2XZDD_DV

!
!
!
!
!
!
!
!
!
!
!
SUBROUTINE B2XZDD_NODIFF(ncv, nd, switch, mpg, src)
  USE B2MOD_SWITCHES_DIFFV
  USE B2MOD_TYPES
  USE B2US_MAP_DIFFV
  USE B2MOD_DIFFSIZES
  IMPLICIT NONE
  INTEGER :: ncv, nd
  TYPE(SWITCHES), INTENT(IN) :: switch
  TYPE(MAPPING), INTENT(IN) :: mpg
  REAL(kind=r8) :: src(ncv, 0:nd)
!     ------------------------------------------------------------------
!      B2XZDD zeroes out the array elements corresponding to dead cells
!      and core boundary cells.
!     ------------------------------------------------------------------
  INTEGER :: id, icv
!     ------------------------------------------------------------------
  DO icv=1,ncv
    IF (mpg%cvreg(icv) .EQ. 0 .AND. switch%zero_dead_and_core .GE. 1) &
&   THEN
      DO id=0,nd
        src(icv, id) = 0.0_R8
      END DO
    ELSE IF (icv .GT. mpg%nci .AND. mpg%cvonclosedsurface(icv) .AND. &
&       switch%zero_dead_and_core .GE. 2) THEN
      DO id=0,nd
        src(icv, id) = 0.0_R8
      END DO
    END IF
  END DO
  RETURN
!     ------------------------------------------------------------------
END SUBROUTINE B2XZDD_NODIFF

