!        Generated by TAPENADE     (INRIA, Ecuador team)
!  Tapenade 3.16 (feature_llhTests) - 27 May 2021 14:23
!
!  Differentiation of diff_r in forward (tangent) mode (with options multiDirectional context noISIZE r8):
!   variations   of useful results: dfunr funv
!   with respect to varying inputs: dfunr funv fun
!
!
!
!
!
!
!
!
!
!
!
!-----------------------------------------------------------------------
!.specification
SUBROUTINE DIFF_R_DV(ncv, nfc, nvx, mode, geo, mpg, mpgd, fun, fund, &
& funv, funvd, dfunr, dfunrd, nbdirs)
  USE B2MOD_TYPES
  USE B2US_GEO_DIFFV
  USE B2US_MAP_DIFFV
!  Hint: nbdirsmax should be the maximum number of differentiation directions
  USE B2MOD_DIFFSIZES
  IMPLICIT NONE
!
!-----------------------------------------------------------------------
!.end diff_r
!
!   ..input arguments
  INTEGER, INTENT(IN) :: ncv, nfc, nvx, mode
  TYPE(GEOMETRY), INTENT(IN) :: geo
  TYPE(MAPPING), INTENT(IN) :: mpg
  TYPE(MAPPING_DIFFV), INTENT(IN) :: mpgd
  REAL(kind=r8) :: fun(ncv), funv(nvx)
  REAL(kind=r8) :: fund(nbdirsmax, ncv), funvd(nbdirsmax, nvx)
!   ..output arguments
  REAL(kind=r8) :: dfunr(nfc)
  REAL(kind=r8) :: dfunrd(nbdirsmax, nfc)
!-----------------------------------------------------------------------
!.documentation
!
!     DIFF_R computes differences of a cell centered quantity fun in the
!     radial direction, on faces of a cell.
!
!     To compute values of the field in cell vertices, the routine relies
!     calls to intvertex.
!
!     If mode.eq.0, the routine will interpolate fun to corners in func
!     If mode.eq.1, the routine assumess func has been pre-computed
!
!-----------------------------------------------------------------------
!.declarations
!
!   ..local variables
  INTEGER :: ifc
!
!   ..procedures
  EXTERNAL INTVERTEX_NODIFF
  EXTERNAL INTVERTEX_DV
  INTEGER :: nd
  REAL(kind=r8) :: temp
  INTEGER :: nbdirs
!
!-----------------------------------------------------------------------
!.computation
!
!   ..test mode
  CALL XERTST(0 .LE. mode .AND. mode .LE. 1, &
&       'diff_r--faulty argument mode')
!
!   ..interpolate fun to cell vertices, using volume weighted averaging
  IF (mode .EQ. 0) CALL INTVERTEX_DV(ncv, nvx, mpg, geo%vxvol, fun, fund&
&                              , funv, funvd, nbdirs)
!
!   ..compute radial difference at faces
  DO ifc=1,nfc
    temp = (geo%fchc(ifc, 1)+geo%fchc(ifc, 2))*geo%fcqbet(ifc, 0)
    DO nd=1,nbdirs
      dfunrd(nd, ifc) = geo%fcqalf(ifc, 1)*(fund(nd, mpg%fccv(ifc, 2))-&
&       fund(nd, mpg%fccv(ifc, 1))) + temp*(funvd(nd, mpg%fcvx(ifc, 2))-&
&       funvd(nd, mpg%fcvx(ifc, 1)))/geo%fcht(ifc)
    END DO
    dfunr(ifc) = geo%fcqalf(ifc, 1)*(fun(mpg%fccv(ifc, 2))-fun(mpg%fccv(&
&     ifc, 1))) + temp*((funv(mpg%fcvx(ifc, 2))-funv(mpg%fcvx(ifc, 1)))/&
&     geo%fcht(ifc))
  END DO
!
!   ..return
  RETURN
END SUBROUTINE DIFF_R_DV

!
!
!
!
!
!
!
!
!
!
!
!-----------------------------------------------------------------------
!.specification
SUBROUTINE DIFF_R_NODIFF(ncv, nfc, nvx, mode, geo, mpg, fun, funv, dfunr&
&)
  USE B2MOD_TYPES
  USE B2US_GEO_DIFFV
  USE B2US_MAP_DIFFV
  USE B2MOD_DIFFSIZES
  IMPLICIT NONE
!
!-----------------------------------------------------------------------
!.end diff_r
!
!   ..input arguments
  INTEGER, INTENT(IN) :: ncv, nfc, nvx, mode
  TYPE(GEOMETRY), INTENT(IN) :: geo
  TYPE(MAPPING), INTENT(IN) :: mpg
  REAL(kind=r8) :: fun(ncv), funv(nvx)
!   ..output arguments
  REAL(kind=r8) :: dfunr(nfc)
!-----------------------------------------------------------------------
!.documentation
!
!     DIFF_R computes differences of a cell centered quantity fun in the
!     radial direction, on faces of a cell.
!
!     To compute values of the field in cell vertices, the routine relies
!     calls to intvertex.
!
!     If mode.eq.0, the routine will interpolate fun to corners in func
!     If mode.eq.1, the routine assumess func has been pre-computed
!
!-----------------------------------------------------------------------
!.declarations
!
!   ..local variables
  INTEGER :: ifc
!
!   ..procedures
  EXTERNAL INTVERTEX_NODIFF
!
!-----------------------------------------------------------------------
!.computation
!
!   ..test mode
  CALL XERTST(0 .LE. mode .AND. mode .LE. 1, &
&       'diff_r--faulty argument mode')
!
!   ..interpolate fun to cell vertices, using volume weighted averaging
  IF (mode .EQ. 0) CALL INTVERTEX_NODIFF(ncv, nvx, mpg, geo%vxvol, fun, &
&                                  funv)
!
!   ..compute radial difference at faces
  DO ifc=1,nfc
    dfunr(ifc) = (fun(mpg%fccv(ifc, 2))-fun(mpg%fccv(ifc, 1)))*geo%&
&     fcqalf(ifc, 1) + (funv(mpg%fcvx(ifc, 2))-funv(mpg%fcvx(ifc, 1)))*(&
&     geo%fchc(ifc, 1)+geo%fchc(ifc, 2))/geo%fcht(ifc)*geo%fcqbet(ifc, 0&
&     )
  END DO
!
!   ..return
  RETURN
END SUBROUTINE DIFF_R_NODIFF

