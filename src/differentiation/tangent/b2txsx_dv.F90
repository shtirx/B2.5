!        Generated by TAPENADE     (INRIA, Ecuador team)
!  Tapenade 3.16 (feature_llhTests) - 27 May 2021 14:23
!
!  Differentiation of b2txsx in forward (tangent) mode (with options multiDirectional context noISIZE r8):
!   variations   of useful results: funsx
!   with respect to varying inputs: fun funsx
!
!
!
!
!
!
!
!
!
!
!
SUBROUTINE B2TXSX_DV(ncv, nfc, geo, mpg, fcvol, fcs, fun, fund, funsx, &
& funsxd, nbdirs)
  USE B2MOD_TYPES
  USE B2US_GEO_DIFFV
  USE B2US_MAP_DIFFV
  USE B2MOD_SUBSYS
!  Hint: nbdirsmax should be the maximum number of differentiation directions
  USE B2MOD_DIFFSIZES
  IMPLICIT NONE
!     ------------------------------------------------------------------
  INTEGER, INTENT(IN) :: ncv, nfc
  TYPE(GEOMETRY), INTENT(IN) :: geo
  TYPE(MAPPING), INTENT(IN) :: mpg
  REAL(kind=r8) :: fcvol(nfc, 2), fcs(nfc), fun(ncv), funsx(nfc)
  REAL(kind=r8) :: fund(nbdirsmax, ncv), funsxd(nbdirsmax, nfc)
!     ------------------------------------------------------------------
!     B2TXSX computes the integrals over cell x-faces of a function
!     that is provided on the cells.
!!!   (Reconsider specification and use of this routine for the case
!     of a non-orthogonal grid.)
!     ------------------------------------------------------------------
  INTEGER :: ifc
  INTEGER :: nd
  REAL(kind=r8) :: temp
  REAL(kind=r8) :: temp0
  INTEGER :: nbdirs
!     ------------------------------------------------------------------
  CALL SUBINI('b2txsx')
!   ..test nCv, nFc
  CALL XERTST(0 .LT. ncv .AND. 0 .LT. nfc, 'faulty argument nCv,nFc')
!   ..compute funsx
  DO ifc=1,nfc
    temp = fcs(ifc)*geo%fcqalf(ifc, 0)
    temp0 = fcvol(ifc, 1) + fcvol(ifc, 2)
    DO nd=1,nbdirs
      funsxd(nd, ifc) = temp*(fcvol(ifc, 2)*fund(nd, mpg%fccv(ifc, 1))+&
&       fcvol(ifc, 1)*fund(nd, mpg%fccv(ifc, 2)))/temp0
    END DO
    funsx(ifc) = temp*((fcvol(ifc, 2)*fun(mpg%fccv(ifc, 1))+fcvol(ifc, 1&
&     )*fun(mpg%fccv(ifc, 2)))/temp0)
  END DO
!   ..return
  CALL SUBEND()
  RETURN
END SUBROUTINE B2TXSX_DV

!
!
!
!
!
!
!
!
!
!
!
SUBROUTINE B2TXSX_NODIFF(ncv, nfc, geo, mpg, fcvol, fcs, fun, funsx)
  USE B2MOD_TYPES
  USE B2US_GEO_DIFFV
  USE B2US_MAP_DIFFV
  USE B2MOD_SUBSYS
  USE B2MOD_DIFFSIZES
  IMPLICIT NONE
!     ------------------------------------------------------------------
  INTEGER, INTENT(IN) :: ncv, nfc
  TYPE(GEOMETRY), INTENT(IN) :: geo
  TYPE(MAPPING), INTENT(IN) :: mpg
  REAL(kind=r8) :: fcvol(nfc, 2), fcs(nfc), fun(ncv), funsx(nfc)
!     ------------------------------------------------------------------
!     B2TXSX computes the integrals over cell x-faces of a function
!     that is provided on the cells.
!!!   (Reconsider specification and use of this routine for the case
!     of a non-orthogonal grid.)
!     ------------------------------------------------------------------
  INTEGER :: ifc
!     ------------------------------------------------------------------
  CALL SUBINI('b2txsx')
!   ..test nCv, nFc
  CALL XERTST(0 .LT. ncv .AND. 0 .LT. nfc, 'faulty argument nCv,nFc')
!   ..compute funsx
  DO ifc=1,nfc
    funsx(ifc) = fcs(ifc)*geo%fcqalf(ifc, 0)*(fcvol(ifc, 2)*fun(mpg%fccv&
&     (ifc, 1))+fcvol(ifc, 1)*fun(mpg%fccv(ifc, 2)))/(fcvol(ifc, 1)+&
&     fcvol(ifc, 2))
  END DO
!   ..return
  CALL SUBEND()
  RETURN
END SUBROUTINE B2TXSX_NODIFF

