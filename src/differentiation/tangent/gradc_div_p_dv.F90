!        Generated by TAPENADE     (INRIA, Ecuador team)
!  Tapenade 3.16 (feature_llhTests) - 27 May 2021 14:23
!
!  Differentiation of gradc_div_p in forward (tangent) mode (with options multiDirectional context noISIZE r8):
!   variations   of useful results: gfunp funf
!   with respect to varying inputs: funf fun
!
!
!
!
!
!
!
!
!
!
!
!-----------------------------------------------------------------------
!.specification
!
SUBROUTINE GRADC_DIV_P_DV(ncv, nfc, nvx, mode, geo, mpg, mpgd, fun, fund&
& , funf, funfd, gfunp, gfunpd, nbdirs)
  USE B2MOD_TYPES
  USE B2US_GEO_DIFFV
  USE B2US_MAP_DIFFV
!  Hint: nbdirsmax should be the maximum number of differentiation directions
  USE B2MOD_DIFFSIZES
  IMPLICIT NONE
!
!-----------------------------------------------------------------------
!.end gradc_div_p
!
!   ..input arguments
  INTEGER, INTENT(IN) :: ncv, nfc, nvx, mode
  TYPE(GEOMETRY), INTENT(IN) :: geo
  TYPE(MAPPING), INTENT(IN) :: mpg
  TYPE(MAPPING_DIFFV), INTENT(IN) :: mpgd
  REAL(kind=r8) :: fun(ncv), funf(nfc)
  REAL(kind=r8) :: fund(nbdirsmax, ncv), funfd(nbdirsmax, nfc)
!   ..output arguments
  REAL(kind=r8) :: gfunp(ncv)
  REAL(kind=r8) :: gfunpd(nbdirsmax, ncv)
!-----------------------------------------------------------------------
!.documentation
!
!     GRADC_DIV_P computes gradients of a cell centered quantity fun in 
!     the poloidal direction, in cell centers.  Computation is based on
!     the divergence theorem.
!     For guard cells, the value of the gradient is set to 0.
!
!-----------------------------------------------------------------------
!.declarations
!
!   ..local variables
  REAL(kind=r8) :: wrkc(ncv), wrkf(nfc)
  REAL(kind=r8) :: wrkfd(nbdirsmax, nfc)
  INTEGER :: nd
  INTEGER :: nbdirs
!
!-----------------------------------------------------------------------
!.computation      
!
!   ..test mode
  CALL XERTST(0 .LE. mode .AND. mode .LE. 1, &
&       'gradc_div_p--faulty argument mode')
!
!   ..interpolate fun to cell faces, using volume weighted averaging
  IF (mode .EQ. 0) CALL INTFACE_DV(ncv, nfc, mpg%fccv, geo%fcvol, fun, &
&                            fund, funf, funfd, nbdirs)
!
!   ..compute gradient
  DO nd=1,nbdirs
    wrkfd(nd, :) = geo%fcqalf(:, 0)*geo%fcs*funfd(nd, :)
  END DO
  wrkf = funf*geo%fcs*geo%fcqalf(:, 0)
  CALL DIV0_DV(ncv, nfc, mpg, wrkf, wrkfd, gfunp, gfunpd, nbdirs)
  wrkf = geo%fcs*geo%fcqalf(:, 0)
  CALL DIV0_NODIFF(ncv, nfc, mpg, wrkf, wrkc)
  DO nd=1,nbdirs
    gfunpd(nd, :) = (gfunpd(nd, :)-wrkc*fund(nd, :))/geo%cvvol
!
!   ..set zero in guard cells (no reliable information from div0 there!)
    gfunpd(nd, mpg%nci+1:mpg%ncv) = 0.D0
  END DO
  gfunp = (gfunp-fun*wrkc)/geo%cvvol
  gfunp(mpg%nci+1:mpg%ncv) = 0.0_R8
!
!   ..return
  RETURN
END SUBROUTINE GRADC_DIV_P_DV

!
!
!
!
!
!
!
!
!
!
!
!-----------------------------------------------------------------------
!.specification
!
SUBROUTINE GRADC_DIV_P_NODIFF(ncv, nfc, nvx, mode, geo, mpg, fun, funf, &
& gfunp)
  USE B2MOD_TYPES
  USE B2US_GEO_DIFFV
  USE B2US_MAP_DIFFV
  USE B2MOD_DIFFSIZES
  IMPLICIT NONE
!
!-----------------------------------------------------------------------
!.end gradc_div_p
!
!   ..input arguments
  INTEGER, INTENT(IN) :: ncv, nfc, nvx, mode
  TYPE(GEOMETRY), INTENT(IN) :: geo
  TYPE(MAPPING), INTENT(IN) :: mpg
  REAL(kind=r8) :: fun(ncv), funf(nfc)
!   ..output arguments
  REAL(kind=r8) :: gfunp(ncv)
!-----------------------------------------------------------------------
!.documentation
!
!     GRADC_DIV_P computes gradients of a cell centered quantity fun in 
!     the poloidal direction, in cell centers.  Computation is based on
!     the divergence theorem.
!     For guard cells, the value of the gradient is set to 0.
!
!-----------------------------------------------------------------------
!.declarations
!
!   ..local variables
  REAL(kind=r8) :: wrkc(ncv), wrkf(nfc)
!
!-----------------------------------------------------------------------
!.computation      
!
!   ..test mode
  CALL XERTST(0 .LE. mode .AND. mode .LE. 1, &
&       'gradc_div_p--faulty argument mode')
!
!   ..interpolate fun to cell faces, using volume weighted averaging
  IF (mode .EQ. 0) CALL INTFACE(ncv, nfc, mpg%fccv, geo%fcvol, fun, funf&
&                        )
!
!   ..compute gradient
  wrkf = funf*geo%fcs*geo%fcqalf(:, 0)
  CALL DIV0_NODIFF(ncv, nfc, mpg, wrkf, gfunp)
  wrkf = geo%fcs*geo%fcqalf(:, 0)
  CALL DIV0_NODIFF(ncv, nfc, mpg, wrkf, wrkc)
  gfunp = (gfunp-fun*wrkc)/geo%cvvol
!
!   ..set zero in guard cells (no reliable information from div0 there!)
  gfunp(mpg%nci+1:mpg%ncv) = 0.0_R8
!
!   ..return
  RETURN
END SUBROUTINE GRADC_DIV_P_NODIFF

