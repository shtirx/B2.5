!        Generated by TAPENADE     (INRIA, Ecuador team)
!  Tapenade 3.16 (feature_llhTests) - 27 May 2021 14:23
!
!  Differentiation of diff in forward (tangent) mode (with options multiDirectional context noISIZE r8):
!   variations   of useful results: funv dfun
!   with respect to varying inputs: funv dfun fun
!   Plus diff mem management of: geo.fchc:in geo.fcht:in geo.fcqalf:in
!                geo.fcqbet:in geo.vxvol:in
!
!
!
!
!
!
!
!
!
!
!
!-----------------------------------------------------------------------
!.specification
SUBROUTINE DIFF_DV(ncv, nfc, nvx, mode, geo, geod, mpg, mpgd, fun, fund&
& , funv, funvd, dfun, dfund, nbdirs)
  USE B2MOD_TYPES
  USE B2US_GEO_DIFFV
  USE B2US_MAP_DIFFV
!  Hint: nbdirsmax should be the maximum number of differentiation directions
  USE B2MOD_DIFFSIZES
  IMPLICIT NONE
!
!-----------------------------------------------------------------------
!.end diff
!
!   ..input arguments
  INTEGER, INTENT(IN) :: ncv, nfc, nvx, mode
  TYPE(GEOMETRY), INTENT(IN) :: geo
  TYPE(GEOMETRY_DIFFV), INTENT(IN) :: geod
  TYPE(MAPPING), INTENT(IN) :: mpg
  TYPE(MAPPING_DIFFV), INTENT(IN) :: mpgd
  REAL(kind=r8) :: fun(ncv), funv(nvx)
  REAL(kind=r8) :: fund(nbdirsmax, ncv), funvd(nbdirsmax, nvx)
!   ..output arguments
  REAL(kind=r8) :: dfun(nfc, 0:1)
  REAL(kind=r8) :: dfund(nbdirsmax, nfc, 0:1)
!-----------------------------------------------------------------------
!.documentation
!
!     DIFF computes differences of a cell centered quantity fun on
!     faces of a cell.
!     dfun(:,0) will hold poloidal differences
!     dfun(:,1) will hold radial differences
!
!     The routine is a simple interface to DIFF_P and DIFF_R, that will
!     compute the actual poloidal and radial differences
!
!-----------------------------------------------------------------------
!.declarations
!
!   ..procedures
  EXTERNAL DIFF_P_NODIFF, DIFF_R_NODIFF
  EXTERNAL DIFF_P_DV, DIFF_R_DV
  INTEGER :: nbdirs
!
!-----------------------------------------------------------------------
!.computation      
!
!   ..test mode
  CALL XERTST(0 .LE. mode .AND. mode .LE. 1, &
&       'diff--faulty argument mode')
!
!   ..interpolate fun to cell vertices, using volume weighted averaging
  IF (mode .EQ. 0) CALL INTVERTEX_DV(ncv, nvx, mpg, geo%vxvol, fun, fund&
&                              , funv, funvd, nbdirs)
!
!   ..compute poloidal and radial differences
  CALL DIFF_P_DV(ncv, nfc, nvx, 1, geo, mpg, mpgd, fun, fund, funv, &
&          funvd, dfun(:, 0), dfund(:, :, 0), nbdirs)
  CALL DIFF_R_DV(ncv, nfc, nvx, 1, geo, mpg, mpgd, fun, fund, funv, &
&          funvd, dfun(:, 1), dfund(:, :, 1), nbdirs)
!
!   ..return
  RETURN
END SUBROUTINE DIFF_DV

!
!
!
!
!
!
!
!
!
!
!
!-----------------------------------------------------------------------
!.specification
SUBROUTINE DIFF_NODIFF(ncv, nfc, nvx, mode, geo, mpg, fun, funv, dfun)
  USE B2MOD_TYPES
  USE B2US_GEO_DIFFV
  USE B2US_MAP_DIFFV
  USE B2MOD_DIFFSIZES
  IMPLICIT NONE
!
!-----------------------------------------------------------------------
!.end diff
!
!   ..input arguments
  INTEGER, INTENT(IN) :: ncv, nfc, nvx, mode
  TYPE(GEOMETRY), INTENT(IN) :: geo
  TYPE(MAPPING), INTENT(IN) :: mpg
  REAL(kind=r8) :: fun(ncv), funv(nvx)
!   ..output arguments
  REAL(kind=r8) :: dfun(nfc, 0:1)
!-----------------------------------------------------------------------
!.documentation
!
!     DIFF computes differences of a cell centered quantity fun on
!     faces of a cell.
!     dfun(:,0) will hold poloidal differences
!     dfun(:,1) will hold radial differences
!
!     The routine is a simple interface to DIFF_P and DIFF_R, that will
!     compute the actual poloidal and radial differences
!
!-----------------------------------------------------------------------
!.declarations
!
!   ..procedures
  EXTERNAL DIFF_P_NODIFF, DIFF_R_NODIFF
!
!-----------------------------------------------------------------------
!.computation      
!
!   ..test mode
  CALL XERTST(0 .LE. mode .AND. mode .LE. 1, &
&       'diff--faulty argument mode')
!
!   ..interpolate fun to cell vertices, using volume weighted averaging
  IF (mode .EQ. 0) CALL INTVERTEX_NODIFF(ncv, nvx, mpg, geo%vxvol, fun, &
&                                  funv)
!
!   ..compute poloidal and radial differences
  CALL DIFF_P_NODIFF(ncv, nfc, nvx, 1, geo, mpg, fun, funv, dfun(:, 0))
  CALL DIFF_R_NODIFF(ncv, nfc, nvx, 1, geo, mpg, fun, funv, dfun(:, 1))
!
!   ..return
  RETURN
END SUBROUTINE DIFF_NODIFF

