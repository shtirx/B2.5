!        Generated by TAPENADE     (INRIA, Ecuador team)
!  Tapenade 3.16 (feature_llhTests) - 27 May 2021 14:23
!
!  Differentiation of b2treq in forward (tangent) mode (with options multiDirectional context noISIZE r8):
!   variations   of useful results: lnlam ceqp
!   with respect to varying inputs: ti ne ni ne2m lnlam ceqp te
!
!
!
!
!
!
!
!
!
!
!
!-----------------------------------------------------------------------
!.specification
!
!xpb !srv 01.07.09
SUBROUTINE B2TREQ_DV(ncv, switch, switchd, vol, vold, te, ted, ti, tid, &
& ne, ned, ni, nid, ne2m, ne2md, lnlam, lnlamd, st_ext, ceqp, ceqpd, &
& nbdirs)
  USE B2MOD_TYPES
  USE B2MOD_CONSTANTS
  USE B2MOD_B2CMPA_DIFFV
  USE B2US_PLASMA_DIFFV
  USE B2MOD_SWITCHES_DIFFV
  USE B2MOD_AD_DIFFV, ONLY : ncall_b2treq
! csc The following are not necessary for computation but are needed
!     for adjoint AD to avoid side-effect variables
  USE B2MOD_AD_DIFFV, ONLY : ncall_b2tlnl
  USE B2MOD_SUBSYS
!  Hint: nbdirsmax should be the maximum number of differentiation directions
  USE B2MOD_DIFFSIZES
  IMPLICIT NONE
!
!-----------------------------------------------------------------------
!.end b2treq
!
!   ..input arguments (unchanged on exit)
  INTEGER :: ncv
  TYPE(SWITCHES), INTENT(IN) :: switch
  TYPE(SWITCHES_DIFFV), INTENT(IN) :: switchd
  TYPE(B2STATEEXT), INTENT(IN) :: st_ext
!xpb !srv 01.07.09
  REAL(kind=r8) :: vol(ncv), ni(ncv, 0:1), te(ncv), ti(ncv), ne(ncv), &
& ne2m(ncv), lnlam(ncv)
  REAL(kind=r8) :: vold(nbdirsmax, ncv), nid(nbdirsmax, ncv, 0:1), ted(&
& nbdirsmax, ncv), tid(nbdirsmax, ncv), ned(nbdirsmax, ncv), ne2md(&
& nbdirsmax, ncv), lnlamd(nbdirsmax, ncv)
!   ..output arguments (unspecified on entry)
  REAL(kind=r8) :: ceqp(ncv)
  REAL(kind=r8) :: ceqpd(nbdirsmax, ncv)
!   ..common blocks
!-----------------------------------------------------------------------
!.documentation
!
!  1. purpose
!
!     B2TREQ computes the cell-integrated energy equipartition
!     multiplier.
!
!
!  3. description (see also routine b2cdca)
!
!     This routine computes the electron-atom energy equipartition
!     multiplier ceqp, defined so that on each cell the signed
!     equipartition energy transfer from atoms to electrons is
!     (ceqp*ne*ni/te**1.5)*(te-ti) .
!
!
!-----------------------------------------------------------------------
!.declarations
!
!   ..local variables
  INTEGER :: icv
  REAL(kind=r8) :: cfeqp
  PARAMETER (cfeqp=4.8e-15_R8)
!     lnlam is the log of the plasma parameter.
!     cfeqp is a numerical coefficient in the energy transfer rate.
!   ..procedures
  INTRINSIC SQRT
  EXTERNAL XERTST
  EXTERNAL B2XVSG
  INTEGER :: arg1
  REAL(kind=r8) :: result1
  INTEGER :: nd
  REAL(kind=r8) :: temp
  REAL(r8) :: temp0
  REAL(kind=r8) :: temp1
  INTEGER :: nbdirs
!   ..initialisation
!
!-----------------------------------------------------------------------
!.computation
!
!   ..subprogram start-up calls
  CALL SUBINI('b2treq')
!   ..test nCv
  CALL XERTST(0 .LT. ncv, 'faulty argument nCv')
!   ..extensive tests on first few calls
  IF (ncall_b2treq .LT. 3) THEN
!    ..test vol, state
    CALL B2XVSG(ncv, vol, 1, 'vol', '.gt.')
    CALL B2XVSG(ncv, te, 1, 'te', '.gt.')
    CALL B2XVSG(ncv, ne, 1, 'ne', '.gt.')
    arg1 = ncv*2
    CALL B2XVSG(arg1, ni, 1, 'ni', '.gt.')
    CALL B2XVSG(ncv, ne2m, 1, 'ne2m', '.gt.')
  END IF
!
  CALL B2TLNL_DV(ncv, switch, switchd, switch%icase_ei, te, ted, ti, tid&
&          , ne, ned, lnlam, lnlamd, nbdirs)
!srv 20.09.11
!   ..compute ceqp
  DO icv=1,ncv
!srv 01.07.09
    result1 = SQRT(ev)
    temp = switch%b2treq_phm0*cfeqp*vol(icv)*ev*result1
    temp0 = st_ext%ni(icv, 0) + ni(icv, 0)
    temp1 = lnlam(icv)*ne2m(icv)/temp0
    DO nd=1,nbdirs
      ceqpd(nd, icv) = temp*(ne2m(icv)*lnlamd(nd, icv)+lnlam(icv)*ne2md(&
&       nd, icv)-temp1*nid(nd, icv, 0))/temp0
    END DO
    ceqp(icv) = temp*temp1
  END DO
!
! ..return
  ncall_b2treq = ncall_b2treq + 1
  CALL SUBEND()
  RETURN
END SUBROUTINE B2TREQ_DV
!

!
!
!
!
!
!
!
!
!
!
!
!-----------------------------------------------------------------------
!.specification
!
!xpb !srv 01.07.09
SUBROUTINE B2TREQ_NODIFF(ncv, switch, vol, te, ti, ne, ni, ne2m, lnlam, &
& st_ext, ceqp)
  USE B2MOD_TYPES
  USE B2MOD_CONSTANTS
  USE B2MOD_B2CMPA_DIFFV
  USE B2US_PLASMA_DIFFV
  USE B2MOD_SWITCHES_DIFFV
  USE B2MOD_AD_DIFFV, ONLY : ncall_b2treq
! csc The following are not necessary for computation but are needed
!     for adjoint AD to avoid side-effect variables
  USE B2MOD_AD_DIFFV, ONLY : ncall_b2tlnl
  USE B2MOD_SUBSYS
  USE B2MOD_DIFFSIZES
  IMPLICIT NONE
!
!-----------------------------------------------------------------------
!.end b2treq
!
!   ..input arguments (unchanged on exit)
  INTEGER :: ncv
  TYPE(SWITCHES), INTENT(IN) :: switch
  TYPE(B2STATEEXT), INTENT(IN) :: st_ext
!xpb !srv 01.07.09
  REAL(kind=r8) :: vol(ncv), ni(ncv, 0:1), te(ncv), ti(ncv), ne(ncv), &
& ne2m(ncv), lnlam(ncv)
!   ..output arguments (unspecified on entry)
  REAL(kind=r8) :: ceqp(ncv)
!   ..common blocks
!-----------------------------------------------------------------------
!.documentation
!
!  1. purpose
!
!     B2TREQ computes the cell-integrated energy equipartition
!     multiplier.
!
!
!  3. description (see also routine b2cdca)
!
!     This routine computes the electron-atom energy equipartition
!     multiplier ceqp, defined so that on each cell the signed
!     equipartition energy transfer from atoms to electrons is
!     (ceqp*ne*ni/te**1.5)*(te-ti) .
!
!
!-----------------------------------------------------------------------
!.declarations
!
!   ..local variables
  INTEGER :: icv
  REAL(kind=r8) :: cfeqp
  PARAMETER (cfeqp=4.8e-15_R8)
!     lnlam is the log of the plasma parameter.
!     cfeqp is a numerical coefficient in the energy transfer rate.
!   ..procedures
  INTRINSIC SQRT
  EXTERNAL XERTST
  EXTERNAL B2XVSG
  INTEGER :: arg1
  REAL(kind=r8) :: result1
!   ..initialisation
!
!-----------------------------------------------------------------------
!.computation
!
!   ..subprogram start-up calls
  CALL SUBINI('b2treq')
!   ..test nCv
  CALL XERTST(0 .LT. ncv, 'faulty argument nCv')
!   ..extensive tests on first few calls
  IF (ncall_b2treq .LT. 3) THEN
!    ..test vol, state
    CALL B2XVSG(ncv, vol, 1, 'vol', '.gt.')
    CALL B2XVSG(ncv, te, 1, 'te', '.gt.')
    CALL B2XVSG(ncv, ne, 1, 'ne', '.gt.')
    arg1 = ncv*2
    CALL B2XVSG(arg1, ni, 1, 'ni', '.gt.')
    CALL B2XVSG(ncv, ne2m, 1, 'ne2m', '.gt.')
  END IF
!
  CALL B2TLNL_NODIFF(ncv, switch, switch%icase_ei, te, ti, ne, lnlam)
!srv 20.09.11
!   ..compute ceqp
  DO icv=1,ncv
!srv 01.07.09
    result1 = SQRT(ev)
    ceqp(icv) = switch%b2treq_phm0*(cfeqp*lnlam(icv)*ev*result1)*vol(icv&
&     )*(ne2m(icv)/(ni(icv, 0)+st_ext%ni(icv, 0)))
  END DO
!
! ..return
  ncall_b2treq = ncall_b2treq + 1
  CALL SUBEND()
  RETURN
END SUBROUTINE B2TREQ_NODIFF
!

