!        Generated by TAPENADE     (INRIA, Ecuador team)
!  Tapenade 3.16 (feature_llhTests) - 27 May 2021 14:23
!
!  Differentiation of b2txsy in forward (tangent) mode (with options multiDirectional context noISIZE r8):
!   variations   of useful results: funsy
!   with respect to varying inputs: fun funsy
!
!
!
!
!
!
!
!
!
!
!
SUBROUTINE B2TXSY_DV(ncv, nfc, geo, mpg, fcvol, fcs, fun, fund, funsy, &
& funsyd, nbdirs)
  USE B2MOD_TYPES
  USE B2US_GEO_DIFFV
  USE B2US_MAP_DIFFV
  USE B2MOD_SUBSYS
!  Hint: nbdirsmax should be the maximum number of differentiation directions
  USE B2MOD_DIFFSIZES
  IMPLICIT NONE
!     ------------------------------------------------------------------
  INTEGER, INTENT(IN) :: ncv, nfc
  TYPE(GEOMETRY), INTENT(IN) :: geo
  TYPE(MAPPING), INTENT(IN) :: mpg
  REAL(kind=r8) :: fcvol(nfc, 2), fcs(nfc), fun(ncv), funsy(nfc)
  REAL(kind=r8) :: fund(nbdirsmax, ncv), funsyd(nbdirsmax, nfc)
!     ------------------------------------------------------------------
!     B2TXSY computes the integrals over cell y-faces of a function
!     that is provided on the cells.
!!!   (Reconsider specification and use of this routine for the case
!     of a non-orthogonal grid.)
!     ------------------------------------------------------------------
  INTEGER :: ifc
  INTEGER :: nd
  REAL(kind=r8) :: temp
  REAL(kind=r8) :: temp0
  INTEGER :: nbdirs
!     ------------------------------------------------------------------
  CALL SUBINI('b2txsy')
!   ..compute funsy
  DO ifc=1,nfc
    temp = fcs(ifc)*geo%fcqalf(ifc, 1)
    temp0 = fcvol(ifc, 1) + fcvol(ifc, 2)
    DO nd=1,nbdirs
      funsyd(nd, ifc) = temp*(fcvol(ifc, 2)*fund(nd, mpg%fccv(ifc, 1))+&
&       fcvol(ifc, 1)*fund(nd, mpg%fccv(ifc, 2)))/temp0
    END DO
    funsy(ifc) = temp*((fcvol(ifc, 2)*fun(mpg%fccv(ifc, 1))+fcvol(ifc, 1&
&     )*fun(mpg%fccv(ifc, 2)))/temp0)
  END DO
!   ..return
  CALL SUBEND()
  RETURN
END SUBROUTINE B2TXSY_DV

!
!
!
!
!
!
!
!
!
!
!
SUBROUTINE B2TXSY_NODIFF(ncv, nfc, geo, mpg, fcvol, fcs, fun, funsy)
  USE B2MOD_TYPES
  USE B2US_GEO_DIFFV
  USE B2US_MAP_DIFFV
  USE B2MOD_SUBSYS
  USE B2MOD_DIFFSIZES
  IMPLICIT NONE
!     ------------------------------------------------------------------
  INTEGER, INTENT(IN) :: ncv, nfc
  TYPE(GEOMETRY), INTENT(IN) :: geo
  TYPE(MAPPING), INTENT(IN) :: mpg
  REAL(kind=r8) :: fcvol(nfc, 2), fcs(nfc), fun(ncv), funsy(nfc)
!     ------------------------------------------------------------------
!     B2TXSY computes the integrals over cell y-faces of a function
!     that is provided on the cells.
!!!   (Reconsider specification and use of this routine for the case
!     of a non-orthogonal grid.)
!     ------------------------------------------------------------------
  INTEGER :: ifc
!     ------------------------------------------------------------------
  CALL SUBINI('b2txsy')
!   ..compute funsy
  DO ifc=1,nfc
    funsy(ifc) = fcs(ifc)*geo%fcqalf(ifc, 1)*(fcvol(ifc, 2)*fun(mpg%fccv&
&     (ifc, 1))+fcvol(ifc, 1)*fun(mpg%fccv(ifc, 2)))/(fcvol(ifc, 1)+&
&     fcvol(ifc, 2))
  END DO
!   ..return
  CALL SUBEND()
  RETURN
END SUBROUTINE B2TXSY_NODIFF

