!        Generated by TAPENADE     (INRIA, Ecuador team)
!  Tapenade 3.16 (develop) - 23 Jul 2024 17:41
!
!  Differentiation of grad_nt in forward (tangent) mode (with options multiDirectional context noISIZE r8):
!   variations   of useful results: gfun
!   with respect to varying inputs: funv fun
!   Plus diff mem management of: geo.fchc:in geo.fcht:in geo.fcqgam:in
!                geo.vxvol:in
!
!
!
!
!
!
!
!
!
!
!
!-----------------------------------------------------------------------
!.specification
!
SUBROUTINE GRAD_NT_DV(ncv, nfc, nvx, mode, geo, geod, mpg, mpgd, fun, &
& fund, funv, funvd, gfun, gfund, nbdirs)
  USE B2MOD_TYPES
  USE B2US_GEO_DIFFV
  USE B2US_MAP_DIFFV
!  Hint: nbdirsmax should be the maximum number of differentiation directions
  USE B2MOD_DIFFSIZES
  IMPLICIT NONE
!   ..input arguments
  INTEGER, INTENT(IN) :: ncv, nfc, nvx, mode
  TYPE(GEOMETRY), INTENT(IN) :: geo
  TYPE(GEOMETRY_DIFFV), INTENT(IN) :: geod
  TYPE(MAPPING), INTENT(IN) :: mpg
  TYPE(MAPPING_DIFFV), INTENT(IN) :: mpgd
  REAL(kind=r8) :: fun(ncv), funv(nvx)
  REAL(kind=r8) :: fund(nbdirsmax, ncv), funvd(nbdirsmax, nvx)
!   ..output arguments
  REAL(kind=r8) :: gfun(nfc, 0:1)
  REAL(kind=r8) :: gfund(nbdirsmax, nfc, 0:1)
!-----------------------------------------------------------------------
!.documentation
!
!     GRAD_NT computes gradients of a cell centered quantity fun on
!     faces of a cell.
!     gfun(:,0) will hold normal gradients
!     gfun(:,1) will hold tangential gradients
!
!     The routine is a simple interface to GRAD_N and GRAD_T, that will
!     compute the actual normal and tangential gradients
!
!-----------------------------------------------------------------------
!.declarations
!
!   ..procedures
  EXTERNAL GRAD_N_NODIFF, GRAD_T_NODIFF
  EXTERNAL GRAD_N_DV, GRAD_T_DV
  INTEGER :: nbdirs
!
!-----------------------------------------------------------------------
!.computation      
!
!   ..test mode
  CALL XERTST(0 .LE. mode .AND. mode .LE. 1, &
&       'grad--faulty argument mode')
!
!   ..interpolate fun to cell vertices, using volume weighted averaging
  IF (mode .EQ. 0) CALL INTVERTEX_DV(ncv, nvx, mpg, geo%vxvol, fun, fund&
&                              , funv, funvd, nbdirs)
!
!   ..compute normal and tangential gradients
  CALL GRAD_N_DV(ncv, nfc, nvx, 1, geo, mpg, mpgd, fun, fund, funv, &
&          funvd, gfun(:, 0), gfund(:, :, 0), nbdirs)
  CALL GRAD_T_DV(ncv, nfc, nvx, 1, geo, mpg, mpgd, fun, fund, funv, &
&          funvd, gfun(:, 1), gfund(:, :, 1), nbdirs)
!
!   ..return
  RETURN
!
!-----------------------------------------------------------------------
!.end grad_nt
!
END SUBROUTINE GRAD_NT_DV

!
!
!
!
!
!
!
!
!
!
!
!-----------------------------------------------------------------------
!.specification
!
SUBROUTINE GRAD_NT_NODIFF(ncv, nfc, nvx, mode, geo, mpg, fun, funv, gfun&
&)
  USE B2MOD_TYPES
  USE B2US_GEO_DIFFV
  USE B2US_MAP_DIFFV
  USE B2MOD_DIFFSIZES
  IMPLICIT NONE
!   ..input arguments
  INTEGER, INTENT(IN) :: ncv, nfc, nvx, mode
  TYPE(GEOMETRY), INTENT(IN) :: geo
  TYPE(MAPPING), INTENT(IN) :: mpg
  REAL(kind=r8) :: fun(ncv), funv(nvx)
!   ..output arguments
  REAL(kind=r8) :: gfun(nfc, 0:1)
!-----------------------------------------------------------------------
!.documentation
!
!     GRAD_NT computes gradients of a cell centered quantity fun on
!     faces of a cell.
!     gfun(:,0) will hold normal gradients
!     gfun(:,1) will hold tangential gradients
!
!     The routine is a simple interface to GRAD_N and GRAD_T, that will
!     compute the actual normal and tangential gradients
!
!-----------------------------------------------------------------------
!.declarations
!
!   ..procedures
  EXTERNAL GRAD_N_NODIFF, GRAD_T_NODIFF
!
!-----------------------------------------------------------------------
!.computation      
!
!   ..test mode
  CALL XERTST(0 .LE. mode .AND. mode .LE. 1, &
&       'grad--faulty argument mode')
!
!   ..interpolate fun to cell vertices, using volume weighted averaging
  IF (mode .EQ. 0) CALL INTVERTEX_NODIFF(ncv, nvx, mpg, geo%vxvol, fun, &
&                                  funv)
!
!   ..compute normal and tangential gradients
  CALL GRAD_N_NODIFF(ncv, nfc, nvx, 1, geo, mpg, fun, funv, gfun(:, 0))
  CALL GRAD_T_NODIFF(ncv, nfc, nvx, 1, geo, mpg, fun, funv, gfun(:, 1))
!
!   ..return
  RETURN
!
!-----------------------------------------------------------------------
!.end grad_nt
!
END SUBROUTINE GRAD_NT_NODIFF

