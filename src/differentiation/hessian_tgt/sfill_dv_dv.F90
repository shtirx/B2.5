!        Generated by TAPENADE     (INRIA, Ecuador team)
!  Tapenade 3.16 (feature_llhTests) - 27 May 2021 14:23
!
!  Differentiation of sfill_dv in forward (tangent) mode (with options multiDirectional context noISIZE r8):
!   variations   of useful results: sxd sx
!   with respect to varying inputs: sxd sx
!        Generated by TAPENADE     (INRIA, Ecuador team)
!  Tapenade 3.16 (feature_llhTests) - 27 May 2021 14:23
!
!  Differentiation of sfill in forward (tangent) mode (with options multiDirectional context noISIZE r8):
!   variations   of useful results: sx
!   with respect to varying inputs: sx
!
!
!
!
!
!
!
!
!
!
!
SUBROUTINE SFILL_DV_DV(n, sa, sad, sx, sxd0, sxd, sxdd, incx, nbdirs, &
& nbdirs0)
  USE B2MOD_TYPES
  USE B2MOD_MATH_DIFFV_DIFFV
!  Hint: nbdirsmax should be the maximum number of differentiation directions
  USE B2MOD_DIFFSIZES
!  Hint: nbdirsmax0 should be the maximum number of differentiation directions
  IMPLICIT NONE
!     ------------------------------------------------------------------
  INTEGER :: n, incx
  REAL(kind=r8) :: sa, sx(0:n*incx-1)
  REAL(kind=r8) :: sxd0(nbdirsmax0, 0:n*incx-1)
  REAL(kind=r8) :: sad(nbdirsmax), sxd(nbdirsmax, 0:n*incx-1)
  REAL(kind=r8) :: sxdd(nbdirsmax0, nbdirsmax, 0:n*incx-1)
!     ------------------------------------------------------------------
!     SFILL (BLAS-like routine) fills a vector with a constant value.
!     sfill(n,sa,sx,incx) sets all elements of the s.p. real*8 vector x
!     to sa. x is described by (n,sx,incx). BLAS storage conventions.
!     Return without effects if n.lt.1.
!     Undefined if incx.eq.0.
!     ------------------------------------------------------------------
  INTEGER :: i, ix
  EXTERNAL XERRAB
  INTEGER :: nd
  INTEGER :: nbdirs
  INTEGER :: nd0
  INTEGER :: nbdirs0
!     ------------------------------------------------------------------
  IF (incx .EQ. 0) THEN
    CALL XERRAB('sfill--error, incx.eq.0')
  ELSE IF (incx .EQ. 1) THEN
    DO i=0,n-1
      DO nd=1,nbdirs
        DO nd0=nd,nbdirs0
          sxdd(nd0, nd, i) = 0.0_8
        END DO
        sxd(nd, i) = 0.d0
      END DO
      DO nd0=1,nbdirs0
        sxd0(nd0, i) = 0.0_8
      END DO
      sx(i) = sa
    END DO
  ELSE
    ix = IBASE(n, incx)
    DO i=0,n-1
      DO nd=1,nbdirs
        DO nd0=nd,nbdirs0
          sxdd(nd0, nd, ix+i*incx) = 0.0_8
        END DO
        sxd(nd, ix+i*incx) = 0.d0
      END DO
      DO nd0=1,nbdirs0
        sxd0(nd0, ix+i*incx) = 0.0_8
      END DO
      sx(ix+i*incx) = sa
    END DO
  END IF
  RETURN
END SUBROUTINE SFILL_DV_DV

!        Generated by TAPENADE     (INRIA, Ecuador team)
!  Tapenade 3.16 (feature_llhTests) - 27 May 2021 14:23
!
!  Differentiation of sfill in forward (tangent) mode (with options multiDirectional context noISIZE r8):
!   variations   of useful results: sx
!   with respect to varying inputs: sx
!
!
!
!
!
!
!
!
!
!
!
SUBROUTINE SFILL_DV_NODIFF(n, sa, sad, sx, sxd, incx, nbdirs)
  USE B2MOD_TYPES
  USE B2MOD_MATH_DIFFV_DIFFV
!  Hint: nbdirsmax should be the maximum number of differentiation directions
  USE B2MOD_DIFFSIZES
  IMPLICIT NONE
!     ------------------------------------------------------------------
  INTEGER :: n, incx
  REAL(kind=r8) :: sa, sx(0:n*incx-1)
  REAL(kind=r8) :: sad(nbdirsmax), sxd(nbdirsmax, 0:n*incx-1)
!     ------------------------------------------------------------------
!     SFILL (BLAS-like routine) fills a vector with a constant value.
!     sfill(n,sa,sx,incx) sets all elements of the s.p. real*8 vector x
!     to sa. x is described by (n,sx,incx). BLAS storage conventions.
!     Return without effects if n.lt.1.
!     Undefined if incx.eq.0.
!     ------------------------------------------------------------------
  INTEGER :: i, ix
  EXTERNAL XERRAB
  INTEGER :: nd
  INTEGER :: nbdirs
!     ------------------------------------------------------------------
  IF (incx .EQ. 0) THEN
    CALL XERRAB('sfill--error, incx.eq.0')
  ELSE IF (incx .EQ. 1) THEN
    DO i=0,n-1
      DO nd=1,nbdirs
        sxd(nd, i) = 0.d0
      END DO
      sx(i) = sa
    END DO
  ELSE
    ix = IBASE(n, incx)
    DO i=0,n-1
      DO nd=1,nbdirs
        sxd(nd, ix+i*incx) = 0.d0
      END DO
      sx(ix+i*incx) = sa
    END DO
  END IF
  RETURN
END SUBROUTINE SFILL_DV_NODIFF

!  Differentiation of sfill_nodiff in forward (tangent) mode (with options multiDirectional context noISIZE r8):
!   variations   of useful results: sx
!   with respect to varying inputs: sx
!
!
!
!
!
!
!
!
!
!
!
SUBROUTINE SFILL_NODIFF_DV(n, sa, sx, sxd, incx, nbdirs)
  USE B2MOD_TYPES
  USE B2MOD_MATH_DIFFV_DIFFV
  USE B2MOD_DIFFSIZES
!  Hint: nbdirsmax0 should be the maximum number of differentiation directions
  IMPLICIT NONE
!     ------------------------------------------------------------------
  INTEGER :: n, incx
  REAL(kind=r8) :: sa, sx(0:n*incx-1)
  REAL(kind=r8) :: sxd(nbdirsmax0, 0:n*incx-1)
!     ------------------------------------------------------------------
!     SFILL (BLAS-like routine) fills a vector with a constant value.
!     sfill(n,sa,sx,incx) sets all elements of the s.p. real*8 vector x
!     to sa. x is described by (n,sx,incx). BLAS storage conventions.
!     Return without effects if n.lt.1.
!     Undefined if incx.eq.0.
!     ------------------------------------------------------------------
  INTEGER :: i, ix
  EXTERNAL XERRAB
  INTEGER :: nd
  INTEGER :: nbdirs
!     ------------------------------------------------------------------
  IF (incx .EQ. 0) THEN
    CALL XERRAB('sfill--error, incx.eq.0')
  ELSE IF (incx .EQ. 1) THEN
    DO i=0,n-1
      DO nd=1,nbdirs
        sxd(nd, i) = 0.0_8
      END DO
      sx(i) = sa
    END DO
  ELSE
    ix = IBASE(n, incx)
    DO i=0,n-1
      DO nd=1,nbdirs
        sxd(nd, ix+i*incx) = 0.0_8
      END DO
      sx(ix+i*incx) = sa
    END DO
  END IF
  RETURN
END SUBROUTINE SFILL_NODIFF_DV

!
!
!
!
!
!
!
!
!
!
!
SUBROUTINE SFILL_NODIFF_NODIFF(n, sa, sx, incx)
  USE B2MOD_TYPES
  USE B2MOD_MATH_DIFFV_DIFFV
  USE B2MOD_DIFFSIZES
  IMPLICIT NONE
!     ------------------------------------------------------------------
  INTEGER :: n, incx
  REAL(kind=r8) :: sa, sx(0:n*incx-1)
!     ------------------------------------------------------------------
!     SFILL (BLAS-like routine) fills a vector with a constant value.
!     sfill(n,sa,sx,incx) sets all elements of the s.p. real*8 vector x
!     to sa. x is described by (n,sx,incx). BLAS storage conventions.
!     Return without effects if n.lt.1.
!     Undefined if incx.eq.0.
!     ------------------------------------------------------------------
  INTEGER :: i, ix
  EXTERNAL XERRAB
!     ------------------------------------------------------------------
  IF (incx .EQ. 0) THEN
    CALL XERRAB('sfill--error, incx.eq.0')
  ELSE IF (incx .EQ. 1) THEN
    DO i=0,n-1
      sx(i) = sa
    END DO
  ELSE
    ix = IBASE(n, incx)
    DO i=0,n-1
      sx(ix+i*incx) = sa
    END DO
  END IF
  RETURN
END SUBROUTINE SFILL_NODIFF_NODIFF

