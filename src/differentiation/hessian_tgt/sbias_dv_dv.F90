!        Generated by TAPENADE     (INRIA, Ecuador team)
!  Tapenade 3.16 (feature_llhTests) - 27 May 2021 14:23
!
!        Generated by TAPENADE     (INRIA, Ecuador team)
!  Tapenade 3.16 (feature_llhTests) - 27 May 2021 14:23
!
!
!
!
!
!
!
!
!
!
!
!
SUBROUTINE SBIAS_NODIFF_NODIFF(n, sa, sx, incx)
  USE B2MOD_TYPES
  USE B2MOD_MATH_DIFFV_DIFFV
  USE B2MOD_DIFFSIZES
  IMPLICIT NONE
!     ------------------------------------------------------------------
  INTEGER :: n, incx
  REAL(kind=r8) :: sa, sx(0:(n-1)*incx)
!     ------------------------------------------------------------------
!     SBIAS (BLAS-like routine) adds a constant value to a vector.
!     sbias(n,sa,sx,incx) replaces the s.p. real*8 vector x by sa+x.
!     x is described by (n,sx,incx). BLAS storage conventions.
!     Return without effects if n.lt.1.
!     Undefined if incx.eq.0.
!     ------------------------------------------------------------------
  INTEGER :: i, ix
  EXTERNAL XERRAB
!     ------------------------------------------------------------------
  IF (incx .EQ. 0) THEN
    CALL XERRAB('sbias--error, incx.eq.0')
  ELSE IF (incx .EQ. 1) THEN
    DO i=0,n-1
      sx(i) = sa + sx(i)
    END DO
  ELSE
    ix = IBASE(n, incx)
    DO i=0,n-1
      sx(ix) = sa + sx(ix)
      ix = ix + incx
    END DO
  END IF
  RETURN
END SUBROUTINE SBIAS_NODIFF_NODIFF

