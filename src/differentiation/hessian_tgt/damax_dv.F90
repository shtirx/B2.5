!        Generated by TAPENADE     (INRIA, Ecuador team)
!  Tapenade 3.16 (develop) - 23 Jul 2024 17:41
!
!  Differentiation of damax in forward (tangent) mode (with options multiDirectional context noISIZE r8):
!   variations   of useful results: damax
!   with respect to varying inputs: sx
!
!
!
!
!
!
!
!
!
!
!
SUBROUTINE DAMAX_DV(n, sx, sxd, incx, damax, damaxd, nbdirs)
  USE B2MOD_TYPES
  USE B2MOD_MATH_DIFFV_DIFFV
!  Hint: nbdirsmax0 should be the maximum number of differentiation directions
  IMPLICIT NONE
  REAL(kind=r8) :: damax
  REAL(kind=r8), DIMENSION(nbdirsmax0) :: damaxd
  INTEGER :: n, incx
  REAL(kind=r8) :: sx(0:n*incx-1)
  REAL(kind=r8) :: sxd(nbdirsmax0, 0:n*incx-1)
!     ------------------------------------------------------------------
!     DAMAX (BLAS-like routine) computes the maximum absolute value in
!     a vector.
!     damax(n,sx,incx) returns the maximum absolute value in the
!     real*8 vector described by (n,sx,incx). BLAS storage conventions.
!     n must be positive.
!     ------------------------------------------------------------------
  INTEGER :: i, ix
  REAL(kind=r8) :: t
  REAL(kind=r8), DIMENSION(nbdirsmax0) :: td
  INTRINSIC MAX, ABS
  EXTERNAL XERRAB
  REAL(kind=r8) :: y1
  REAL(kind=r8), DIMENSION(nbdirsmax0) :: y1d
  REAL(kind=r8) :: y2
  REAL(kind=r8), DIMENSION(nbdirsmax0) :: y2d
  INTEGER :: nd
  INTEGER :: nbdirs
!     ------------------------------------------------------------------
  IF (n .LT. 1 .OR. incx .EQ. 0) THEN
    CALL XERRAB('damax--error, n.lt.1.or.incx.eq.0')
    damaxd = 0.D0
  ELSE IF (incx .EQ. 1) THEN
    IF (sx(0) .GE. 0.) THEN
      DO nd=1,nbdirs
        td(nd) = sxd(nd, 0)
      END DO
      t = sx(0)
    ELSE
      DO nd=1,nbdirs
        td(nd) = -sxd(nd, 0)
      END DO
      t = -sx(0)
    END IF
    DO i=1,n-1
      IF (sx(i) .GE. 0.) THEN
        DO nd=1,nbdirs
          y1d(nd) = sxd(nd, i)
        END DO
        y1 = sx(i)
      ELSE
        DO nd=1,nbdirs
          y1d(nd) = -sxd(nd, i)
        END DO
        y1 = -sx(i)
      END IF
      IF (t .LT. y1) THEN
        DO nd=1,nbdirs
          td(nd) = y1d(nd)
        END DO
        t = y1
      ELSE
        t = t
      END IF
    END DO
    DO nd=1,nbdirs
      damaxd(nd) = td(nd)
    END DO
    damax = t
  ELSE
    ix = IBASE(n, incx)
    IF (sx(ix) .GE. 0.) THEN
      DO nd=1,nbdirs
        td(nd) = sxd(nd, ix)
      END DO
      t = sx(ix)
    ELSE
      DO nd=1,nbdirs
        td(nd) = -sxd(nd, ix)
      END DO
      t = -sx(ix)
    END IF
    ix = ix + incx
    DO i=1,n-1
      IF (sx(ix) .GE. 0.) THEN
        DO nd=1,nbdirs
          y2d(nd) = sxd(nd, ix)
        END DO
        y2 = sx(ix)
      ELSE
        DO nd=1,nbdirs
          y2d(nd) = -sxd(nd, ix)
        END DO
        y2 = -sx(ix)
      END IF
      IF (t .LT. y2) THEN
        DO nd=1,nbdirs
          td(nd) = y2d(nd)
        END DO
        t = y2
      ELSE
        t = t
      END IF
      ix = ix + incx
    END DO
    DO nd=1,nbdirs
      damaxd(nd) = td(nd)
    END DO
    damax = t
  END IF
  RETURN
!     ------------------------------------------------------------------
END SUBROUTINE DAMAX_DV

!
!
!
!
!
!
!
!
!
!
!
FUNCTION DAMAX_NODIFF(n, sx, incx) RESULT (damax)
  USE B2MOD_TYPES
  USE B2MOD_MATH_DIFFV_DIFFV
  IMPLICIT NONE
  REAL(kind=r8) :: damax
  INTEGER :: n, incx
  REAL(kind=r8) :: sx(0:n*incx-1)
!     ------------------------------------------------------------------
!     DAMAX (BLAS-like routine) computes the maximum absolute value in
!     a vector.
!     damax(n,sx,incx) returns the maximum absolute value in the
!     real*8 vector described by (n,sx,incx). BLAS storage conventions.
!     n must be positive.
!     ------------------------------------------------------------------
  INTEGER :: i, ix
  REAL(kind=r8) :: t
  INTRINSIC MAX, ABS
  EXTERNAL XERRAB
  REAL(kind=r8) :: y1
  REAL(kind=r8) :: y2
!     ------------------------------------------------------------------
  IF (n .LT. 1 .OR. incx .EQ. 0) THEN
    CALL XERRAB('damax--error, n.lt.1.or.incx.eq.0')
  ELSE IF (incx .EQ. 1) THEN
    IF (sx(0) .GE. 0.) THEN
      t = sx(0)
    ELSE
      t = -sx(0)
    END IF
    DO i=1,n-1
      IF (sx(i) .GE. 0.) THEN
        y1 = sx(i)
      ELSE
        y1 = -sx(i)
      END IF
      IF (t .LT. y1) THEN
        t = y1
      ELSE
        t = t
      END IF
    END DO
    damax = t
  ELSE
    ix = IBASE(n, incx)
    IF (sx(ix) .GE. 0.) THEN
      t = sx(ix)
    ELSE
      t = -sx(ix)
    END IF
    ix = ix + incx
    DO i=1,n-1
      IF (sx(ix) .GE. 0.) THEN
        y2 = sx(ix)
      ELSE
        y2 = -sx(ix)
      END IF
      IF (t .LT. y2) THEN
        t = y2
      ELSE
        t = t
      END IF
      ix = ix + incx
    END DO
    damax = t
  END IF
  RETURN
!     ------------------------------------------------------------------
END FUNCTION DAMAX_NODIFF

