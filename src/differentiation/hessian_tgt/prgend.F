      subroutine prgend ()
      use b2mod_types
      use b2mod_ipmain
      use b2mod_lwimai
      use b2mod_lwmain
      use b2mod_subsys
      implicit none
*     ------------------------------------------------------------------
*     PRGEND performs any tasks required at the end of program
*     execution.
*     ------------------------------------------------------------------
      integer lwmx, lwimx
*     ------------------------------------------------------------------
*   ..write maximum workspace use
*     (activate or deactivate as desired)
      if (.true.) then
       call lwmax (lwmx)
       call lwimax (lwimx)
       write (*,'(1x,a,i9)') 'maximum use of b2mod_stack%w: ', lwmx
       write (*,'(1x,a,i9)') 'maximum use of b2mod_stack%wi:', lwimx
      endif
*   ..test use of internal parameters
      call ipfini ()
*   ..subprogram shut-down calls
      call subend ()
*   ..system shut-down calls
      call sysend ()
#ifdef PERFMON
      call perfoff
      call perfout ('whole')
#endif
      return
*     ------------------------------------------------------------------
      end subroutine prgend
      subroutine prgfin ()
      use b2mod_types
      use b2mod_ipmain
      use b2mod_lwimai
      use b2mod_lwmain
      use b2mod_subsys
      implicit none
*     ------------------------------------------------------------------
*     PRGFIN performs any tasks required at the end of program
*     execution.
*     ------------------------------------------------------------------
      integer lwmx, lwimx
*     ------------------------------------------------------------------
*   ..write maximum workspace use
*     (activate or deactivate as desired)
      if (.true.) then
       call lwmax (lwmx)
       call lwimax (lwimx)
       write (*,'(1x,a,i9)') 'maximum use of b2mod_stack%w: ', lwmx
       write (*,'(1x,a,i9)') 'maximum use of b2mod_stack%wi:', lwimx
      endif
*   ..test use of internal parameters
      if (.false.) call ipfini ()
*   ..subprogram shut-down calls
      call subend ()
*   ..system shut-down calls
      call sysend ()
#ifdef PERFMON
      call perfoff
      call perfout ('whole')
#endif
      return
*     ------------------------------------------------------------------
      end subroutine prgfin
