!        Generated by TAPENADE     (INRIA, Ecuador team)
!  Tapenade 3.16 (develop) - 23 Jul 2024 17:41
!
!  Differentiation of b2tbfl_dv in forward (tangent) mode (with options multiDirectional context noISIZE r8):
!   variations   of useful results: divd div
!   with respect to varying inputs: flux fluxd
!        Generated by TAPENADE     (INRIA, Ecuador team)
!  Tapenade 3.16 (develop) - 23 Jul 2024 17:41
!
!  Differentiation of b2tbfl in forward (tangent) mode (with options multiDirectional context noISIZE r8):
!   variations   of useful results: div
!   with respect to varying inputs: flux
!
!
!
!
!
!
!
!
!
!
!
!srv 12.02.16 {
SUBROUTINE B2TBFL_DV_DV(ncv, nfc, lcore, mpg, flux, fluxd0, fluxd, &
& fluxdd, tbfl, tbfld0, tbfld, tbfldd, div, divd0, divd, divdd, nbdirs, &
& nbdirs0)
  USE B2MOD_TYPES
  USE B2MOD_BOUNDARY_NAMELIST_DIFFV_DIFFV
  USE B2US_MAP_DIFFV_DIFFV
  USE B2MOD_SUBSYS
!  Hint: nbdirsmax should be the maximum number of differentiation directions
  USE B2MOD_DIFFSIZES
!  Hint: nbdirsmax0 should be the maximum number of differentiation directions
  IMPLICIT NONE
!   ..input arguments (unchanged on exit)
  INTEGER :: ncv, nfc
  LOGICAL :: lcore
  TYPE(MAPPING), INTENT(IN) :: mpg
!   ..output arguments (unspecified on entry)
  REAL(kind=r8) :: flux(nfc, 0:1), tbfl(nbc), div
  REAL(kind=r8) :: fluxd0(nbdirsmax0, nfc, 0:1), tbfld0(nbdirsmax0, nbc)&
& , divd0(nbdirsmax0)
  REAL(kind=r8) :: fluxd(nbdirsmax, nfc, 0:1), tbfld(nbdirsmax, nbc), &
& divd(nbdirsmax)
  REAL(kind=r8) :: fluxdd(nbdirsmax0, nbdirsmax, nfc, 0:1), tbfldd(&
& nbdirsmax0, nbdirsmax, nbc), divdd(nbdirsmax0, nbdirsmax)
!-----------------------------------------------------------------------
!.documentation
!
!  1. purpose
!
!     B2TBFL computes the total flux through the boundaries.
!            if lcore is .true. then all boundaries are included
!            if lcore is .false. then the core boundary is not included
!
!  8. further comments
!
!-----------------------------------------------------------------------
!.declarations
!
!   ..local variables
  INTEGER :: ib, ibc, icv, ifc
  INTEGER, SAVE :: ncall=0
  INTEGER :: nd
  REAL(kind=r8) :: temp
  INTEGER :: nbdirs
  INTEGER :: nd0
  INTEGER :: nbdirs0
!-----------------------------------------------------------------------
!.computation
!
! ..preliminaries
!   ..subprogram start-up calls
  CALL SUBINI('b2tbfl')
!   ..test nCv, nFc
  CALL XERTST(0 .LT. ncv .AND. 0 .LT. nfc, 'faulty argument nCv, nFc')
  div = 0.0_R8
  tbfld = 0.d0
  divd = 0.d0
  tbfld0 = 0.D0
  tbfldd = 0.D0
  divdd = 0.D0
  divd0 = 0.D0
  DO ib=1,nbc
! compute flux through boundary; positive if out of the domain
    DO nd=1,nbdirs
      DO nd0=nd,nbdirs0
        tbfldd(nd0, nd, ib) = 0.D0
      END DO
      tbfld(nd, ib) = 0.d0
    END DO
    DO nd0=1,nbdirs0
      tbfld0(nd0, ib) = 0.D0
    END DO
    tbfl(ib) = 0.0_R8
! loop over number of cells in the boundary
    DO ibc=1,mpg%bccvp(ib, 2)
! number of the guard cell
      icv = mpg%bccv(mpg%bccvp(ib, 1)+ibc-1, 1)
! number of the guard cell face
      ifc = mpg%cvfc(mpg%cvfcp(icv, 1))
      IF (lcore .OR. (.NOT.mpg%cvonclosedsurface(icv))) THEN
        temp = mpg%bcfcor(mpg%bccvp(ib, 1)+ibc-1)
        DO nd=1,nbdirs
          DO nd0=nd,nbdirs0
            tbfldd(nd0, nd, ib) = tbfldd(nd0, nd, ib) + temp*(fluxdd(nd0&
&             , nd, ifc, 0)+fluxdd(nd0, nd, ifc, 1))
          END DO
          tbfld(nd, ib) = tbfld(nd, ib) + temp*(fluxd(nd, ifc, 0)+fluxd(&
&           nd, ifc, 1))
        END DO
        DO nd0=1,nbdirs0
          tbfld0(nd0, ib) = tbfld0(nd0, ib) + temp*(fluxd0(nd0, ifc, 0)+&
&           fluxd0(nd0, ifc, 1))
        END DO
        tbfl(ib) = tbfl(ib) + temp*(flux(ifc, 0)+flux(ifc, 1))
      END IF
! add boundary flux to total divergence

    END DO
    DO nd=1,nbdirs
      DO nd0=nd,nbdirs0
        divdd(nd0, nd) = divdd(nd0, nd) + tbfldd(nd0, nd, ib)
      END DO
      divd(nd) = divd(nd) + tbfld(nd, ib)
    END DO
    DO nd0=1,nbdirs0
      divd0(nd0) = divd0(nd0) + tbfld0(nd0, ib)
    END DO
    div = div + tbfl(ib)
  END DO
! ..return
  ncall = ncall + 1
  CALL SUBEND()
  RETURN
!
!-----------------------------------------------------------------------
!.end b2tbfl
END SUBROUTINE B2TBFL_DV_DV

!        Generated by TAPENADE     (INRIA, Ecuador team)
!  Tapenade 3.16 (develop) - 23 Jul 2024 17:41
!
!  Differentiation of b2tbfl in forward (tangent) mode (with options multiDirectional context noISIZE r8):
!   variations   of useful results: div
!   with respect to varying inputs: flux
!
!
!
!
!
!
!
!
!
!
!
!srv 12.02.16 {
SUBROUTINE B2TBFL_DV_NODIFF(ncv, nfc, lcore, mpg, flux, fluxd, tbfl, &
& tbfld, div, divd, nbdirs)
  USE B2MOD_TYPES
  USE B2MOD_BOUNDARY_NAMELIST_DIFFV_DIFFV
  USE B2US_MAP_DIFFV_DIFFV
  USE B2MOD_SUBSYS
!  Hint: nbdirsmax should be the maximum number of differentiation directions
  USE B2MOD_DIFFSIZES
  IMPLICIT NONE
!   ..input arguments (unchanged on exit)
  INTEGER :: ncv, nfc
  LOGICAL :: lcore
  TYPE(MAPPING), INTENT(IN) :: mpg
!   ..output arguments (unspecified on entry)
  REAL(kind=r8) :: flux(nfc, 0:1), tbfl(nbc), div
  REAL(kind=r8) :: fluxd(nbdirsmax, nfc, 0:1), tbfld(nbdirsmax, nbc), &
& divd(nbdirsmax)
!-----------------------------------------------------------------------
!.documentation
!
!  1. purpose
!
!     B2TBFL computes the total flux through the boundaries.
!            if lcore is .true. then all boundaries are included
!            if lcore is .false. then the core boundary is not included
!
!  8. further comments
!
!-----------------------------------------------------------------------
!.declarations
!
!   ..local variables
  INTEGER :: ib, ibc, icv, ifc
  INTEGER, SAVE :: ncall=0
  INTEGER :: nd
  REAL(kind=r8) :: temp
  INTEGER :: nbdirs
!-----------------------------------------------------------------------
!.computation
!
! ..preliminaries
!   ..subprogram start-up calls
  CALL SUBINI('b2tbfl')
!   ..test nCv, nFc
  CALL XERTST(0 .LT. ncv .AND. 0 .LT. nfc, 'faulty argument nCv, nFc')
  div = 0.0_R8
  tbfld = 0.d0
  divd = 0.d0
  DO ib=1,nbc
! compute flux through boundary; positive if out of the domain
    DO nd=1,nbdirs
      tbfld(nd, ib) = 0.d0
    END DO
    tbfl(ib) = 0.0_R8
! loop over number of cells in the boundary
    DO ibc=1,mpg%bccvp(ib, 2)
! number of the guard cell
      icv = mpg%bccv(mpg%bccvp(ib, 1)+ibc-1, 1)
! number of the guard cell face
      ifc = mpg%cvfc(mpg%cvfcp(icv, 1))
      IF (lcore .OR. (.NOT.mpg%cvonclosedsurface(icv))) THEN
        temp = mpg%bcfcor(mpg%bccvp(ib, 1)+ibc-1)
        DO nd=1,nbdirs
          tbfld(nd, ib) = tbfld(nd, ib) + temp*(fluxd(nd, ifc, 0)+fluxd(&
&           nd, ifc, 1))
        END DO
        tbfl(ib) = tbfl(ib) + temp*(flux(ifc, 0)+flux(ifc, 1))
      END IF
! add boundary flux to total divergence

    END DO
    DO nd=1,nbdirs
      divd(nd) = divd(nd) + tbfld(nd, ib)
    END DO
    div = div + tbfl(ib)
  END DO
! ..return
  ncall = ncall + 1
  CALL SUBEND()
  RETURN
!
!-----------------------------------------------------------------------
!.end b2tbfl
END SUBROUTINE B2TBFL_DV_NODIFF

!srv 12.02.16 }
!
!
!
!
!
!
!
!
!
!
!
!srv 12.02.16 {
SUBROUTINE B2TBFL_NODIFF_NODIFF(ncv, nfc, lcore, mpg, flux, tbfl, div)
  USE B2MOD_TYPES
  USE B2MOD_BOUNDARY_NAMELIST_DIFFV_DIFFV
  USE B2US_MAP_DIFFV_DIFFV
  USE B2MOD_SUBSYS
  USE B2MOD_DIFFSIZES
  IMPLICIT NONE
!   ..input arguments (unchanged on exit)
  INTEGER :: ncv, nfc
  LOGICAL :: lcore
  TYPE(MAPPING), INTENT(IN) :: mpg
!   ..output arguments (unspecified on entry)
  REAL(kind=r8) :: flux(nfc, 0:1), tbfl(nbc), div
!-----------------------------------------------------------------------
!.documentation
!
!  1. purpose
!
!     B2TBFL computes the total flux through the boundaries.
!            if lcore is .true. then all boundaries are included
!            if lcore is .false. then the core boundary is not included
!
!  8. further comments
!
!-----------------------------------------------------------------------
!.declarations
!
!   ..local variables
  INTEGER :: ib, ibc, icv, ifc
  INTEGER, SAVE :: ncall=0
!-----------------------------------------------------------------------
!.computation
!
! ..preliminaries
!   ..subprogram start-up calls
  CALL SUBINI('b2tbfl')
!   ..test nCv, nFc
  CALL XERTST(0 .LT. ncv .AND. 0 .LT. nfc, 'faulty argument nCv, nFc')
  div = 0.0_R8
  DO ib=1,nbc
! compute flux through boundary; positive if out of the domain
    tbfl(ib) = 0.0_R8
! loop over number of cells in the boundary
    DO ibc=1,mpg%bccvp(ib, 2)
! number of the guard cell
      icv = mpg%bccv(mpg%bccvp(ib, 1)+ibc-1, 1)
! number of the guard cell face
      ifc = mpg%cvfc(mpg%cvfcp(icv, 1))
      IF (lcore .OR. (.NOT.mpg%cvonclosedsurface(icv))) tbfl(ib) = tbfl(&
&         ib) + mpg%bcfcor(mpg%bccvp(ib, 1)+ibc-1)*(flux(ifc, 0)+flux(&
&         ifc, 1))
! add boundary flux to total divergence

    END DO
    div = div + tbfl(ib)
  END DO
! ..return
  ncall = ncall + 1
  CALL SUBEND()
  RETURN
!
!-----------------------------------------------------------------------
!.end b2tbfl
END SUBROUTINE B2TBFL_NODIFF_NODIFF
!srv 12.02.16 }

