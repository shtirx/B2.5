!        Generated by TAPENADE     (INRIA, Ecuador team)
!  Tapenade 3.16 (feature_llhTests) - 27 May 2021 14:23
!
!        Generated by TAPENADE     (INRIA, Ecuador team)
!  Tapenade 3.16 (feature_llhTests) - 27 May 2021 14:23
!
!
!
!
!
!
!
!
!
!
!
!
MODULE B2MOD_NEUTR_SRC_SCALING_DIFFV_DIFFV
!
!  version : 24.07.2002 17:08
!
  USE B2MOD_TYPES
  USE B2MOD_VERSION_DIFFV_DIFFV
!  Hint: nbdirsmax should be the maximum number of differentiation directions
  USE B2MOD_DIFFSIZES
!  Hint: nbdirsmax0 should be the maximum number of differentiation directions
  IMPLICIT NONE
!
  REAL(kind=r8), ALLOCATABLE, SAVE :: sclstra(:, :), sclalph(:, :), &
& sclsrcn(:, :), sclbase(:, :), sclrtio(:, :), tflux(:), flux_scale(:)

CONTAINS
!
!
  SUBROUTINE ALLOC_B2MOD_NEUTR_SRC_SCALING(nsd, nstra)
    USE B2MOD_DIFFSIZES
    IMPLICIT NONE
    INTEGER :: nsd, nstra
!
!xpb The actual used size is (0:natmi,nstra) but natmi is not known yet
    ALLOCATE(sclstra(0:nsd, nstra))
    ALLOCATE(sclalph(0:nsd, nstra))
    ALLOCATE(sclsrcn(0:nsd, nstra))
    ALLOCATE(sclbase(0:nsd, nstra))
    ALLOCATE(sclrtio(0:nsd, nstra))
    ALLOCATE(tflux(nstra))
    ALLOCATE(flux_scale(nstra))
!
    sclstra = 1.0_R8
    sclalph = 1.0_R8
    sclsrcn = 0.0_R8
    sclbase = 1.0_R8
    sclrtio = 1.0_R8
    tflux = 0.0_R8
    flux_scale = 0.0_R8
!
    RETURN
  END SUBROUTINE ALLOC_B2MOD_NEUTR_SRC_SCALING

!
  SUBROUTINE DEALLOC_B2MOD_NEUTR_SRC_SCALING()
    USE B2MOD_DIFFSIZES
    IMPLICIT NONE
    INTRINSIC ALLOCATED
!
    IF (.NOT.ALLOCATED(sclstra)) THEN
      RETURN
    ELSE
      DEALLOCATE(sclstra)
      DEALLOCATE(sclalph)
      DEALLOCATE(sclsrcn)
      DEALLOCATE(sclbase)
      DEALLOCATE(sclrtio)
      DEALLOCATE(tflux)
      DEALLOCATE(flux_scale)
!
      RETURN
    END IF
  END SUBROUTINE DEALLOC_B2MOD_NEUTR_SRC_SCALING

!
  SUBROUTINE READ_B2MOD_NEUTR_SRC_SCALING(nread, ns, nstra)
    USE B2MOD_DIMENSIONS
    USE B2MOD_DIFFSIZES
    IMPLICIT NONE
    INTEGER :: nread, ns, nstra, idum(0:9)
    EXTERNAL CFRUIN, CFRURE
    INTRINSIC SIZE
    INTEGER :: arg1
!
    IF (b2fplasma_version .GE. '01.001.024') THEN
      CALL CFRUIN(nread, 1, idum, 'nstra')
      nstra = idum(0)
    ELSE
      nstra = def_nstra
    END IF
    CALL ALLOC_B2MOD_NEUTR_SRC_SCALING(ns, nstra)
    IF (b2fplasma_version .GE. '01.001.023' .AND. nstra .GT. 0) THEN
      arg1 = SIZE(sclstra)
      CALL CFRURE(nread, arg1, sclstra, 'sclstra')
      arg1 = SIZE(sclrtio)
      CALL CFRURE(nread, arg1, sclrtio, 'sclrtio')
    END IF
!
    RETURN
  END SUBROUTINE READ_B2MOD_NEUTR_SRC_SCALING

!
  SUBROUTINE WRITE_B2MOD_NEUTR_SRC_SCALING(nwrite, nstra)
    USE B2MOD_DIFFSIZES
    IMPLICIT NONE
    INTEGER :: nwrite, nstra, idum(0:9)
    EXTERNAL CFWUIN, CFWURE_NODIFF
    INTRINSIC SIZE
    INTEGER :: arg1
!
    IF (newversion .GE. '01.001.024') THEN
      idum(0) = nstra
      CALL CFWUIN(nwrite, 1, idum, 'nstra')
    END IF
    IF (newversion .GE. '01.001.023' .AND. nstra .GT. 0) THEN
      WRITE(*, *) newversion, 'sclstra, sclrtio'
      arg1 = SIZE(sclstra)
      CALL CFWURE_NODIFF(nwrite, arg1, sclstra, 'sclstra')
      arg1 = SIZE(sclrtio)
      CALL CFWURE_NODIFF(nwrite, arg1, sclrtio, 'sclrtio')
    END IF
!
    RETURN
  END SUBROUTINE WRITE_B2MOD_NEUTR_SRC_SCALING

END MODULE B2MOD_NEUTR_SRC_SCALING_DIFFV_DIFFV

