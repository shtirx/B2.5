!        Generated by TAPENADE     (INRIA, Ecuador team)
!  Tapenade 3.16 (feature_llhTests) - 27 May 2021 14:23
!
!        Generated by TAPENADE     (INRIA, Ecuador team)
!  Tapenade 3.16 (feature_llhTests) - 27 May 2021 14:23
!
!
!
!
!
!
!
!
!
!
!
!
SUBROUTINE INTVERTEX1_NODIFF_NODIFF(ncv, nvx, nz, mpg, vxvol, centre, &
& vertex)
  USE B2MOD_TYPES
  USE B2US_MAP_DIFFV_DIFFV
  USE B2MOD_DIFFSIZES
  IMPLICIT NONE
  TYPE(MAPPING), INTENT(IN) :: mpg
  INTEGER :: ncv, nvx, nz
  REAL(kind=r8) :: vxvol(mpg%nvmxcv), centre(ncv, 0:nz), vertex(nvx, 0:&
& nz)
  INTEGER :: is
  EXTERNAL INTVERTEX_NODIFF_NODIFF
  DO is=0,nz
    CALL INTVERTEX_NODIFF_NODIFF(ncv, nvx, mpg, vxvol, centre(1, is), &
&                          vertex(1, is))
  END DO
  RETURN
END SUBROUTINE INTVERTEX1_NODIFF_NODIFF

!  Differentiation of intvertex_dv in forward (tangent) mode (with options multiDirectional context noISIZE r8):
!   variations   of useful results: vertexd vertex
!   with respect to varying inputs: centred vertexd centre
!  Differentiation of intvertex in forward (tangent) mode (with options multiDirectional context noISIZE r8):
!   variations   of useful results: vertex
!   with respect to varying inputs: centre
!
SUBROUTINE INTVERTEX_DV_DV(ncv, nvx, mpg, vxvol, centre, centred0, &
& centred, centredd, vertex, vertexd0, vertexd, vertexdd, nbdirs, &
& nbdirs0)
  USE B2MOD_TYPES
  USE B2US_MAP_DIFFV_DIFFV
!  Hint: nbdirsmax should be the maximum number of differentiation directions
  USE B2MOD_DIFFSIZES
!  Hint: nbdirsmax0 should be the maximum number of differentiation directions
  IMPLICIT NONE
  INTEGER :: ncv, nvx
  TYPE(MAPPING), INTENT(IN) :: mpg
  REAL(kind=r8) :: vxvol(mpg%nvmxcv), centre(ncv), vertex(nvx)
  REAL(kind=r8) :: centred0(nbdirsmax0, ncv), vertexd0(nbdirsmax0, nvx)
  REAL(kind=r8) :: centred(nbdirsmax, ncv), vertexd(nbdirsmax, nvx)
  REAL(kind=r8) :: centredd(nbdirsmax0, nbdirsmax, ncv), vertexdd(&
& nbdirsmax0, nbdirsmax, nvx)
  INTEGER :: ivx, icv
  REAL(kind=r8) :: volsum
  INTEGER :: nd
  REAL(kind=r8) :: temp
  INTEGER :: nbdirs
  INTEGER :: nd0
  INTEGER :: nbdirs0
!
  vertex = 0.0_R8
  DO nd=1,nbdirsmax
    DO nd0=1,nbdirs0
      vertexdd(nd0, nd, :) = 0.0_8
    END DO
    vertexd(nd, :) = 0.d0
  END DO
  vertexd0(:, :) = 0.0_8
  DO ivx=1,nvx
    volsum = 0.0_R8
    DO icv=1,mpg%vxcvp(ivx, 2)
      volsum = volsum + 1._R8/vxvol(mpg%vxcvp(ivx, 1)+icv-1)
      temp = vxvol(mpg%vxcvp(ivx, 1)+icv-1)
      DO nd=1,nbdirs
        DO nd0=nd,nbdirs0
          vertexdd(nd0, nd, ivx) = vertexdd(nd0, nd, ivx) + centredd(nd0&
&           , nd, mpg%vxcv(mpg%vxcvp(ivx, 1)+icv-1))/temp
        END DO
        vertexd(nd, ivx) = vertexd(nd, ivx) + centred(nd, mpg%vxcv(mpg%&
&         vxcvp(ivx, 1)+icv-1))/temp
      END DO
      DO nd0=1,nbdirs0
        vertexd0(nd0, ivx) = vertexd0(nd0, ivx) + centred0(nd0, mpg%vxcv&
&         (mpg%vxcvp(ivx, 1)+icv-1))/temp
      END DO
      vertex(ivx) = vertex(ivx) + centre(mpg%vxcv(mpg%vxcvp(ivx, 1)+icv-&
&       1))/temp
    END DO
    DO nd=1,nbdirs
      DO nd0=nd,nbdirs0
        vertexdd(nd0, nd, ivx) = vertexdd(nd0, nd, ivx)/(volsum+&
&         1.0e-30_R8)
      END DO
      vertexd(nd, ivx) = vertexd(nd, ivx)/(volsum+1.0e-30_R8)
    END DO
    DO nd0=1,nbdirs0
      vertexd0(nd0, ivx) = vertexd0(nd0, ivx)/(volsum+1.0e-30_R8)
    END DO
    vertex(ivx) = vertex(ivx)/(volsum+1.0e-30_R8)
  END DO
!
  RETURN
END SUBROUTINE INTVERTEX_DV_DV

!  Differentiation of intvertex in forward (tangent) mode (with options multiDirectional context noISIZE r8):
!   variations   of useful results: vertex
!   with respect to varying inputs: centre
!
SUBROUTINE INTVERTEX_DV_NODIFF(ncv, nvx, mpg, vxvol, centre, centred, &
& vertex, vertexd, nbdirs)
  USE B2MOD_TYPES
  USE B2US_MAP_DIFFV_DIFFV
!  Hint: nbdirsmax should be the maximum number of differentiation directions
  USE B2MOD_DIFFSIZES
  IMPLICIT NONE
  INTEGER :: ncv, nvx
  TYPE(MAPPING), INTENT(IN) :: mpg
  REAL(kind=r8) :: vxvol(mpg%nvmxcv), centre(ncv), vertex(nvx)
  REAL(kind=r8) :: centred(nbdirsmax, ncv), vertexd(nbdirsmax, nvx)
  INTEGER :: ivx, icv
  REAL(kind=r8) :: volsum
  INTEGER :: nd
  REAL(kind=r8) :: temp
  INTEGER :: nbdirs
!
  vertex = 0.0_R8
  DO nd=1,nbdirsmax
    vertexd(nd, :) = 0.d0
  END DO
  DO ivx=1,nvx
    volsum = 0.0_R8
    DO icv=1,mpg%vxcvp(ivx, 2)
      volsum = volsum + 1._R8/vxvol(mpg%vxcvp(ivx, 1)+icv-1)
      temp = vxvol(mpg%vxcvp(ivx, 1)+icv-1)
      DO nd=1,nbdirs
        vertexd(nd, ivx) = vertexd(nd, ivx) + centred(nd, mpg%vxcv(mpg%&
&         vxcvp(ivx, 1)+icv-1))/temp
      END DO
      vertex(ivx) = vertex(ivx) + centre(mpg%vxcv(mpg%vxcvp(ivx, 1)+icv-&
&       1))/temp
    END DO
    DO nd=1,nbdirs
      vertexd(nd, ivx) = vertexd(nd, ivx)/(volsum+1.0e-30_R8)
    END DO
    vertex(ivx) = vertex(ivx)/(volsum+1.0e-30_R8)
  END DO
!
  RETURN
END SUBROUTINE INTVERTEX_DV_NODIFF

!
SUBROUTINE INTVERTEX_NODIFF_NODIFF(ncv, nvx, mpg, vxvol, centre, vertex)
  USE B2MOD_TYPES
  USE B2US_MAP_DIFFV_DIFFV
  USE B2MOD_DIFFSIZES
  IMPLICIT NONE
  INTEGER :: ncv, nvx
  TYPE(MAPPING), INTENT(IN) :: mpg
  REAL(kind=r8) :: vxvol(mpg%nvmxcv), centre(ncv), vertex(nvx)
  INTEGER :: ivx, icv
  REAL(kind=r8) :: volsum
!
  vertex = 0.0_R8
  DO ivx=1,nvx
    volsum = 0.0_R8
    DO icv=1,mpg%vxcvp(ivx, 2)
      volsum = volsum + 1._R8/vxvol(mpg%vxcvp(ivx, 1)+icv-1)
      vertex(ivx) = vertex(ivx) + centre(mpg%vxcv(mpg%vxcvp(ivx, 1)+icv-&
&       1))/vxvol(mpg%vxcvp(ivx, 1)+icv-1)
    END DO
    vertex(ivx) = vertex(ivx)/(volsum+1.0e-30_R8)
  END DO
!
  RETURN
END SUBROUTINE INTVERTEX_NODIFF_NODIFF

!  Differentiation of intvertex_s_dv in forward (tangent) mode (with options multiDirectional context noISIZE r8):
!   variations   of useful results: vertexd vertex
!   with respect to varying inputs: centred vertexd centre
!  Differentiation of intvertex_s in forward (tangent) mode (with options multiDirectional context noISIZE r8):
!   variations   of useful results: vertex
!   with respect to varying inputs: centre
!
SUBROUTINE INTVERTEX_S_DV_DV(ivx, ncv, nvx, mpg, vxvol, centre, centred0&
& , centred, centredd, vertex, vertexd0, vertexd, vertexdd, nbdirs, &
& nbdirs0)
!same as intevertex, but interpolation to a single vertex iVx
  USE B2MOD_TYPES
  USE B2US_MAP_DIFFV_DIFFV
!  Hint: nbdirsmax should be the maximum number of differentiation directions
  USE B2MOD_DIFFSIZES
!  Hint: nbdirsmax0 should be the maximum number of differentiation directions
  IMPLICIT NONE
  INTEGER :: ncv, nvx, ivx
  TYPE(MAPPING), INTENT(IN) :: mpg
  REAL(kind=r8), INTENT(IN) :: vxvol(mpg%nvmxcv), centre(ncv)
  REAL(kind=r8), INTENT(IN) :: centred0(nbdirsmax0, ncv)
  REAL(kind=r8), INTENT(IN) :: centred(nbdirsmax, ncv)
  REAL(kind=r8), INTENT(IN) :: centredd(nbdirsmax0, nbdirsmax, ncv)
  REAL(kind=r8), INTENT(OUT) :: vertex
  REAL(kind=r8), DIMENSION(nbdirsmax0), INTENT(OUT) :: vertexd0
  REAL(kind=r8), DIMENSION(nbdirsmax), INTENT(OUT) :: vertexd
  REAL(kind=r8), DIMENSION(nbdirsmax0, nbdirsmax), INTENT(OUT) :: &
& vertexdd
  INTEGER :: icv
  REAL(kind=r8) :: volsum
  INTEGER :: nd
  REAL(kind=r8) :: temp
  INTEGER :: nbdirs
  INTEGER :: nd0
  INTEGER :: nbdirs0
!
  vertex = 0.0_R8
  volsum = 0.0_R8
  DO nd=1,nbdirsmax
    DO nd0=1,nbdirs0
      vertexdd(nd0, nd) = 0.0_8
    END DO
    vertexd(nd) = 0.d0
  END DO
  vertexd0(:) = 0.0_8
  DO icv=1,mpg%vxcvp(ivx, 2)
    volsum = volsum + 1._R8/vxvol(mpg%vxcvp(ivx, 1)+icv-1)
    temp = vxvol(mpg%vxcvp(ivx, 1)+icv-1)
    DO nd=1,nbdirs
      DO nd0=nd,nbdirs0
        vertexdd(nd0, nd) = vertexdd(nd0, nd) + centredd(nd0, nd, mpg%&
&         vxcv(mpg%vxcvp(ivx, 1)+icv-1))/temp
      END DO
      vertexd(nd) = vertexd(nd) + centred(nd, mpg%vxcv(mpg%vxcvp(ivx, 1)&
&       +icv-1))/temp
    END DO
    DO nd0=1,nbdirs0
      vertexd0(nd0) = vertexd0(nd0) + centred0(nd0, mpg%vxcv(mpg%vxcvp(&
&       ivx, 1)+icv-1))/temp
    END DO
    vertex = vertex + centre(mpg%vxcv(mpg%vxcvp(ivx, 1)+icv-1))/temp
  END DO
  DO nd=1,nbdirs
    DO nd0=nd,nbdirs0
      vertexdd(nd0, nd) = vertexdd(nd0, nd)/(volsum+1.0e-30_R8)
    END DO
    vertexd(nd) = vertexd(nd)/(volsum+1.0e-30_R8)
  END DO
  DO nd0=1,nbdirs0
    vertexd0(nd0) = vertexd0(nd0)/(volsum+1.0e-30_R8)
  END DO
  vertex = vertex/(volsum+1.0e-30_R8)
!
  RETURN
END SUBROUTINE INTVERTEX_S_DV_DV

!  Differentiation of intvertex_s in forward (tangent) mode (with options multiDirectional context noISIZE r8):
!   variations   of useful results: vertex
!   with respect to varying inputs: centre
!
SUBROUTINE INTVERTEX_S_DV_NODIFF(ivx, ncv, nvx, mpg, vxvol, centre, &
& centred, vertex, vertexd, nbdirs)
!same as intevertex, but interpolation to a single vertex iVx
  USE B2MOD_TYPES
  USE B2US_MAP_DIFFV_DIFFV
!  Hint: nbdirsmax should be the maximum number of differentiation directions
  USE B2MOD_DIFFSIZES
  IMPLICIT NONE
  INTEGER :: ncv, nvx, ivx
  TYPE(MAPPING), INTENT(IN) :: mpg
  REAL(kind=r8), INTENT(IN) :: vxvol(mpg%nvmxcv), centre(ncv)
  REAL(kind=r8), INTENT(IN) :: centred(nbdirsmax, ncv)
  REAL(kind=r8), INTENT(OUT) :: vertex
  REAL(kind=r8), DIMENSION(nbdirsmax), INTENT(OUT) :: vertexd
  INTEGER :: icv
  REAL(kind=r8) :: volsum
  INTEGER :: nd
  REAL(kind=r8) :: temp
  INTEGER :: nbdirs
!
  vertex = 0.0_R8
  volsum = 0.0_R8
  DO nd=1,nbdirsmax
    vertexd(nd) = 0.d0
  END DO
  DO icv=1,mpg%vxcvp(ivx, 2)
    volsum = volsum + 1._R8/vxvol(mpg%vxcvp(ivx, 1)+icv-1)
    temp = vxvol(mpg%vxcvp(ivx, 1)+icv-1)
    DO nd=1,nbdirs
      vertexd(nd) = vertexd(nd) + centred(nd, mpg%vxcv(mpg%vxcvp(ivx, 1)&
&       +icv-1))/temp
    END DO
    vertex = vertex + centre(mpg%vxcv(mpg%vxcvp(ivx, 1)+icv-1))/temp
  END DO
  DO nd=1,nbdirs
    vertexd(nd) = vertexd(nd)/(volsum+1.0e-30_R8)
  END DO
  vertex = vertex/(volsum+1.0e-30_R8)
!
  RETURN
END SUBROUTINE INTVERTEX_S_DV_NODIFF

!
!
FUNCTION INTVERTEX_S_NODIFF_NODIFF(ivx, ncv, nvx, mpg, vxvol, centre) &
&RESULT (vertex)
!same as intevertex, but interpolation to a single vertex iVx
  USE B2MOD_TYPES
  USE B2US_MAP_DIFFV_DIFFV
  USE B2MOD_DIFFSIZES
  IMPLICIT NONE
  INTEGER :: ncv, nvx, ivx
  TYPE(MAPPING), INTENT(IN) :: mpg
  REAL(kind=r8), INTENT(IN) :: vxvol(mpg%nvmxcv), centre(ncv)
  REAL(kind=r8) :: vertex
  INTEGER :: icv
  REAL(kind=r8) :: volsum
!
  vertex = 0.0_R8
  volsum = 0.0_R8
  DO icv=1,mpg%vxcvp(ivx, 2)
    volsum = volsum + 1._R8/vxvol(mpg%vxcvp(ivx, 1)+icv-1)
    vertex = vertex + centre(mpg%vxcv(mpg%vxcvp(ivx, 1)+icv-1))/vxvol(&
&     mpg%vxcvp(ivx, 1)+icv-1)
  END DO
  vertex = vertex/(volsum+1.0e-30_R8)
!
  RETURN
END FUNCTION INTVERTEX_S_NODIFF_NODIFF
!

