!        Generated by TAPENADE     (INRIA, Ecuador team)
!  Tapenade 3.16 (develop) - 23 Jul 2024 17:41
!
!  Differentiation of intface in forward (tangent) mode (with options multiDirectional context noISIZE r8):
!   variations   of useful results: face
!   with respect to varying inputs: face centre
!
SUBROUTINE INTFACE_DV(ncv, nfc, fccv, fcvol, centre, centred, face, &
& faced, nbdirs)
  USE B2MOD_TYPES
!  Hint: nbdirsmax should be the maximum number of differentiation directions
  USE B2MOD_DIFFSIZES
  IMPLICIT NONE
!
  INTEGER :: ncv, nfc
  INTEGER :: fccv(nfc, 2)
  REAL(kind=r8) :: fcvol(nfc, 2), centre(ncv), face(nfc)
  REAL(kind=r8) :: centred(nbdirsmax, ncv), faced(nbdirsmax, nfc)
  INTEGER :: ifc
  INTEGER :: nd
  REAL(kind=r8) :: temp
  INTEGER :: nbdirs
  DO ifc=1,nfc
    temp = fcvol(ifc, 1) + fcvol(ifc, 2)
    DO nd=1,nbdirs
      faced(nd, ifc) = (fcvol(ifc, 1)*centred(nd, fccv(ifc, 2))+fcvol(&
&       ifc, 2)*centred(nd, fccv(ifc, 1)))/temp
    END DO
    face(ifc) = (fcvol(ifc, 1)*centre(fccv(ifc, 2))+fcvol(ifc, 2)*centre&
&     (fccv(ifc, 1)))/temp
  END DO
  RETURN
END SUBROUTINE INTFACE_DV

!  Differentiation of intface_s in forward (tangent) mode (with options multiDirectional context noISIZE r8):
!   variations   of useful results: face
!   with respect to varying inputs: centre
!
SUBROUTINE INTFACE_S_DV(ifc, ncv, nfc, fccv, fcvol, centre, centred, &
& face, faced, nbdirs)
!same as intface, but interpolation to a single face iFc
  USE B2MOD_TYPES
!  Hint: nbdirsmax should be the maximum number of differentiation directions
  USE B2MOD_DIFFSIZES
  IMPLICIT NONE
!
  INTEGER, INTENT(IN) :: ncv, nfc, ifc
  INTEGER, INTENT(IN) :: fccv(nfc, 2)
  REAL(kind=r8), INTENT(IN) :: fcvol(nfc, 2), centre(ncv)
  REAL(kind=r8), INTENT(IN) :: centred(nbdirsmax, ncv)
  REAL(kind=r8), INTENT(OUT) :: face
  REAL(kind=r8), DIMENSION(nbdirsmax), INTENT(OUT) :: faced
  INTEGER :: nd
  REAL(kind=r8) :: temp
  INTEGER :: nbdirs
!
  temp = fcvol(ifc, 1) + fcvol(ifc, 2)
  DO nd=1,nbdirs
    faced(nd) = (fcvol(ifc, 1)*centred(nd, fccv(ifc, 2))+fcvol(ifc, 2)*&
&     centred(nd, fccv(ifc, 1)))/temp
  END DO
  face = (fcvol(ifc, 1)*centre(fccv(ifc, 2))+fcvol(ifc, 2)*centre(fccv(&
&   ifc, 1)))/temp
  RETURN
END SUBROUTINE INTFACE_S_DV

