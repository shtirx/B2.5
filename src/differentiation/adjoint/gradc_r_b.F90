!        Generated by TAPENADE     (INRIA, Ecuador team)
!  Tapenade 3.16 (develop) - 23 Jul 2024 17:41
!
!  Differentiation of gradc_r in reverse (adjoint) mode (with options context noISIZE r8):
!   gradient     of useful results: gfunr funv fun
!   with respect to varying inputs: funv fun
!   Plus diff mem management of: mpg.intcellr:in geo.fchc:in geo.fcht:in
!                geo.fcqgam:in geo.fcqalf:in geo.fcqbet:in geo.vxvol:in
!
!
!
!
!
!
!
!
!
!
!
!-----------------------------------------------------------------------
!.specification
!
SUBROUTINE GRADC_R_B(ncv, nfc, nvx, mode, geo, geob, mpg, mpgb, fun, &
& funb, funv, funvb, gfunr, gfunrb)
  USE B2MOD_TYPES
  USE B2US_GEO_DIFF
  USE B2US_MAP_DIFF
  IMPLICIT NONE
!   ..input arguments
  INTEGER, INTENT(IN) :: ncv, nfc, nvx, mode
  TYPE(GEOMETRY), INTENT(IN) :: geo
  TYPE(GEOMETRY_DIFF) :: geob
  TYPE(MAPPING), INTENT(IN) :: mpg
  TYPE(MAPPING_DIFF) :: mpgb
  REAL(kind=r8) :: fun(ncv), funv(nvx)
  REAL(kind=r8) :: funb(ncv), funvb(nvx)
!   ..output arguments
  REAL(kind=r8) :: gfunr(ncv)
  REAL(kind=r8) :: gfunrb(ncv)
!-----------------------------------------------------------------------
!.documentation
!
!     GRADC_R computes gradients of a cell centered quantity fun in the 
!     radial direction, in cell centers. The routine first computes
!     the gradients on faces, and then interpolates to cell centers.
!
!-----------------------------------------------------------------------
!.declarations
!
!   ..local variables
  REAL(kind=r8) :: gfunrf(nfc)
  REAL(kind=r8) :: gfunrfb(nfc)
  INTEGER*4 :: branch
!
!-----------------------------------------------------------------------
!.computation      
!
!   ..test mode
!
!   ..interpolate fun to cell vertices, using volume weighted averaging
  IF (mode .EQ. 0) THEN
    CALL INTVERTEX_FWD(ncv, nvx, mpg, geo%vxvol, fun, funv)
    CALL PUSHCONTROL1B(0)
  ELSE
    CALL PUSHCONTROL1B(1)
  END IF
!
!   ..compute gradients on faces
!
!   ..interpolate to centers
  gfunrf = 1.0_R8 !csc added this here for safety in gfortran
  CALL INTCELL_FWD(nfc, ncv, mpg, mpg%intcellr, gfunrf, gfunr)
!
!   ..return
!
!-----------------------------------------------------------------------
!.end gradc_r
!
  gfunrfb = 0.D0
  CALL INTCELL_BWD(nfc, ncv, mpg, mpg%intcellr, gfunrf, gfunrfb, gfunr, &
&            gfunrb)
  CALL GRAD_R_B(ncv, nfc, nvx, 1, geo, mpg, mpgb, fun, funb, funv, funvb&
&         , gfunrf, gfunrfb)
  CALL POPCONTROL1B(branch)
  IF (branch .EQ. 0) THEN
    CALL INTVERTEX_BWD(ncv, nvx, mpg, geo%vxvol, fun, funb, funv, funvb)
    funvb = 0.D0
  END IF
END SUBROUTINE GRADC_R_B

!
!
!
!
!
!
!
!
!
!
!
!-----------------------------------------------------------------------
!.specification
!
SUBROUTINE GRADC_R_NODIFF(ncv, nfc, nvx, mode, geo, mpg, fun, funv, &
& gfunr)
  USE B2MOD_TYPES
  USE B2US_GEO_DIFF
  USE B2US_MAP_DIFF
  IMPLICIT NONE
!   ..input arguments
  INTEGER, INTENT(IN) :: ncv, nfc, nvx, mode
  TYPE(GEOMETRY), INTENT(IN) :: geo
  TYPE(MAPPING), INTENT(IN) :: mpg
  REAL(kind=r8) :: fun(ncv), funv(nvx)
!   ..output arguments
  REAL(kind=r8) :: gfunr(ncv)
!-----------------------------------------------------------------------
!.documentation
!
!     GRADC_R computes gradients of a cell centered quantity fun in the 
!     radial direction, in cell centers. The routine first computes
!     the gradients on faces, and then interpolates to cell centers.
!
!-----------------------------------------------------------------------
!.declarations
!
!   ..local variables
  REAL(kind=r8) :: gfunrf(nfc)
!
!-----------------------------------------------------------------------
!.computation      
!
!   ..test mode
  CALL XERTST(0 .LE. mode .AND. mode .LE. 1, &
&       'gradc_r--faulty argument mode')
!
!   ..interpolate fun to cell vertices, using volume weighted averaging
  IF (mode .EQ. 0) CALL INTVERTEX_NODIFF(ncv, nvx, mpg, geo%vxvol, fun, &
&                                  funv)
!
!   ..compute gradients on faces
  CALL GRAD_R_NODIFF(ncv, nfc, nvx, 1, geo, mpg, fun, funv, gfunrf)
!
!   ..interpolate to centers
  CALL INTCELL_NODIFF(nfc, ncv, mpg, mpg%intcellr, gfunrf, gfunr)
!
!   ..return
  RETURN
!
!-----------------------------------------------------------------------
!.end gradc_r
!
END SUBROUTINE GRADC_R_NODIFF

