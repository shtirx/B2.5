!        Generated by TAPENADE     (INRIA, Ecuador team)
!  Tapenade 3.16 (develop) - 23 Jul 2024 17:41
!
!  Differentiation of grad in reverse (adjoint) mode (with options context noISIZE r8):
!   gradient     of useful results: gfun funv fun
!   with respect to varying inputs: gfun funv fun
!   Plus diff mem management of: geo.fchc:in geo.fcht:in geo.fcqgam:in
!                geo.fcqalf:in geo.fcqbet:in geo.vxvol:in
!
!
!
!
!
!
!
!
!
!
!
!-----------------------------------------------------------------------
!.specification
!
SUBROUTINE GRAD_B(ncv, nfc, nvx, mode, geo, geob, mpg, mpgb, fun, funb, &
& funv, funvb, gfun, gfunb)
  USE B2MOD_TYPES
  USE B2US_GEO_DIFF
  USE B2US_MAP_DIFF
  IMPLICIT NONE
!   ..input arguments
  INTEGER, INTENT(IN) :: ncv, nfc, nvx, mode
  TYPE(GEOMETRY), INTENT(IN) :: geo
  TYPE(GEOMETRY_DIFF) :: geob
  TYPE(MAPPING), INTENT(IN) :: mpg
  TYPE(MAPPING_DIFF) :: mpgb
  REAL(kind=r8) :: fun(ncv), funv(nvx)
  REAL(kind=r8) :: funb(ncv), funvb(nvx)
!   ..output arguments
  REAL(kind=r8) :: gfun(nfc, 0:1)
  REAL(kind=r8) :: gfunb(nfc, 0:1)
!-----------------------------------------------------------------------
!.documentation
!
!     GRAD computes gradients of a cell centered quantity fun on
!     faces of a cell.
!     gfun(:,0) will hold poloidal gradients
!     gfun(:,1) will hold radial gradients
!
!     The routine is a simple interface to GRAD_P and GRAD_R, that will
!     compute the actual poloidal and radial gradients
!
!-----------------------------------------------------------------------
!.declarations
!
!   ..procedures
  EXTERNAL GRAD_P_NODIFF, GRAD_R_NODIFF
  EXTERNAL GRAD_P_FWD, GRAD_P_BWD, GRAD_R_B
  INTEGER*4 :: branch
!
!-----------------------------------------------------------------------
!.computation      
!
!   ..test mode
!
!   ..interpolate fun to cell vertices, using volume weighted averaging
  IF (mode .EQ. 0) THEN
    CALL INTVERTEX_FWD(ncv, nvx, mpg, geo%vxvol, fun, funv)
    CALL PUSHCONTROL1B(0)
  ELSE
    CALL PUSHCONTROL1B(1)
  END IF
!
!   ..compute poloidal and radial gradients
  CALL GRAD_P_FWD(ncv, nfc, nvx, 1, geo, mpg, mpgb, fun, funv, gfun(1, 0&
&           ))
!
!   ..return
!
!-----------------------------------------------------------------------
!.end grad
!
  CALL GRAD_R_B(ncv, nfc, nvx, 1, geo, mpg, mpgb, fun, funb, funv, funvb&
&         , gfun(1, 1), gfunb(1, 1))
  CALL GRAD_P_BWD(ncv, nfc, nvx, 1, geo, mpg, mpgb, fun, funb, funv, &
&           funvb, gfun(1, 0), gfunb(1, 0))
  CALL POPCONTROL1B(branch)
  IF (branch .EQ. 0) THEN
    CALL INTVERTEX_BWD(ncv, nvx, mpg, geo%vxvol, fun, funb, funv, funvb)
    funvb = 0.D0
  END IF
END SUBROUTINE GRAD_B

!
!
!
!
!
!
!
!
!
!
!
!-----------------------------------------------------------------------
!.specification
!
SUBROUTINE GRAD_NODIFF(ncv, nfc, nvx, mode, geo, mpg, fun, funv, gfun)
  USE B2MOD_TYPES
  USE B2US_GEO_DIFF
  USE B2US_MAP_DIFF
  IMPLICIT NONE
!   ..input arguments
  INTEGER, INTENT(IN) :: ncv, nfc, nvx, mode
  TYPE(GEOMETRY), INTENT(IN) :: geo
  TYPE(MAPPING), INTENT(IN) :: mpg
  REAL(kind=r8) :: fun(ncv), funv(nvx)
!   ..output arguments
  REAL(kind=r8) :: gfun(nfc, 0:1)
!-----------------------------------------------------------------------
!.documentation
!
!     GRAD computes gradients of a cell centered quantity fun on
!     faces of a cell.
!     gfun(:,0) will hold poloidal gradients
!     gfun(:,1) will hold radial gradients
!
!     The routine is a simple interface to GRAD_P and GRAD_R, that will
!     compute the actual poloidal and radial gradients
!
!-----------------------------------------------------------------------
!.declarations
!
!   ..procedures
  EXTERNAL GRAD_P_NODIFF, GRAD_R_NODIFF
!
!-----------------------------------------------------------------------
!.computation      
!
!   ..test mode
  CALL XERTST(0 .LE. mode .AND. mode .LE. 1, &
&       'grad--faulty argument mode')
!
!   ..interpolate fun to cell vertices, using volume weighted averaging
  IF (mode .EQ. 0) CALL INTVERTEX_NODIFF(ncv, nvx, mpg, geo%vxvol, fun, &
&                                  funv)
!
!   ..compute poloidal and radial gradients
  CALL GRAD_P_NODIFF(ncv, nfc, nvx, 1, geo, mpg, fun, funv, gfun(1, 0))
  CALL GRAD_R_NODIFF(ncv, nfc, nvx, 1, geo, mpg, fun, funv, gfun(1, 1))
!
!   ..return
  RETURN
!
!-----------------------------------------------------------------------
!.end grad
!
END SUBROUTINE GRAD_NODIFF

