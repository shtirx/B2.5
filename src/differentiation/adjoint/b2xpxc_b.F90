!        Generated by TAPENADE     (INRIA, Ecuador team)
!  Tapenade 3.16 (develop) - 13 Feb 2024 17:09
!
!
!
!
!
!
!
!
!
!
!
!
SUBROUTINE B2XPXC_NODIFF(nx, ny, funx, fun)
  USE B2MOD_TYPES
  USE B2MOD_INDIRECT_DIFF
  IMPLICIT NONE
  INTEGER :: nx, ny
  REAL(kind=r8) :: funx(-1:nx, -1:ny), fun(-1:nx, -1:ny)
!     ------------------------------------------------------------------
!      B2XPXC is obsolete.
!      B2XPXC interpolates a function from x-faces to cell centers.
!     ------------------------------------------------------------------
  INTEGER :: ix, iy
!     ------------------------------------------------------------------
!$$$  call subini ('b2xpxc')
  DO iy=-1,ny
    DO ix=-1,nx
      IF (leftix(ix, iy) .EQ. -2) THEN
        fun(ix, iy) = funx(rightix(ix, iy), rightiy(ix, iy))
      ELSE IF (rightix(ix, iy) .EQ. nx + 1) THEN
        fun(ix, iy) = funx(ix, iy)
      ELSE
        fun(ix, iy) = (funx(ix, iy)+funx(rightix(ix, iy), rightiy(ix, iy&
&         )))/2
      END IF
    END DO
  END DO
!$$$  call subend ()
  RETURN
!     ------------------------------------------------------------------
END SUBROUTINE B2XPXC_NODIFF

