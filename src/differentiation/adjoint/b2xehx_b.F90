!        Generated by TAPENADE     (INRIA, Ecuador team)
!  Tapenade 3.16 (develop) - 23 Jul 2024 17:41
!
!  Differentiation of b2xehx in reverse (adjoint) mode (with options context noISIZE r8):
!   gradient     of useful results: ehx ne po te
!   with respect to varying inputs: ne po te
!   Plus diff mem management of: geo.fchc:in geo.fcht:in geo.fcqalf:in
!                geo.fcqbet:in geo.vxvol:in
!
!
!
!
!
!
!
!
!
!
!
!-----------------------------------------------------------------------
!.specification
!
!srv 13.01.17
SUBROUTINE B2XEHX_B(ncv, nfc, nvx, switch, geo, geob, mpg, mpgb, te, teb&
& , po, pob, ne, neb, ehx, ehxb)
  USE B2MOD_TYPES
  USE B2MOD_CONSTANTS
  USE B2US_GEO_DIFF
  USE B2US_MAP_DIFF
  USE B2MOD_SWITCHES_DIFF
! csc The following are not necessary for computation but are needed
!     for adjoint AD to avoid side-effect variables
  USE B2MOD_AD_DIFF, ONLY : ncall_b2xehx
  USE B2MOD_SUBSYS
  IMPLICIT NONE
!   ..input arguments (unchanged on exit)
  INTEGER :: ncv, nfc, nvx
  TYPE(SWITCHES), INTENT(IN) :: switch
  TYPE(GEOMETRY), INTENT(IN) :: geo
  TYPE(GEOMETRY_DIFF) :: geob
  TYPE(MAPPING), INTENT(IN) :: mpg
  TYPE(MAPPING_DIFF) :: mpgb
  REAL(kind=r8) :: te(ncv), po(ncv), ne(ncv)
  REAL(kind=r8) :: teb(ncv), pob(ncv), neb(ncv)
!   ..output arguments (unspecified on entry)
  REAL(kind=r8) :: ehx(nfc)
  REAL(kind=r8) :: ehxb(nfc)
!   ..common blocks
!-----------------------------------------------------------------------
!.documentation
!
!  1. purpose
!
!     B2XEHX computes the modified poloidal electrical field on all cell
!     faces.
!
!  8. further comments
!
!-----------------------------------------------------------------------
!.declarations
!
!   ..global named constants
!
!   ..local variables
  REAL(kind=r8) :: dpop(nfc), wrk0(nfc), wrk1(nfc), wrk2(nfc), wrkv(nvx)&
& , nete(ncv), ones(nfc, 2)
  REAL(kind=r8) :: dpopb(nfc), wrk0b(nfc), wrk1b(nfc), wrk2b(nfc), wrkvb&
& (nvx), neteb(ncv)
!   ..procedures
  EXTERNAL XERTST
  EXTERNAL B2XVSG
  INTRINSIC LOG
  REAL(kind=r8), DIMENSION(ncv) :: arg1
  REAL(kind=r8), DIMENSION(ncv) :: arg1b
!   ..initialization
!-----------------------------------------------------------------------
!.computation
!
! ..preliminaries
!   ..subprogram start-up calls
!   ..test nCv, nFc
!   ..extensive tests on first few calls
  REAL(kind=r8), DIMENSION(nfc) :: tempb
!srv 29.01.20 }
!
!   ..compute ehx
  IF (.NOT.(switch%pot_eq .NE. 1 .AND. switch%no_current .NE. 0)) THEN
    IF (switch%mode_ehx .EQ. 0) THEN
      ones = 1.0_R8
      nete = ne*te
!       ..compute poloidal differences of po and pe
      CALL DIFF_P_FWD(ncv, nfc, nvx, 0, geo, mpg, mpgb, po, wrkv, dpop)
      CALL DIFF_P_FWD(ncv, nfc, nvx, 0, geo, mpg, mpgb, nete, wrkv, wrk0&
&              )
!       ..interpolate ne to faces
      CALL INTFACE_FWD(ncv, nfc, mpg%fccv, ones, ne, wrk1)
!       ..compute ehx      
      wrk0b = 0.D0
      wrk1b = 0.D0
      dpopb = 0.D0
      tempb = ehxb/(qe*wrk1)
      dpopb = -ehxb
      wrk0b = tempb
      wrk1b = -(wrk0*tempb/wrk1)
      CALL INTFACE_BWD(ncv, nfc, mpg%fccv, ones, ne, neb, wrk1, wrk1b)
      neteb = 0.D0
      wrkvb = 0.D0
      CALL DIFF_P_BWD(ncv, nfc, nvx, 0, geo, mpg, mpgb, nete, neteb, &
&               wrkv, wrkvb, wrk0, wrk0b)
      CALL DIFF_P_BWD(ncv, nfc, nvx, 0, geo, mpg, mpgb, po, pob, wrkv, &
&               wrkvb, dpop, dpopb)
      neb = neb + te*neteb
      teb = teb + ne*neteb
    ELSE IF (switch%mode_ehx .EQ. 1) THEN
      nete = ne*te
!       ..compute poloidal differences of po and pe
      CALL DIFF_P_FWD(ncv, nfc, nvx, 0, geo, mpg, mpgb, po, wrkv, dpop)
      CALL DIFF_P_FWD(ncv, nfc, nvx, 0, geo, mpg, mpgb, nete, wrkv, wrk0&
&              )
!       ..interpolate ne to faces
      CALL INTFACE_FWD(ncv, nfc, mpg%fccv, geo%fchc, ne, wrk1)
!       ..compute ehx
      wrk0b = 0.D0
      wrk1b = 0.D0
      dpopb = 0.D0
      tempb = ehxb/(qe*wrk1)
      dpopb = -ehxb
      wrk0b = tempb
      wrk1b = -(wrk0*tempb/wrk1)
      CALL INTFACE_BWD(ncv, nfc, mpg%fccv, geo%fchc, ne, neb, wrk1, &
&                wrk1b)
      neteb = 0.D0
      wrkvb = 0.D0
      CALL DIFF_P_BWD(ncv, nfc, nvx, 0, geo, mpg, mpgb, nete, neteb, &
&               wrkv, wrkvb, wrk0, wrk0b)
      CALL DIFF_P_BWD(ncv, nfc, nvx, 0, geo, mpg, mpgb, po, pob, wrkv, &
&               wrkvb, dpop, dpopb)
      neb = neb + te*neteb
      teb = teb + ne*neteb
    ELSE
      ones = 1.0_R8
!       ..compute poloidal differences of po, te and log(ne) on faces
      CALL DIFF_P_FWD(ncv, nfc, nvx, 0, geo, mpg, mpgb, po, wrkv, dpop)
      CALL DIFF_P_FWD(ncv, nfc, nvx, 0, geo, mpg, mpgb, te, wrkv, wrk0)
      arg1 = LOG(ne)
      CALL DIFF_P_FWD(ncv, nfc, nvx, 0, geo, mpg, mpgb, arg1, wrkv, wrk1&
&              )
!       ..interpolate te to faces
      CALL INTFACE_FWD(ncv, nfc, mpg%fccv, ones, te, wrk2)
!       ..compute ehx      
      wrk0b = 0.D0
      wrk1b = 0.D0
      wrk2b = 0.D0
      dpopb = 0.D0
      tempb = ehxb/qe
      dpopb = -ehxb
      wrk0b = tempb
      wrk1b = wrk2*tempb
      wrk2b = wrk1*tempb
      CALL INTFACE_BWD(ncv, nfc, mpg%fccv, ones, te, teb, wrk2, wrk2b)
      arg1b = 0.D0
      wrkvb = 0.D0
      CALL DIFF_P_BWD(ncv, nfc, nvx, 0, geo, mpg, mpgb, arg1, arg1b, &
&               wrkv, wrkvb, wrk1, wrk1b)
      neb = neb + arg1b/ne
      CALL DIFF_P_BWD(ncv, nfc, nvx, 0, geo, mpg, mpgb, te, teb, wrkv, &
&               wrkvb, wrk0, wrk0b)
      CALL DIFF_P_BWD(ncv, nfc, nvx, 0, geo, mpg, mpgb, po, pob, wrkv, &
&               wrkvb, dpop, dpopb)
    END IF
  END IF
END SUBROUTINE B2XEHX_B

!
!
!
!
!
!
!
!
!
!
!
!-----------------------------------------------------------------------
!.specification
!
!srv 13.01.17
SUBROUTINE B2XEHX_NODIFF(ncv, nfc, nvx, switch, geo, mpg, te, po, ne, &
& ehx)
  USE B2MOD_TYPES
  USE B2MOD_CONSTANTS
  USE B2US_GEO_DIFF
  USE B2US_MAP_DIFF
  USE B2MOD_SWITCHES_DIFF
! csc The following are not necessary for computation but are needed
!     for adjoint AD to avoid side-effect variables
  USE B2MOD_AD_DIFF, ONLY : ncall_b2xehx
  USE B2MOD_SUBSYS
  IMPLICIT NONE
!   ..input arguments (unchanged on exit)
  INTEGER :: ncv, nfc, nvx
  TYPE(SWITCHES), INTENT(IN) :: switch
  TYPE(GEOMETRY), INTENT(IN) :: geo
  TYPE(MAPPING), INTENT(IN) :: mpg
  REAL(kind=r8) :: te(ncv), po(ncv), ne(ncv)
!   ..output arguments (unspecified on entry)
  REAL(kind=r8) :: ehx(nfc)
!   ..common blocks
!-----------------------------------------------------------------------
!.documentation
!
!  1. purpose
!
!     B2XEHX computes the modified poloidal electrical field on all cell
!     faces.
!
!  8. further comments
!
!-----------------------------------------------------------------------
!.declarations
!
!   ..global named constants
!
!   ..local variables
  REAL(kind=r8) :: dpop(nfc), wrk0(nfc), wrk1(nfc), wrk2(nfc), wrkv(nvx)&
& , nete(ncv), ones(nfc, 2)
!   ..procedures
  EXTERNAL XERTST
  EXTERNAL B2XVSG
  INTRINSIC LOG
  REAL(kind=r8), DIMENSION(ncv) :: arg1
!   ..initialization
!-----------------------------------------------------------------------
!.computation
!
! ..preliminaries
!   ..subprogram start-up calls
  CALL SUBINI('b2xehx')
!   ..test nCv, nFc
  CALL XERTST(0 .LT. ncv .AND. 0 .LT. nfc, 'faulty argument nCv, nFc')
!   ..extensive tests on first few calls
  IF (ncall_b2xehx .LT. 3) THEN
!    ..test sign of ne, te, csig
    CALL B2XVSG(ncv, ne, 1, 'ne', '.gt.')
    CALL B2XVSG(ncv, te, 1, 'te', '.gt.')
  END IF
!srv 29.01.20 }
!
!   ..compute ehx
  IF (switch%pot_eq .NE. 1 .AND. switch%no_current .NE. 0) THEN
!srv 29.01.20 {
    ehx = 0.0_R8
  ELSE IF (switch%mode_ehx .EQ. 0) THEN
    ones = 1.0_R8
    nete = ne*te
!       ..compute poloidal differences of po and pe
    CALL DIFF_P_NODIFF(ncv, nfc, nvx, 0, geo, mpg, po, wrkv, dpop)
    CALL DIFF_P_NODIFF(ncv, nfc, nvx, 0, geo, mpg, nete, wrkv, wrk0)
!       ..interpolate ne to faces
    CALL INTFACE(ncv, nfc, mpg%fccv, ones, ne, wrk1)
!       ..compute ehx      
    ehx = -dpop + wrk0/(qe*wrk1)
  ELSE IF (switch%mode_ehx .EQ. 1) THEN
    nete = ne*te
!       ..compute poloidal differences of po and pe
    CALL DIFF_P_NODIFF(ncv, nfc, nvx, 0, geo, mpg, po, wrkv, dpop)
    CALL DIFF_P_NODIFF(ncv, nfc, nvx, 0, geo, mpg, nete, wrkv, wrk0)
!       ..interpolate ne to faces
    CALL INTFACE(ncv, nfc, mpg%fccv, geo%fchc, ne, wrk1)
!       ..compute ehx
    ehx = -dpop + wrk0/(qe*wrk1)
  ELSE
    ones = 1.0_R8
!       ..compute poloidal differences of po, te and log(ne) on faces
    CALL DIFF_P_NODIFF(ncv, nfc, nvx, 0, geo, mpg, po, wrkv, dpop)
    CALL DIFF_P_NODIFF(ncv, nfc, nvx, 0, geo, mpg, te, wrkv, wrk0)
    arg1 = LOG(ne)
    CALL DIFF_P_NODIFF(ncv, nfc, nvx, 0, geo, mpg, arg1, wrkv, wrk1)
!       ..interpolate te to faces
    CALL INTFACE(ncv, nfc, mpg%fccv, ones, te, wrk2)
!       ..compute ehx      
    ehx = -dpop + (wrk0+wrk1*wrk2)/qe
  END IF
!
! ..return
  ncall_b2xehx = ncall_b2xehx + 1
  CALL SUBEND()
  RETURN
!
!-----------------------------------------------------------------------
!.end b2xehx
!
END SUBROUTINE B2XEHX_NODIFF

