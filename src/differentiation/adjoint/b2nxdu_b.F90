!        Generated by TAPENADE     (INRIA, Ecuador team)
!  Tapenade 3.16 (develop) - 13 Feb 2024 17:09
!
!  Differentiation of b2nxdu in reverse (adjoint) mode (with options context noISIZE r8):
!   gradient     of useful results: smq na ua smqdu wrk0 wrk1
!   with respect to varying inputs: smq na ua
!
!
!
!
!
!
!
!
!
!
!
!-----------------------------------------------------------------------
!.specification
!
SUBROUTINE B2NXDU_B(ncv, ns, na, nab, ua, uab, smq, smqb, smqdu, smqdub&
& , wrk0, wrk0b, wrk1, wrk1b)
  USE B2MOD_TYPES
  USE B2MOD_CONSTANTS
  USE B2MOD_B2CMPA_DIFF
! csc The following are not necessary for computation but are needed
!     for adjoint AD to avoid side-effect variables
  USE B2MOD_AD_DIFF, ONLY : ncall_b2nxdu
  USE B2MOD_SUBSYS
  IMPLICIT NONE
!   ..input arguments (unchanged on exit)
  INTEGER :: ncv, ns
  REAL(kind=r8) :: na(ncv, 0:ns-1), ua(ncv, 0:ns-1), smq(ncv, 0:3, 0:ns-&
& 1)
  REAL(kind=r8) :: nab(ncv, 0:ns-1), uab(ncv, 0:ns-1), smqb(ncv, 0:3, 0:&
& ns-1)
!   ..output arguments (unspecified on entry)
  REAL(kind=r8) :: smqdu(ncv)
  REAL(kind=r8) :: smqdub(ncv)
!   ..workspace arguments (unspecified on entry and on exit)
  REAL(kind=r8) :: wrk0(ncv), wrk1(ncv)
  REAL(kind=r8) :: wrk0b(ncv), wrk1b(ncv)
!   ..common blocks
!-----------------------------------------------------------------------
!.documentation
!
!  1. purpose
!
!     B2NXDU computes the artificial velocity shift that is
!     required to make the total numerical friction force cancel.
!
!-----------------------------------------------------------------------
!.declarations
!
!   ..local variables
  INTEGER :: icv, is
!   ..procedures
  EXTERNAL XERTST, SFILL_NODIFF
  EXTERNAL SFILL_FWD, SFILL_BWD
  EXTERNAL B2XVSG
!   ..initialisation
!
!-----------------------------------------------------------------------
!.computation
!
! ..preliminaries
!   ..subprogram start-up calls
!   ..test nCv
!   ..extensive tests on first few calls
  REAL(kind=r8), DIMENSION(ncv) :: tempb
  REAL(kind=r8), DIMENSION(ncv) :: tempb0
  REAL(kind=r8) :: tempb1
  INTEGER*4 :: branch
!
! ..compute smqdu
!   ..compute auxiliaries wrk0 and wrk1
  CALL SFILL_NODIFF(ncv, 0.0_R8, wrk0, 1)
  CALL SFILL_NODIFF(ncv, 0.0_R8, wrk1, 1)
  DO is=0,ns-1
    CALL PUSHREAL8ARRAY(wrk0, r8*ncv/8)
    wrk0 = wrk0 + smq(:, 1, is) + smq(:, 3, is)*am(is)*mp*na(:, is)
    CALL PUSHREAL8ARRAY(wrk1, r8*ncv/8)
    wrk1 = wrk1 + smq(:, 0, is) + smq(:, 1, is)*ua(:, is) + smq(:, 2, is&
&     )*am(is)*mp*na(:, is) + smq(:, 3, is)*am(is)*mp*na(:, is)*ua(:, is&
&     )
  END DO
!   ..compute smqdu
  DO icv=1,ncv
    IF (wrk0(icv) .EQ. 0.0_R8) THEN
      CALL PUSHCONTROL1B(1)
    ELSE
      CALL PUSHCONTROL1B(0)
    END IF
  END DO
  DO icv=ncv,1,-1
    CALL POPCONTROL1B(branch)
    IF (branch .EQ. 0) THEN
      tempb1 = smqdub(icv)/wrk0(icv)
      smqdub(icv) = 0.D0
      wrk1b(icv) = wrk1b(icv) + tempb1
      wrk0b(icv) = wrk0b(icv) - wrk1(icv)*tempb1/wrk0(icv)
    ELSE
      smqdub(icv) = 0.D0
    END IF
  END DO
  DO is=ns-1,0,-1
    tempb0 = am(is)*mp*wrk1b
    CALL POPREAL8ARRAY(wrk1, r8*ncv/8)
    smqb(:, 0, is) = smqb(:, 0, is) + wrk1b
    smqb(:, 1, is) = smqb(:, 1, is) + ua(:, is)*wrk1b + wrk0b
    uab(:, is) = uab(:, is) + smq(:, 1, is)*wrk1b + na(:, is)*smq(:, 3, &
&     is)*tempb0
    tempb = am(is)*mp*wrk1b
    nab(:, is) = nab(:, is) + ua(:, is)*smq(:, 3, is)*tempb0 + smq(:, 2&
&     , is)*tempb
    smqb(:, 2, is) = smqb(:, 2, is) + na(:, is)*tempb
    CALL POPREAL8ARRAY(wrk0, r8*ncv/8)
    tempb = am(is)*mp*wrk0b
    smqb(:, 3, is) = smqb(:, 3, is) + na(:, is)*ua(:, is)*tempb0 + na(:&
&     , is)*tempb
    nab(:, is) = nab(:, is) + smq(:, 3, is)*tempb
  END DO
END SUBROUTINE B2NXDU_B

!
!
!
!
!
!
!
!
!
!
!
!-----------------------------------------------------------------------
!.specification
!
SUBROUTINE B2NXDU_NODIFF(ncv, ns, na, ua, smq, smqdu, wrk0, wrk1)
  USE B2MOD_TYPES
  USE B2MOD_CONSTANTS
  USE B2MOD_B2CMPA_DIFF
! csc The following are not necessary for computation but are needed
!     for adjoint AD to avoid side-effect variables
  USE B2MOD_AD_DIFF, ONLY : ncall_b2nxdu
  USE B2MOD_SUBSYS
  IMPLICIT NONE
!   ..input arguments (unchanged on exit)
  INTEGER :: ncv, ns
  REAL(kind=r8) :: na(ncv, 0:ns-1), ua(ncv, 0:ns-1), smq(ncv, 0:3, 0:ns-&
& 1)
!   ..output arguments (unspecified on entry)
  REAL(kind=r8) :: smqdu(ncv)
!   ..workspace arguments (unspecified on entry and on exit)
  REAL(kind=r8) :: wrk0(ncv), wrk1(ncv)
!   ..common blocks
!-----------------------------------------------------------------------
!.documentation
!
!  1. purpose
!
!     B2NXDU computes the artificial velocity shift that is
!     required to make the total numerical friction force cancel.
!
!-----------------------------------------------------------------------
!.declarations
!
!   ..local variables
  INTEGER :: icv, is
!   ..procedures
  EXTERNAL XERTST, SFILL_NODIFF
  EXTERNAL B2XVSG
!   ..initialisation
!
!-----------------------------------------------------------------------
!.computation
!
! ..preliminaries
!   ..subprogram start-up calls
  CALL SUBINI('b2nxdu')
!   ..test nCv
  CALL XERTST(0 .LT. ncv, 'faulty argument nCv')
!   ..extensive tests on first few calls
  IF (ncall_b2nxdu .LT. 3) THEN
!    ..test sign of smq
    DO is=0,ns-1
      CALL B2XVSG(ncv, smq(1, 1, is), 1, 'smq1', '.le.')
      CALL B2XVSG(ncv, smq(1, 3, is), 1, 'smq3', '.le.')
    END DO
  END IF
!
! ..compute smqdu
!   ..compute auxiliaries wrk0 and wrk1
  CALL SFILL_NODIFF(ncv, 0.0_R8, wrk0, 1)
  CALL SFILL_NODIFF(ncv, 0.0_R8, wrk1, 1)
  DO is=0,ns-1
    wrk0 = wrk0 + smq(:, 1, is) + smq(:, 3, is)*am(is)*mp*na(:, is)
    wrk1 = wrk1 + smq(:, 0, is) + smq(:, 1, is)*ua(:, is) + smq(:, 2, is&
&     )*am(is)*mp*na(:, is) + smq(:, 3, is)*am(is)*mp*na(:, is)*ua(:, is&
&     )
  END DO
!   ..compute smqdu
  DO icv=1,ncv
    IF (wrk0(icv) .EQ. 0.0_R8) THEN
      smqdu(icv) = 0.0_R8
    ELSE
      smqdu(icv) = wrk1(icv)/wrk0(icv)
    END IF
  END DO
!
! ..return
  ncall_b2nxdu = ncall_b2nxdu + 1
  CALL SUBEND()
  RETURN
!
!-----------------------------------------------------------------------
!.end b2nxdu
!
END SUBROUTINE B2NXDU_NODIFF

