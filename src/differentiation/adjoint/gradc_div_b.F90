!        Generated by TAPENADE     (INRIA, Ecuador team)
!  Tapenade 3.16 (develop) - 13 Feb 2024 17:09
!
!  Differentiation of gradc_div in reverse (adjoint) mode (with options context noISIZE r8):
!   gradient     of useful results: funf gfun fun
!   with respect to varying inputs: funf fun
!   Plus diff mem management of: geo.cvvol:in geo.fcs:in geo.fcvol:in
!                geo.fcqalf:in
!
!
!
!
!
!
!
!
!
!
!
!-----------------------------------------------------------------------
!.specification
!
SUBROUTINE GRADC_DIV_B(ncv, nfc, nvx, mode, geo, geob, mpg, mpgb, fun, &
& funb, funf, funfb, gfun, gfunb)
  USE B2MOD_TYPES
  USE B2US_GEO_DIFF
  USE B2US_MAP_DIFF
  IMPLICIT NONE
!   ..input arguments
  INTEGER :: ncv, nfc, nvx, mode
  TYPE(GEOMETRY), INTENT(IN) :: geo
  TYPE(GEOMETRY_DIFF) :: geob
  TYPE(MAPPING), INTENT(IN) :: mpg
  TYPE(MAPPING_DIFF) :: mpgb
  REAL(kind=r8) :: fun(ncv), funf(nfc)
  REAL(kind=r8) :: funb(ncv), funfb(nfc)
!   ..output arguments
  REAL(kind=r8) :: gfun(ncv, 0:1)
  REAL(kind=r8) :: gfunb(ncv, 0:1)
!-----------------------------------------------------------------------
!.documentation
!
!     GRADC_DIV computes gradients of a cell centered quantity fun in
!     cell centers.  Computation is based on the divergence theorem.
!     dfun(:,:,0) will hold the gradient in the poloidal direction
!     dfun(:,:,1) will hold the gradient in the radial direction
!
!     The routine is a simple interface to GRADC_DIV_P and GRADC_DIV_R, 
!     that compute the actual poloidal and radial gradients.
!
!-----------------------------------------------------------------------
!.declarations
!
!   ..local variables
!
!   ..procedures
  EXTERNAL GRADC_DIV_P_NODIFF, GRADC_DIV_R_NODIFF
  EXTERNAL GRADC_DIV_P_B, GRADC_DIV_R_B
  INTEGER*4 :: branch
!
!-----------------------------------------------------------------------
!.computation      
!
!   ..test mode
!
!   ..interpolate fun to cell faces, using volume weighted averaging
  IF (mode .EQ. 0) THEN
    CALL INTFACE_FWD(ncv, nfc, mpg%fccv, geo%fcvol, fun, funf)
    CALL PUSHCONTROL1B(0)
  ELSE
    CALL PUSHCONTROL1B(1)
  END IF
  CALL GRADC_DIV_R_B(ncv, nfc, nvx, 1, geo, mpg, mpgb, fun, funb, funf, &
&              funfb, gfun(:, 1), gfunb(:, 1))
  gfunb(:, 1) = 0.D0
  CALL GRADC_DIV_P_B(ncv, nfc, nvx, 1, geo, mpg, mpgb, fun, funb, funf, &
&              funfb, gfun(:, 0), gfunb(:, 0))
  gfunb(:, 0) = 0.D0
  CALL POPCONTROL1B(branch)
  IF (branch .EQ. 0) CALL INTFACE_BWD(ncv, nfc, mpg%fccv, geo%fcvol, fun&
&                               , funb, funf, funfb)
END SUBROUTINE GRADC_DIV_B

!
!
!
!
!
!
!
!
!
!
!
!-----------------------------------------------------------------------
!.specification
!
SUBROUTINE GRADC_DIV_NODIFF(ncv, nfc, nvx, mode, geo, mpg, fun, funf, &
& gfun)
  USE B2MOD_TYPES
  USE B2US_GEO_DIFF
  USE B2US_MAP_DIFF
  IMPLICIT NONE
!   ..input arguments
  INTEGER :: ncv, nfc, nvx, mode
  TYPE(GEOMETRY), INTENT(IN) :: geo
  TYPE(MAPPING), INTENT(IN) :: mpg
  REAL(kind=r8) :: fun(ncv), funf(nfc)
!   ..output arguments
  REAL(kind=r8) :: gfun(ncv, 0:1)
!-----------------------------------------------------------------------
!.documentation
!
!     GRADC_DIV computes gradients of a cell centered quantity fun in
!     cell centers.  Computation is based on the divergence theorem.
!     dfun(:,:,0) will hold the gradient in the poloidal direction
!     dfun(:,:,1) will hold the gradient in the radial direction
!
!     The routine is a simple interface to GRADC_DIV_P and GRADC_DIV_R, 
!     that compute the actual poloidal and radial gradients.
!
!-----------------------------------------------------------------------
!.declarations
!
!   ..local variables
!
!   ..procedures
  EXTERNAL GRADC_DIV_P_NODIFF, GRADC_DIV_R_NODIFF
!
!-----------------------------------------------------------------------
!.computation      
!
!   ..test mode
  CALL XERTST(0 .LE. mode .AND. mode .LE. 1, &
&       'gradc_div--faulty argument mode')
!
!   ..interpolate fun to cell faces, using volume weighted averaging
  IF (mode .EQ. 0) CALL INTFACE(ncv, nfc, mpg%fccv, geo%fcvol, fun, funf&
&                        )
!
!   ..compute poloidal and radial gradients
  CALL GRADC_DIV_P_NODIFF(ncv, nfc, nvx, 1, geo, mpg, fun, funf, gfun(:&
&                   , 0))
  CALL GRADC_DIV_R_NODIFF(ncv, nfc, nvx, 1, geo, mpg, fun, funf, gfun(:&
&                   , 1))
!
!   ..return
  RETURN
!
!-----------------------------------------------------------------------
!.end gradc_div
!
END SUBROUTINE GRADC_DIV_NODIFF

