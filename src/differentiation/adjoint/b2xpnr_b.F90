!        Generated by TAPENADE     (INRIA, Ecuador team)
!  Tapenade 3.16 (develop) - 13 Feb 2024 17:09
!
!  Differentiation of b2xpnr in reverse (adjoint) mode (with options context noISIZE r8):
!   gradient     of useful results: na nirm
!   with respect to varying inputs: na
!
!
!
!
!
!
!
!
!
!
!
SUBROUTINE B2XPNR_B(ncv, ns, na, nab, nirm, nirmb)
  USE B2MOD_TYPES
  USE B2MOD_B2CMPA_DIFF
  IMPLICIT NONE
  INTEGER :: ncv, ns
  REAL(kind=r8) :: na(ncv, 0:ns-1), nirm(ncv)
  REAL(kind=r8) :: nab(ncv, 0:ns-1), nirmb(ncv)
!     ------------------------------------------------------------------
!     B2XPNR computes the atom density weighted by 1/sqrt(am), nirm:
!       nirm() = (sum is :: na(,is)/sqrt(am(is))) .
!     ------------------------------------------------------------------
  INTEGER :: is
  INTRINSIC SQRT
!     ------------------------------------------------------------------
!$$$  call subini ('b2xpnr')
  INTEGER*4 :: branch
  DO is=0,ns-1
    IF (.NOT.is_neutral(is)) THEN
      CALL PUSHCONTROL1B(1)
    ELSE
      CALL PUSHCONTROL1B(0)
    END IF
  END DO
  DO is=ns-1,0,-1
    CALL POPCONTROL1B(branch)
    IF (branch .NE. 0) nab(:, is) = nab(:, is) + nirmb/SQRT(am(is))
  END DO
END SUBROUTINE B2XPNR_B

!
!
!
!
!
!
!
!
!
!
!
SUBROUTINE B2XPNR_NODIFF(ncv, ns, na, nirm)
  USE B2MOD_TYPES
  USE B2MOD_B2CMPA_DIFF
  IMPLICIT NONE
  INTEGER :: ncv, ns
  REAL(kind=r8) :: na(ncv, 0:ns-1), nirm(ncv)
!     ------------------------------------------------------------------
!     B2XPNR computes the atom density weighted by 1/sqrt(am), nirm:
!       nirm() = (sum is :: na(,is)/sqrt(am(is))) .
!     ------------------------------------------------------------------
  INTEGER :: is
  INTRINSIC SQRT
!     ------------------------------------------------------------------
!$$$  call subini ('b2xpnr')
  nirm = 0.0_R8
  DO is=0,ns-1
    IF (.NOT.is_neutral(is)) nirm = nirm + na(:, is)/SQRT(am(is))
  END DO
!$$$  call subend ()
  RETURN
!     ------------------------------------------------------------------
END SUBROUTINE B2XPNR_NODIFF

