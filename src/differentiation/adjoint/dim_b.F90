!        Generated by TAPENADE     (INRIA, Ecuador team)
!  Tapenade 3.16 (develop) - 23 Jul 2024 17:41
!
!  Differentiation of dim in reverse (adjoint) mode, forward sweep (with options context noISIZE r8):
!   gradient     of useful results: dim
!   with respect to varying inputs: x y
FUNCTION DIM_FWD(x, y) RESULT (dim)
  USE B2MOD_TYPES
  IMPLICIT NONE
  REAL(kind=r8) :: dim, x, y, dum
  INTRINSIC MAX
  IF (x - y .LT. 0.0_R8) THEN
    dim = 0.0_R8
    CALL PUSHCONTROL1B(0)
  ELSE
    dim = x - y
    CALL PUSHCONTROL1B(1)
  END IF
END FUNCTION DIM_FWD

!  Differentiation of dim in reverse (adjoint) mode, backward sweep (with options context noISIZE r8):
!   gradient     of useful results: dim
!   with respect to varying inputs: x y
SUBROUTINE DIM_BWD(x, xb, y, yb, dimb)
  USE B2MOD_TYPES
  IMPLICIT NONE
  REAL(kind=r8) :: dim, x, y, dum
  REAL(kind=r8) :: dimb, xb, yb
  INTRINSIC MAX
  INTEGER*4 :: branch
  CALL POPCONTROL1B(branch)
  IF (branch .EQ. 0) THEN
    xb = 0.D0
    yb = 0.D0
  ELSE
    xb = dimb
    yb = -dimb
  END IF
END SUBROUTINE DIM_BWD

