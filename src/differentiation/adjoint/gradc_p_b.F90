!        Generated by TAPENADE     (INRIA, Ecuador team)
!  Tapenade 3.16 (develop) - 23 Jul 2024 17:41
!
!  Differentiation of gradc_p in reverse (adjoint) mode, forward sweep (with options context noISIZE r8):
!   gradient     of useful results: gfunp funv fun
!   with respect to varying inputs: funv fun
!   Plus diff mem management of: mpg.intcellp:in geo.fchc:in geo.fcht:in
!                geo.fcqgam:in geo.fcqalf:in geo.fcqbet:in geo.vxvol:in
!
!
!
!
!
!
!
!
!
!
!
!-----------------------------------------------------------------------
!.specification
SUBROUTINE GRADC_P_FWD(ncv, nfc, nvx, mode, geo, geob, mpg, mpgb, fun, &
& funv, gfunp)
  USE B2MOD_TYPES
  USE B2US_GEO_DIFF
  USE B2US_MAP_DIFF
  IMPLICIT NONE
!   ..input arguments
  INTEGER, INTENT(IN) :: ncv, nfc, nvx, mode
  TYPE(GEOMETRY), INTENT(IN) :: geo
  TYPE(MAPPING), INTENT(IN) :: mpg
  REAL(kind=r8) :: fun(ncv), funv(nvx)
!   ..output arguments
  REAL(kind=r8) :: gfunp(ncv)
!-----------------------------------------------------------------------
!.documentation
!
!     GRADC_P computes gradients of a cell centered quantity fun in the 
!     poloidal direction, in cell centers. The routine first computes
!     the gradients on faces, and then interpolates to cell centers.
!
!-----------------------------------------------------------------------
!.declarations
!
!   ..local variables
  REAL(kind=r8) :: gfunpf(nfc)
  TYPE(MAPPING_DIFF) :: mpgb
  TYPE(GEOMETRY_DIFF) :: geob
!
!-----------------------------------------------------------------------
!.computation      
!
!   ..test mode
!
!   ..interpolate fun to cell vertices, using volume weighted averaging
  IF (mode .EQ. 0) THEN
    CALL INTVERTEX_FWD(ncv, nvx, mpg, geo%vxvol, fun, funv)
    CALL PUSHCONTROL1B(0)
  ELSE
    CALL PUSHCONTROL1B(1)
  END IF
!
!   ..compute gradients on faces
  CALL GRAD_P_FWD(ncv, nfc, nvx, 1, geo, mpg, mpgb, fun, funv, gfunpf)
!
!   ..interpolate to centers
  CALL INTCELL_FWD(nfc, ncv, mpg, mpg%intcellp, gfunpf, gfunp)
!
!   ..return
!
!-----------------------------------------------------------------------
!.end gradc_p
!
  CALL PUSHREAL8ARRAY(gfunpf, r8*nfc/8)
END SUBROUTINE GRADC_P_FWD

!  Differentiation of gradc_p in reverse (adjoint) mode, backward sweep (with options context noISIZE r8):
!   gradient     of useful results: gfunp funv fun
!   with respect to varying inputs: funv fun
!   Plus diff mem management of: mpg.intcellp:in geo.fchc:in geo.fcht:in
!                geo.fcqgam:in geo.fcqalf:in geo.fcqbet:in geo.vxvol:in
!
!
!
!
!
!
!
!
!
!
!
!-----------------------------------------------------------------------
!.specification
SUBROUTINE GRADC_P_BWD(ncv, nfc, nvx, mode, geo, geob, mpg, mpgb, fun, &
& funb, funv, funvb, gfunp, gfunpb)
  USE B2MOD_TYPES
  USE B2US_GEO_DIFF
  USE B2US_MAP_DIFF
  IMPLICIT NONE
  INTEGER, INTENT(IN) :: ncv, nfc, nvx, mode
  TYPE(GEOMETRY), INTENT(IN) :: geo
  TYPE(GEOMETRY_DIFF) :: geob
  TYPE(MAPPING), INTENT(IN) :: mpg
  TYPE(MAPPING_DIFF) :: mpgb
  REAL(kind=r8) :: fun(ncv), funv(nvx)
  REAL(kind=r8) :: funb(ncv), funvb(nvx)
  REAL(kind=r8) :: gfunp(ncv)
  REAL(kind=r8) :: gfunpb(ncv)
  REAL(kind=r8) :: gfunpf(nfc)
  REAL(kind=r8) :: gfunpfb(nfc)
  INTEGER*4 :: branch
  CALL POPREAL8ARRAY(gfunpf, r8*nfc/8)
  gfunpfb = 0.D0
  CALL INTCELL_BWD(nfc, ncv, mpg, mpg%intcellp, gfunpf, gfunpfb, gfunp, &
&            gfunpb)
  CALL GRAD_P_BWD(ncv, nfc, nvx, 1, geo, mpg, mpgb, fun, funb, funv, &
&           funvb, gfunpf, gfunpfb)
  CALL POPCONTROL1B(branch)
  IF (branch .EQ. 0) THEN
    CALL INTVERTEX_BWD(ncv, nvx, mpg, geo%vxvol, fun, funb, funv, funvb)
    funvb = 0.D0
  END IF
END SUBROUTINE GRADC_P_BWD

!
!
!
!
!
!
!
!
!
!
!
!-----------------------------------------------------------------------
!.specification
SUBROUTINE GRADC_P_NODIFF(ncv, nfc, nvx, mode, geo, mpg, fun, funv, &
& gfunp)
  USE B2MOD_TYPES
  USE B2US_GEO_DIFF
  USE B2US_MAP_DIFF
  IMPLICIT NONE
!   ..input arguments
  INTEGER, INTENT(IN) :: ncv, nfc, nvx, mode
  TYPE(GEOMETRY), INTENT(IN) :: geo
  TYPE(MAPPING), INTENT(IN) :: mpg
  REAL(kind=r8) :: fun(ncv), funv(nvx)
!   ..output arguments
  REAL(kind=r8) :: gfunp(ncv)
!-----------------------------------------------------------------------
!.documentation
!
!     GRADC_P computes gradients of a cell centered quantity fun in the 
!     poloidal direction, in cell centers. The routine first computes
!     the gradients on faces, and then interpolates to cell centers.
!
!-----------------------------------------------------------------------
!.declarations
!
!   ..local variables
  REAL(kind=r8) :: gfunpf(nfc)
!
!-----------------------------------------------------------------------
!.computation      
!
!   ..test mode
  CALL XERTST(0 .LE. mode .AND. mode .LE. 1, &
&       'gradc_p--faulty argument mode')
!
!   ..interpolate fun to cell vertices, using volume weighted averaging
  IF (mode .EQ. 0) CALL INTVERTEX_NODIFF(ncv, nvx, mpg, geo%vxvol, fun, &
&                                  funv)
!
!   ..compute gradients on faces
  CALL GRAD_P_NODIFF(ncv, nfc, nvx, 1, geo, mpg, fun, funv, gfunpf)
!
!   ..interpolate to centers
  CALL INTCELL_NODIFF(nfc, ncv, mpg, mpg%intcellp, gfunpf, gfunp)
!
!   ..return
  RETURN
!
!-----------------------------------------------------------------------
!.end gradc_p
!
END SUBROUTINE GRADC_P_NODIFF

