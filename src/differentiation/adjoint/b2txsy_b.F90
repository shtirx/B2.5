!        Generated by TAPENADE     (INRIA, Ecuador team)
!  Tapenade 3.16 (develop) - 13 Feb 2024 17:09
!
!  Differentiation of b2txsy in reverse (adjoint) mode (with options context noISIZE r8):
!   gradient     of useful results: fun funsy
!   with respect to varying inputs: fun funsy
!
!
!
!
!
!
!
!
!
!
!
SUBROUTINE B2TXSY_B(ncv, nfc, geo, mpg, fcvol, fcs, fun, funb, funsy, &
& funsyb)
  USE B2MOD_TYPES
  USE B2US_GEO_DIFF
  USE B2US_MAP_DIFF
  USE B2MOD_SUBSYS
  IMPLICIT NONE
  INTEGER :: ncv, nfc
  TYPE(GEOMETRY), INTENT(IN) :: geo
  TYPE(MAPPING), INTENT(IN) :: mpg
  REAL(kind=r8) :: fcvol(nfc, 2), fcs(nfc), fun(ncv), funsy(nfc)
  REAL(kind=r8) :: funb(ncv), funsyb(nfc)
!     ------------------------------------------------------------------
!     B2TXSY computes the integrals over cell y-faces of a function
!     that is provided on the cells.
!!!   (Reconsider specification and use of this routine for the case
!     of a non-orthogonal grid.)
!     ------------------------------------------------------------------
  INTEGER :: ifc
!     ------------------------------------------------------------------
  REAL(kind=r8) :: tempb
  DO ifc=nfc,1,-1
    tempb = fcs(ifc)*geo%fcqalf(ifc, 1)*funsyb(ifc)/(fcvol(ifc, 1)+fcvol&
&     (ifc, 2))
    funsyb(ifc) = 0.D0
    funb(mpg%fccv(ifc, 1)) = funb(mpg%fccv(ifc, 1)) + fcvol(ifc, 2)*&
&     tempb
    funb(mpg%fccv(ifc, 2)) = funb(mpg%fccv(ifc, 2)) + fcvol(ifc, 1)*&
&     tempb
  END DO
END SUBROUTINE B2TXSY_B

!
!
!
!
!
!
!
!
!
!
!
SUBROUTINE B2TXSY_NODIFF(ncv, nfc, geo, mpg, fcvol, fcs, fun, funsy)
  USE B2MOD_TYPES
  USE B2US_GEO_DIFF
  USE B2US_MAP_DIFF
  USE B2MOD_SUBSYS
  IMPLICIT NONE
  INTEGER :: ncv, nfc
  TYPE(GEOMETRY), INTENT(IN) :: geo
  TYPE(MAPPING), INTENT(IN) :: mpg
  REAL(kind=r8) :: fcvol(nfc, 2), fcs(nfc), fun(ncv), funsy(nfc)
!     ------------------------------------------------------------------
!     B2TXSY computes the integrals over cell y-faces of a function
!     that is provided on the cells.
!!!   (Reconsider specification and use of this routine for the case
!     of a non-orthogonal grid.)
!     ------------------------------------------------------------------
  INTEGER :: ifc
!     ------------------------------------------------------------------
  CALL SUBINI('b2txsy')
!   ..compute funsy
  DO ifc=1,nfc
    funsy(ifc) = fcs(ifc)*geo%fcqalf(ifc, 1)*(fcvol(ifc, 2)*fun(mpg%fccv&
&     (ifc, 1))+fcvol(ifc, 1)*fun(mpg%fccv(ifc, 2)))/(fcvol(ifc, 1)+&
&     fcvol(ifc, 2))
  END DO
!   ..return
  CALL SUBEND()
  RETURN
!     ------------------------------------------------------------------
END SUBROUTINE B2TXSY_NODIFF

