!        Generated by TAPENADE     (INRIA, Ecuador team)
!  Tapenade 3.16 (develop) - 13 Feb 2024 17:09
!
!  Differentiation of b2treq in reverse (adjoint) mode (with options context noISIZE r8):
!   gradient     of useful results: ti ne ni ne2m lnlam ceqp te
!   with respect to varying inputs: ti ne ni ne2m lnlam ceqp te
!
!
!
!
!
!
!
!
!
!
!
!-----------------------------------------------------------------------
!.specification
!
!xpb !srv 01.07.09
SUBROUTINE B2TREQ_B(ncv, switch, switchb, vol, volb, te, teb, ti, tib, &
& ne, neb, ni, nib, ne2m, ne2mb, lnlam, lnlamb, st_ext, ceqp, ceqpb)
  USE B2MOD_TYPES
  USE B2MOD_CONSTANTS
  USE B2MOD_B2CMPA_DIFF
  USE B2US_PLASMA_DIFF
  USE B2MOD_SWITCHES_DIFF
  USE B2MOD_AD_DIFF, ONLY : ncall_b2treq
! csc The following are not necessary for computation but are needed
!     for adjoint AD to avoid side-effect variables
  USE B2MOD_AD_DIFF, ONLY : ncall_b2tlnl
  USE B2MOD_SUBSYS
  IMPLICIT NONE
!   ..input arguments (unchanged on exit)
  INTEGER :: ncv
  TYPE(SWITCHES), INTENT(IN) :: switch
  TYPE(SWITCHES) :: switchb
  TYPE(B2STATEEXT), INTENT(IN) :: st_ext
!xpb !srv 01.07.09
  REAL(kind=r8) :: vol(ncv), ni(ncv, 0:1), te(ncv), ti(ncv), ne(ncv), &
& ne2m(ncv), lnlam(ncv)
  REAL(kind=r8) :: volb(ncv), nib(ncv, 0:1), teb(ncv), tib(ncv), neb(ncv&
& ), ne2mb(ncv), lnlamb(ncv)
!   ..output arguments (unspecified on entry)
  REAL(kind=r8) :: ceqp(ncv)
  REAL(kind=r8) :: ceqpb(ncv)
!   ..common blocks
!-----------------------------------------------------------------------
!.documentation
!
!  1. purpose
!
!     B2TREQ computes the cell-integrated energy equipartition
!     multiplier.
!
!
!  3. description (see also routine b2cdca)
!
!     This routine computes the electron-atom energy equipartition
!     multiplier ceqp, defined so that on each cell the signed
!     equipartition energy transfer from atoms to electrons is
!     (ceqp*ne*ni/te**1.5)*(te-ti) .
!
!
!-----------------------------------------------------------------------
!.declarations
!
!   ..local variables
  INTEGER :: icv
  REAL(kind=r8) :: cfeqp
  PARAMETER (cfeqp=4.8e-15_R8)
!     lnlam is the log of the plasma parameter.
!     cfeqp is a numerical coefficient in the energy transfer rate.
!   ..procedures
  INTRINSIC SQRT
  EXTERNAL XERTST
  EXTERNAL B2XVSG
  INTEGER :: arg1
!   ..initialisation
!
!-----------------------------------------------------------------------
!.computation
!
!   ..subprogram start-up calls
!   ..test nCv
!   ..extensive tests on first few calls
  REAL(r8) :: temp
  REAL(kind=r8) :: tempb
!
!srv 20.09.11
  CALL B2TLNL_NODIFF(ncv, switch, switch%icase_ei, te, ti, ne, lnlam)
  DO icv=ncv,1,-1
    temp = st_ext%ni(icv, 0) + ni(icv, 0)
    tempb = SQRT(ev)*switch%b2treq_phm0*cfeqp*ev*vol(icv)*ceqpb(icv)/&
&     temp
    ceqpb(icv) = 0.D0
    lnlamb(icv) = lnlamb(icv) + ne2m(icv)*tempb
    ne2mb(icv) = ne2mb(icv) + lnlam(icv)*tempb
    nib(icv, 0) = nib(icv, 0) - lnlam(icv)*ne2m(icv)*tempb/temp
  END DO
  CALL B2TLNL_B(ncv, switch, switchb, switch%icase_ei, te, teb, ti, tib&
&         , ne, neb, lnlam, lnlamb)
END SUBROUTINE B2TREQ_B
!

!
!
!
!
!
!
!
!
!
!
!
!-----------------------------------------------------------------------
!.specification
!
!xpb !srv 01.07.09
SUBROUTINE B2TREQ_NODIFF(ncv, switch, vol, te, ti, ne, ni, ne2m, lnlam, &
& st_ext, ceqp)
  USE B2MOD_TYPES
  USE B2MOD_CONSTANTS
  USE B2MOD_B2CMPA_DIFF
  USE B2US_PLASMA_DIFF
  USE B2MOD_SWITCHES_DIFF
  USE B2MOD_AD_DIFF, ONLY : ncall_b2treq
! csc The following are not necessary for computation but are needed
!     for adjoint AD to avoid side-effect variables
  USE B2MOD_AD_DIFF, ONLY : ncall_b2tlnl
  USE B2MOD_SUBSYS
  IMPLICIT NONE
!   ..input arguments (unchanged on exit)
  INTEGER :: ncv
  TYPE(SWITCHES), INTENT(IN) :: switch
  TYPE(B2STATEEXT), INTENT(IN) :: st_ext
!xpb !srv 01.07.09
  REAL(kind=r8) :: vol(ncv), ni(ncv, 0:1), te(ncv), ti(ncv), ne(ncv), &
& ne2m(ncv), lnlam(ncv)
!   ..output arguments (unspecified on entry)
  REAL(kind=r8) :: ceqp(ncv)
!   ..common blocks
!-----------------------------------------------------------------------
!.documentation
!
!  1. purpose
!
!     B2TREQ computes the cell-integrated energy equipartition
!     multiplier.
!
!
!  3. description (see also routine b2cdca)
!
!     This routine computes the electron-atom energy equipartition
!     multiplier ceqp, defined so that on each cell the signed
!     equipartition energy transfer from atoms to electrons is
!     (ceqp*ne*ni/te**1.5)*(te-ti) .
!
!
!-----------------------------------------------------------------------
!.declarations
!
!   ..local variables
  INTEGER :: icv
  REAL(kind=r8) :: cfeqp
  PARAMETER (cfeqp=4.8e-15_R8)
!     lnlam is the log of the plasma parameter.
!     cfeqp is a numerical coefficient in the energy transfer rate.
!   ..procedures
  INTRINSIC SQRT
  EXTERNAL XERTST
  EXTERNAL B2XVSG
  INTEGER :: arg1
!   ..initialisation
!
!-----------------------------------------------------------------------
!.computation
!
!   ..subprogram start-up calls
  CALL SUBINI('b2treq')
!   ..test nCv
  CALL XERTST(0 .LT. ncv, 'faulty argument nCv')
!   ..extensive tests on first few calls
  IF (ncall_b2treq .LT. 3) THEN
!    ..test vol, state
    CALL B2XVSG(ncv, vol, 1, 'vol', '.gt.')
    CALL B2XVSG(ncv, te, 1, 'te', '.gt.')
    CALL B2XVSG(ncv, ne, 1, 'ne', '.gt.')
    arg1 = ncv*2
    CALL B2XVSG(arg1, ni, 1, 'ni', '.gt.')
    CALL B2XVSG(ncv, ne2m, 1, 'ne2m', '.gt.')
  END IF
!
!srv 20.09.11
  CALL B2TLNL_NODIFF(ncv, switch, switch%icase_ei, te, ti, ne, lnlam)
!   ..compute ceqp
  DO icv=1,ncv
!srv 01.07.09
    ceqp(icv) = switch%b2treq_phm0*(cfeqp*lnlam(icv)*ev*SQRT(ev))*vol(&
&     icv)*(ne2m(icv)/(ni(icv, 0)+st_ext%ni(icv, 0)))
  END DO
!
! ..return
  ncall_b2treq = ncall_b2treq + 1
  CALL SUBEND()
  RETURN
!
!-----------------------------------------------------------------------
!.end b2treq
!
END SUBROUTINE B2TREQ_NODIFF
!

