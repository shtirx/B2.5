!        Generated by TAPENADE     (INRIA, Ecuador team)
!  Tapenade 3.16 (develop) - 13 Feb 2024 17:09
!
!  Differentiation of b2xzdd in reverse (adjoint) mode, forward sweep (with options context noISIZE r8):
!   gradient     of useful results: src
!   with respect to varying inputs: src
!
!
!
!
!
!
!
!
!
!
!
SUBROUTINE B2XZDD_FWD(ncv, nd, switch, mpg, src, srcb)
  USE B2MOD_SWITCHES_DIFF
  USE B2MOD_TYPES
  USE B2US_MAP_DIFF
  IMPLICIT NONE
  INTEGER :: ncv, nd
  TYPE(SWITCHES), INTENT(IN) :: switch
  TYPE(MAPPING), INTENT(IN) :: mpg
  REAL(kind=r8) :: src(ncv, 0:nd)
!     ------------------------------------------------------------------
!      B2XZDD zeroes out the array elements corresponding to dead cells
!      and core boundary cells.
!     ------------------------------------------------------------------
  INTEGER :: id, icv
  REAL(kind=r8) :: srcb(ncv, 0:nd)
!     ------------------------------------------------------------------
  DO icv=1,ncv
    IF (mpg%cvreg(icv) .EQ. 0 .AND. switch%zero_dead_and_core .GE. 1) &
&   THEN
      CALL PUSHCONTROL2B(2)
    ELSE IF (icv .GT. mpg%nci .AND. mpg%cvonclosedsurface(icv) .AND. &
&       switch%zero_dead_and_core .GE. 2) THEN
      CALL PUSHCONTROL2B(1)
    ELSE
      CALL PUSHCONTROL2B(0)
    END IF
  END DO
END SUBROUTINE B2XZDD_FWD

!  Differentiation of b2xzdd in reverse (adjoint) mode, backward sweep (with options context noISIZE r8):
!   gradient     of useful results: src
!   with respect to varying inputs: src
!
!
!
!
!
!
!
!
!
!
!
SUBROUTINE B2XZDD_BWD(ncv, nd, switch, mpg, src, srcb)
  USE B2MOD_SWITCHES_DIFF
  USE B2MOD_TYPES
  USE B2US_MAP_DIFF
  IMPLICIT NONE
  INTEGER :: ncv, nd
  TYPE(SWITCHES), INTENT(IN) :: switch
  TYPE(MAPPING), INTENT(IN) :: mpg
  REAL(kind=r8) :: src(ncv, 0:nd)
  REAL(kind=r8) :: srcb(ncv, 0:nd)
  INTEGER :: id, icv
  INTEGER*4 :: branch
  DO icv=ncv,1,-1
    CALL POPCONTROL2B(branch)
    IF (branch .NE. 0) THEN
      IF (branch .EQ. 1) THEN
        DO id=nd,0,-1
          srcb(icv, id) = 0.D0
        END DO
      ELSE
        DO id=nd,0,-1
          srcb(icv, id) = 0.D0
        END DO
      END IF
    END IF
  END DO
END SUBROUTINE B2XZDD_BWD

!
!
!
!
!
!
!
!
!
!
!
SUBROUTINE B2XZDD_NODIFF(ncv, nd, switch, mpg, src)
  USE B2MOD_SWITCHES_DIFF
  USE B2MOD_TYPES
  USE B2US_MAP_DIFF
  IMPLICIT NONE
  INTEGER :: ncv, nd
  TYPE(SWITCHES), INTENT(IN) :: switch
  TYPE(MAPPING), INTENT(IN) :: mpg
  REAL(kind=r8) :: src(ncv, 0:nd)
!     ------------------------------------------------------------------
!      B2XZDD zeroes out the array elements corresponding to dead cells
!      and core boundary cells.
!     ------------------------------------------------------------------
  INTEGER :: id, icv
!     ------------------------------------------------------------------
  DO icv=1,ncv
    IF (mpg%cvreg(icv) .EQ. 0 .AND. switch%zero_dead_and_core .GE. 1) &
&   THEN
      DO id=0,nd
        src(icv, id) = 0.0_R8
      END DO
    ELSE IF (icv .GT. mpg%nci .AND. mpg%cvonclosedsurface(icv) .AND. &
&       switch%zero_dead_and_core .GE. 2) THEN
      DO id=0,nd
        src(icv, id) = 0.0_R8
      END DO
    END IF
  END DO
  RETURN
!     ------------------------------------------------------------------
END SUBROUTINE B2XZDD_NODIFF

