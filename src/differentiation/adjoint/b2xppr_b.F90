!        Generated by TAPENADE     (INRIA, Ecuador team)
!  Tapenade 3.16 (develop) - 13 Feb 2024 17:09
!
!
!
!
!
!
!
!
!
!
!
!
SUBROUTINE B2XPPR_NODIFF(ncv, ns, ne, na, te, ti, tn, pr, st_ext)
  USE B2MOD_TYPES
  USE B2US_PLASMA_DIFF
  USE B2MOD_B2CMPA_DIFF
  IMPLICIT NONE
  INTEGER :: ncv, ns
  TYPE(B2STATEEXT), INTENT(IN) :: st_ext
  REAL(kind=r8) :: ne(ncv), na(ncv, 0:ns-1), te(ncv), ti(ncv), tn(ncv), &
& pr(ncv)
!     ------------------------------------------------------------------
!     B2XPPR computes the total pressure, pr:
!       pr() = ne()*te() + (sum is :: na(,is)*ti()) .
!     ------------------------------------------------------------------
  INTEGER :: is
!     ------------------------------------------------------------------
!$$$  call subini ('b2xppr')
  pr = ne*te
  DO is=0,ns-1
    IF (.NOT.is_neutral(is)) THEN
      pr = pr + na(:, is)*ti
    ELSE
      pr = pr + na(:, is)*tn
    END IF
  END DO
  DO is=0,st_ext%ns-1
    pr = pr + st_ext%na(:, is)*st_ext%ta(:, is)
  END DO
!$$$  call subend ()
  RETURN
!     ------------------------------------------------------------------
END SUBROUTINE B2XPPR_NODIFF

