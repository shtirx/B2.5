!        Generated by TAPENADE     (INRIA, Ecuador team)
!  Tapenade 3.16 (develop) - 13 Feb 2024 17:09
!
!  Differentiation of sfill in reverse (adjoint) mode, forward sweep (with options context noISIZE r8):
!   gradient     of useful results: sx
!   with respect to varying inputs: sx
!
!
!
!
!
!
!
!
!
!
!
SUBROUTINE SFILL_FWD(n, sa, sx, sxb, incx)
  USE B2MOD_TYPES
  USE B2MOD_MATH_DIFF
  IMPLICIT NONE
  INTEGER :: n, incx
  REAL(kind=r8) :: sa, sx(0:n*incx-1)
!     ------------------------------------------------------------------
!     SFILL (BLAS-like routine) fills a vector with a constant value.
!     sfill(n,sa,sx,incx) sets all elements of the s.p. real*8 vector x
!     to sa. x is described by (n,sx,incx). BLAS storage conventions.
!     Return without effects if n.lt.1.
!     Undefined if incx.eq.0.
!     ------------------------------------------------------------------
  INTEGER :: i, ix
  EXTERNAL XERRAB
  REAL(kind=r8) :: sxb(0:n*incx-1)
!     ------------------------------------------------------------------
  IF (incx .EQ. 0) THEN
    CALL PUSHCONTROL2B(0)
  ELSE IF (incx .EQ. 1) THEN
    DO i=0,n-1
      CALL PUSHREAL8(sx(i), r8/8)
      sx(i) = sa
    END DO
    CALL PUSHCONTROL2B(1)
  ELSE
    ix = IBASE(n, incx)
    DO i=0,n-1
      CALL PUSHREAL8(sx(ix+i*incx), r8/8)
      sx(ix+i*incx) = sa
    END DO
    CALL PUSHINTEGER4(ix)
    CALL PUSHCONTROL2B(2)
  END IF
END SUBROUTINE SFILL_FWD

!  Differentiation of sfill in reverse (adjoint) mode, backward sweep (with options context noISIZE r8):
!   gradient     of useful results: sx
!   with respect to varying inputs: sx
!
!
!
!
!
!
!
!
!
!
!
SUBROUTINE SFILL_BWD(n, sa, sab, sx, sxb, incx)
  USE B2MOD_TYPES
  USE B2MOD_MATH_DIFF
  IMPLICIT NONE
  INTEGER :: n, incx
  REAL(kind=r8) :: sa, sx(0:n*incx-1)
  REAL(kind=r8) :: sab, sxb(0:n*incx-1)
  INTEGER :: i, ix
  EXTERNAL XERRAB
  INTEGER*4 :: branch
  CALL POPCONTROL2B(branch)
  IF (branch .NE. 0) THEN
    IF (branch .EQ. 1) THEN
      DO i=n-1,0,-1
        CALL POPREAL8(sx(i), r8/8)
        sxb(i) = 0.D0
      END DO
    ELSE
      CALL POPINTEGER4(ix)
      DO i=n-1,0,-1
        CALL POPREAL8(sx(ix+i*incx), r8/8)
        sxb(ix+i*incx) = 0.D0
      END DO
    END IF
  END IF
END SUBROUTINE SFILL_BWD

!
!
!
!
!
!
!
!
!
!
!
SUBROUTINE SFILL_NODIFF(n, sa, sx, incx)
  USE B2MOD_TYPES
  USE B2MOD_MATH_DIFF
  IMPLICIT NONE
  INTEGER :: n, incx
  REAL(kind=r8) :: sa, sx(0:n*incx-1)
!     ------------------------------------------------------------------
!     SFILL (BLAS-like routine) fills a vector with a constant value.
!     sfill(n,sa,sx,incx) sets all elements of the s.p. real*8 vector x
!     to sa. x is described by (n,sx,incx). BLAS storage conventions.
!     Return without effects if n.lt.1.
!     Undefined if incx.eq.0.
!     ------------------------------------------------------------------
  INTEGER :: i, ix
  EXTERNAL XERRAB
!     ------------------------------------------------------------------
  IF (incx .EQ. 0) THEN
    CALL XERRAB('sfill--error, incx.eq.0')
  ELSE IF (incx .EQ. 1) THEN
    DO i=0,n-1
      sx(i) = sa
    END DO
  ELSE
    ix = IBASE(n, incx)
    DO i=0,n-1
      sx(ix+i*incx) = sa
    END DO
  END IF
  RETURN
!     ------------------------------------------------------------------
END SUBROUTINE SFILL_NODIFF

