!        Generated by TAPENADE     (INRIA, Ecuador team)
!  Tapenade 3.16 (develop) - 23 Jul 2024 17:41
!
!  Differentiation of b2tfed in reverse (adjoint) mode (with options context noISIZE r8):
!   gradient     of useful results: veecrb ne vedia po te
!   with respect to varying inputs: ne po te
!   Plus diff mem management of: geo.cvonedbsq:in geo.fcbb:in geo.fchc:in
!                geo.fcht:in geo.fcqgam:in geo.fcqalf:in geo.fcqbet:in
!                geo.vxvol:in geo.vxonedbsq:in
!
!
!
!
!
!
!
!
!
!
!
!-----------------------------------------------------------------------
!.specification
!
SUBROUTINE B2TFED_B(ncv, nfc, nvx, switch, geo, geob, mpg, mpgb, &
& facdrift, fac_exb, ne, neb, te, teb, po, pob, vedia, vediab, veecrb, &
& veecrbb)
  USE B2MOD_TYPES
  USE B2MOD_CONSTANTS
  USE B2MOD_SWITCHES_DIFF
  USE B2US_GEO_DIFF
  USE B2US_MAP_DIFF
! csc The following are not necessary for computation but are needed
!     for adjoint AD to avoid side-effect variables
  USE B2MOD_AD_DIFF, ONLY : ncall_b2tfed
  USE B2MOD_SUBSYS
  IMPLICIT NONE
!   ..input arguments (unchanged on exit)
  INTEGER :: ncv, nfc, nvx
  TYPE(SWITCHES), INTENT(IN) :: switch
  TYPE(GEOMETRY), INTENT(IN) :: geo
  TYPE(GEOMETRY_DIFF) :: geob
  TYPE(MAPPING), INTENT(IN) :: mpg
  TYPE(MAPPING_DIFF) :: mpgb
  REAL(kind=r8) :: facdrift(nfc), fac_exb(nfc), ne(ncv), te(ncv), po(ncv&
& )
  REAL(kind=r8) :: neb(ncv), teb(ncv), pob(ncv)
!   ..output arguments (unspecified on entry)
  REAL(kind=r8) :: vedia(nfc, 0:1), veecrb(nfc, 0:1)
  REAL(kind=r8) :: vediab(nfc, 0:1), veecrbb(nfc, 0:1)
!-----------------------------------------------------------------------
!.documentation
!
!  1. purpose
!
!     B2TFED computes the electron drift velocities.
!
!-----------------------------------------------------------------------
!.declarations
!
!   ..local variables
  REAL(kind=r8) :: pov(nvx), dpo(nfc, 0:1), donedbsq(nfc, 0:1), nef(nfc)&
& , wrk(ncv), wrk0(nfc), wght(nfc, 2)
  REAL(kind=r8) :: povb(nvx), dpob(nfc, 0:1), nefb(nfc), wrkb(ncv), &
& wrk0b(nfc)
!   ..procedures
  EXTERNAL XERTST
  EXTERNAL B2XVSG
  INTRINSIC MINVAL
  INTRINSIC MAXVAL
  REAL(kind=r8) :: result1
  REAL(kind=r8) :: result2
!   ..initialisation
!
!-----------------------------------------------------------------------
!.computation
!
! ..preliminaries
!   ..subprogram start-up calls
!   ..test nCv, nFc
!   ..extensive tests on first few calls
  REAL(kind=r8), DIMENSION(nfc) :: tempb
!   ..test facdrift
!
! Drift terms
!xpb
!xpb
  CALL PUSHREAL8(result1, r8/8)
  result1 = MAXVAL(facdrift)
  CALL PUSHREAL8(result2, r8/8)
  result2 = MAXVAL(fac_exb)
  IF (result1 .NE. 0.0_R8 .OR. result2 .NE. 0.0_R8) THEN
!xpb
!wdk precomputation of gradients on faces
    CALL GRAD_NODIFF(ncv, nfc, nvx, 1, geo, mpg, geo%cvonedbsq, geo%&
&              vxonedbsq, donedbsq)
!     ..computation of ExB drift
!wdk    to be checked: now we use inverse volume weighting
!wdk                   (original code: simple averaging)
    wrk0 = geo%fcbb(:, 2)/geo%fcbb(:, 3)**2
!     ..computation of grad-B drift
    wght = 1.0_R8
    wrk = ne*te/qe
    CALL INTFACE_FWD(ncv, nfc, mpg%fccv, wght, wrk, wrk0)
    CALL INTFACE_FWD(ncv, nfc, mpg%fccv, wght, ne, nef)
!wdk    to be checked: interpolation of geometric factors (hy1)
!wdk                   now consistent between x and y faces
!wdk                   (and consistent with treatment of other drifts)
!wdk                   (implicitly through grad-routine)
    nefb = 0.D0
    wrk0b = 0.D0
    tempb = switch%raddidr*facdrift*geo%fcbb(:, 2)*donedbsq(:, 0)*vediab&
&     (:, 1)/nef
    vediab(:, 1) = 0.D0
    wrk0b = tempb
    nefb = -(wrk0*tempb/nef)
    tempb = -(switch%poldidr*facdrift*geo%fcbb(:, 2)*donedbsq(:, 1)*&
&     vediab(:, 0)/nef)
    wrk0b = wrk0b + tempb
    nefb = nefb - wrk0*tempb/nef
    CALL INTFACE_BWD(ncv, nfc, mpg%fccv, wght, ne, neb, nef, nefb)
    wrkb = 0.D0
    CALL INTFACE_BWD(ncv, nfc, mpg%fccv, wght, wrk, wrkb, wrk0, wrk0b)
    neb = neb + te*wrkb/qe
    teb = teb + ne*wrkb/qe
    dpob = 0.D0
    dpob(:, 0) = dpob(:, 0) + wrk0*switch%radeldr*fac_exb*veecrbb(:, 1)
    veecrbb(:, 1) = 0.D0
    dpob(:, 1) = dpob(:, 1) - wrk0*switch%poleldr*fac_exb*veecrbb(:, 0)
    povb = 0.D0
    CALL GRAD_B(ncv, nfc, nvx, 0, geo, geob, mpg, mpgb, po, pob, pov, &
&         povb, dpo, dpob)
  END IF
  CALL POPREAL8(result2, r8/8)
  CALL POPREAL8(result1, r8/8)
END SUBROUTINE B2TFED_B

!
!
!
!
!
!
!
!
!
!
!
!-----------------------------------------------------------------------
!.specification
!
SUBROUTINE B2TFED_NODIFF(ncv, nfc, nvx, switch, geo, mpg, facdrift, &
& fac_exb, ne, te, po, vedia, veecrb)
  USE B2MOD_TYPES
  USE B2MOD_CONSTANTS
  USE B2MOD_SWITCHES_DIFF
  USE B2US_GEO_DIFF
  USE B2US_MAP_DIFF
! csc The following are not necessary for computation but are needed
!     for adjoint AD to avoid side-effect variables
  USE B2MOD_AD_DIFF, ONLY : ncall_b2tfed
  USE B2MOD_SUBSYS
  IMPLICIT NONE
!   ..input arguments (unchanged on exit)
  INTEGER :: ncv, nfc, nvx
  TYPE(SWITCHES), INTENT(IN) :: switch
  TYPE(GEOMETRY), INTENT(IN) :: geo
  TYPE(MAPPING), INTENT(IN) :: mpg
  REAL(kind=r8) :: facdrift(nfc), fac_exb(nfc), ne(ncv), te(ncv), po(ncv&
& )
!   ..output arguments (unspecified on entry)
  REAL(kind=r8) :: vedia(nfc, 0:1), veecrb(nfc, 0:1)
!-----------------------------------------------------------------------
!.documentation
!
!  1. purpose
!
!     B2TFED computes the electron drift velocities.
!
!-----------------------------------------------------------------------
!.declarations
!
!   ..local variables
  REAL(kind=r8) :: pov(nvx), dpo(nfc, 0:1), donedbsq(nfc, 0:1), nef(nfc)&
& , wrk(ncv), wrk0(nfc), wght(nfc, 2)
!   ..procedures
  EXTERNAL XERTST
  EXTERNAL B2XVSG
  INTRINSIC MINVAL
  INTRINSIC MAXVAL
  REAL(kind=r8) :: result1
  REAL(kind=r8) :: result2
!   ..initialisation
!
!-----------------------------------------------------------------------
!.computation
!
! ..preliminaries
!   ..subprogram start-up calls
  CALL SUBINI('b2tfed')
!   ..test nCv, nFc
  CALL XERTST(0 .LT. ncv .AND. 0 .LT. nfc, 'faulty argument nCv, nFc')
!   ..extensive tests on first few calls
  IF (ncall_b2tfed .LT. 3) THEN
!    ..test sign of te
    CALL B2XVSG(ncv, ne, 1, 'ne', '.gt.')
    CALL B2XVSG(ncv, te, 1, 'te', '.gt.')
  END IF
!   ..test facdrift
  result1 = MINVAL(facdrift)
  result2 = MAXVAL(facdrift)
  CALL XERTST(0.0_R8 .LE. result1 .AND. 1.0_R8 .GE. result2, &
&       'faulty argument facdrift')
!
! Drift terms
!xpb
  vedia = 0.0_R8
!xpb
  veecrb = 0.0_R8
  result1 = MAXVAL(facdrift)
  result2 = MAXVAL(fac_exb)
  IF (result1 .NE. 0.0_R8 .OR. result2 .NE. 0.0_R8) THEN
!xpb
!wdk precomputation of gradients on faces
    CALL GRAD_NODIFF(ncv, nfc, nvx, 0, geo, mpg, po, pov, dpo)
    CALL GRAD_NODIFF(ncv, nfc, nvx, 1, geo, mpg, geo%cvonedbsq, geo%&
&              vxonedbsq, donedbsq)
!     ..computation of ExB drift
!wdk    to be checked: now we use inverse volume weighting
!wdk                   (original code: simple averaging)
    wrk0 = geo%fcbb(:, 2)/geo%fcbb(:, 3)**2
    veecrb(:, 0) = -(switch%poleldr*fac_exb*wrk0*dpo(:, 1))
    veecrb(:, 1) = switch%radeldr*fac_exb*wrk0*dpo(:, 0)
!     ..computation of grad-B drift
    wght = 1.0_R8
    wrk = ne*te/qe
    CALL INTFACE(ncv, nfc, mpg%fccv, wght, wrk, wrk0)
    CALL INTFACE(ncv, nfc, mpg%fccv, wght, ne, nef)
!wdk    to be checked: interpolation of geometric factors (hy1)
!wdk                   now consistent between x and y faces
!wdk                   (and consistent with treatment of other drifts)
!wdk                   (implicitly through grad-routine)
    vedia(:, 0) = -(switch%poldidr*facdrift*wrk0*geo%fcbb(:, 2)*donedbsq&
&     (:, 1)/nef)
    vedia(:, 1) = switch%raddidr*facdrift*wrk0*geo%fcbb(:, 2)*donedbsq(:&
&     , 0)/nef
  END IF
!
! ..return
  ncall_b2tfed = ncall_b2tfed + 1
  CALL SUBEND()
  RETURN
!
!-----------------------------------------------------------------------
!.end b2tfed
!
END SUBROUTINE B2TFED_NODIFF

