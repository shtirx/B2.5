!        Generated by TAPENADE     (INRIA, Ecuador team)
!  Tapenade 3.16 (develop) - 23 Jul 2024 17:41
!
!  Differentiation of b2sasum in reverse (adjoint) mode (with options context noISIZE r8):
!   gradient     of useful results: b2sasum sx
!   with respect to varying inputs: sx
SUBROUTINE B2SASUM_B(n, sx, sxb, incx, b2sasumb)
  USE B2MOD_TYPES
  USE B2MOD_MATH_DIFF
  IMPLICIT NONE
  REAL(kind=r8) :: b2sasum
  REAL(kind=r8) :: b2sasumb
  INTEGER :: n, incx
  REAL(kind=r8) :: sx(0:*)
  REAL(kind=r8) :: sxb(0:*)
!     ------------------------------------------------------------------
!     B2SASUM (BLAS routine) computes the L1-norm of a vector.
!     b2sasum(n,sx,incx) returns the sum of absolute values of the
!     elements in the d.p. real vector described by (n,sx,incx).
!     BLAS storage conventions. Return with b2sasum.eq.0 if n.lt.1.
!     Undefined if incx.eq.0.
!     ------------------------------------------------------------------
  INTEGER :: i, ix
  REAL(kind=r8) :: t
  REAL(kind=r8) :: tb
  INTRINSIC ABS
  EXTERNAL XERRAB
  REAL(kind=r8) :: abs0
  REAL(kind=r8) :: abs0b
  REAL(kind=r8) :: abs1
  REAL(kind=r8) :: abs1b
  INTEGER*4 :: branch
!     ------------------------------------------------------------------
  IF (incx .NE. 0) THEN
    IF (incx .EQ. 1) THEN
      DO i=0,n-1
        IF (sx(i) .GE. 0.) THEN
          CALL PUSHCONTROL1B(0)
        ELSE
          CALL PUSHCONTROL1B(1)
        END IF
      END DO
      tb = b2sasumb
      DO i=n-1,0,-1
        abs0b = tb
        CALL POPCONTROL1B(branch)
        IF (branch .EQ. 0) THEN
          sxb(i) = sxb(i) + abs0b
        ELSE
          sxb(i) = sxb(i) - abs0b
        END IF
      END DO
    ELSE
      ix = IBASE(n, incx)
      DO i=0,n-1
        IF (sx(ix) .GE. 0.) THEN
          CALL PUSHCONTROL1B(0)
        ELSE
          CALL PUSHCONTROL1B(1)
        END IF
        CALL PUSHINTEGER4(ix)
        ix = ix + incx
      END DO
      tb = b2sasumb
      DO i=n-1,0,-1
        CALL POPINTEGER4(ix)
        abs1b = tb
        CALL POPCONTROL1B(branch)
        IF (branch .EQ. 0) THEN
          sxb(ix) = sxb(ix) + abs1b
        ELSE
          sxb(ix) = sxb(ix) - abs1b
        END IF
      END DO
    END IF
  END IF
END SUBROUTINE B2SASUM_B

FUNCTION B2SASUM_NODIFF(n, sx, incx) RESULT (b2sasum)
  USE B2MOD_TYPES
  USE B2MOD_MATH_DIFF
  IMPLICIT NONE
  REAL(kind=r8) :: b2sasum
  INTEGER :: n, incx
  REAL(kind=r8) :: sx(0:*)
!     ------------------------------------------------------------------
!     B2SASUM (BLAS routine) computes the L1-norm of a vector.
!     b2sasum(n,sx,incx) returns the sum of absolute values of the
!     elements in the d.p. real vector described by (n,sx,incx).
!     BLAS storage conventions. Return with b2sasum.eq.0 if n.lt.1.
!     Undefined if incx.eq.0.
!     ------------------------------------------------------------------
  INTEGER :: i, ix
  REAL(kind=r8) :: t
  INTRINSIC ABS
  EXTERNAL XERRAB
  REAL(kind=r8) :: abs0
  REAL(kind=r8) :: abs1
!     ------------------------------------------------------------------
  IF (incx .EQ. 0) THEN
    CALL XERRAB('b2sasum--error, incx.eq.0')
  ELSE IF (incx .EQ. 1) THEN
    t = 0.0_R8
    DO i=0,n-1
      IF (sx(i) .GE. 0.) THEN
        abs0 = sx(i)
      ELSE
        abs0 = -sx(i)
      END IF
      t = t + abs0
    END DO
    b2sasum = t
  ELSE
    ix = IBASE(n, incx)
    t = 0.0_R8
    DO i=0,n-1
      IF (sx(ix) .GE. 0.) THEN
        abs1 = sx(ix)
      ELSE
        abs1 = -sx(ix)
      END IF
      t = t + abs1
      ix = ix + incx
    END DO
    b2sasum = t
  END IF
  RETURN
!     ------------------------------------------------------------------
END FUNCTION B2SASUM_NODIFF

FUNCTION B2S2SUM_NODIFF(n1, n2, sx, inc1x, inc2x) RESULT (b2s2sum)
  USE B2MOD_TYPES
  USE B2MOD_MATH_DIFF
  IMPLICIT NONE
  REAL(kind=r8) :: b2s2sum
  INTEGER :: n1, n2, inc1x, inc2x
  REAL(kind=r8) :: sx(*)
!     ------------------------------------------------------------------
!     COMPUTE SUM OF VALUES IN THE D.P. REAL MATRIX DESCRIBED BY
!     (N1, N2, SX, INC1X, INC2X). BLAS**2 STORAGE CONVENTIONS.
!     RETURN WITH B2S2SUM = 0 WHEN N1 .LT. 1 OR N2 .LT. 1.
!     ------------------------------------------------------------------
  INTEGER :: i, i2
  EXTERNAL B2SSUM_NODIFF
  REAL(kind=r8) :: t, B2SSUM_NODIFF
  INTEGER :: arg1
  REAL(kind=r8) :: result1
!     ------------------------------------------------------------------
  IF (n1 .LT. 1 .OR. n2 .LT. 1) THEN
    b2s2sum = 0.0_R8
    RETURN
!        - - - - - - -
  ELSE IF (n1*inc1x .EQ. inc2x) THEN
    arg1 = n1*n2
    b2s2sum = B2SSUM_NODIFF(arg1, sx, inc1x)
    RETURN
!        - - - - - - -
  ELSE
    t = 0.0_R8
    i2 = IBASE1(n2, inc2x)
    DO i=1,n2
      result1 = B2SSUM_NODIFF(n1, sx(i2), inc1x)
      t = t + result1
      i2 = i2 + inc2x
    END DO
    b2s2sum = t
    RETURN
!        - - - - - - -
  END IF
END FUNCTION B2S2SUM_NODIFF

!
!
!
!
!
!
!
!
!
!
!
FUNCTION B2SSUM_NODIFF(n, sx, incx) RESULT (b2ssum)
  USE B2MOD_TYPES
  USE B2MOD_MATH_DIFF
  IMPLICIT NONE
  REAL(kind=r8) :: b2ssum
  INTEGER :: n, incx
  REAL(kind=r8) :: sx(0:n*incx-1)
!     ------------------------------------------------------------------
!     B2SSUM (BLAS-like routine) sums the elements of a vector.
!     b2ssum(n,sx,incx) returns the sum of the elements in the d.p. real
!     vector described by (n,sx,incx). BLAS storage conventions.
!     Return with b2ssum.eq.0 if n.lt.1.
!     Undefined if incx.eq.0.
!     ------------------------------------------------------------------
  INTEGER :: i, ix
  REAL(kind=r8) :: t
  EXTERNAL XERRAB
!     ------------------------------------------------------------------
  IF (incx .EQ. 0) THEN
    CALL XERRAB('b2ssum--error, incx.eq.0')
  ELSE IF (incx .EQ. 1) THEN
    t = 0.0_R8
    DO i=0,n-1
      t = t + sx(i)
    END DO
    b2ssum = t
  ELSE
    ix = IBASE(n, incx)
    t = 0.0_R8
    DO i=0,n-1
      t = t + sx(ix)
      ix = ix + incx
    END DO
    b2ssum = t
  END IF
  RETURN
!     ------------------------------------------------------------------
END FUNCTION B2SSUM_NODIFF

!  Differentiation of b2sscal in reverse (adjoint) mode (with options context noISIZE r8):
!   gradient     of useful results: sx
!   with respect to varying inputs: sx
SUBROUTINE B2SSCAL_B(n, sa, sx, sxb, incx)
  USE B2MOD_TYPES
  USE B2MOD_MATH_DIFF
  IMPLICIT NONE
  INTEGER :: n, incx
  REAL(kind=r8) :: sa, sx(0:*)
  REAL(kind=r8) :: sxb(0:*)
!     ------------------------------------------------------------------
!     B2SSCAL (BLAS routine) performs the vector operation x:=sa*x.
!     x is an d.p. real vector, described by (n,sx,incx). BLAS storage
!     conventions. Return without effects if sa.eq.1.
!     Undefined if incx.eq.0.
!     ------------------------------------------------------------------
  INTEGER :: i, ix
  EXTERNAL XERRAB
!     ------------------------------------------------------------------
  IF (incx .NE. 0) THEN
    IF (sa .NE. 1.0_R8) THEN
      IF (incx .EQ. 1) THEN
        DO i=n-1,0,-1
          sxb(i) = sa*sxb(i)
        END DO
      ELSE
        ix = IBASE(n, incx)
        DO i=0,n-1
          CALL PUSHINTEGER4(ix)
          ix = ix + incx
        END DO
        DO i=n-1,0,-1
          CALL POPINTEGER4(ix)
          sxb(ix) = sa*sxb(ix)
        END DO
      END IF
    END IF
  END IF
END SUBROUTINE B2SSCAL_B

SUBROUTINE B2SSCAL_NODIFF(n, sa, sx, incx)
  USE B2MOD_TYPES
  USE B2MOD_MATH_DIFF
  IMPLICIT NONE
  INTEGER :: n, incx
  REAL(kind=r8) :: sa, sx(0:*)
!     ------------------------------------------------------------------
!     B2SSCAL (BLAS routine) performs the vector operation x:=sa*x.
!     x is an d.p. real vector, described by (n,sx,incx). BLAS storage
!     conventions. Return without effects if sa.eq.1.
!     Undefined if incx.eq.0.
!     ------------------------------------------------------------------
  INTEGER :: i, ix
  EXTERNAL XERRAB
!     ------------------------------------------------------------------
  IF (incx .EQ. 0) THEN
    CALL XERRAB('b2sscal--error, incx.eq.0')
  ELSE IF (sa .NE. 1.0_R8) THEN
    IF (incx .EQ. 1) THEN
      DO i=0,n-1
        sx(i) = sa*sx(i)
      END DO
    ELSE
      ix = IBASE(n, incx)
      DO i=0,n-1
        sx(ix) = sa*sx(ix)
        ix = ix + incx
      END DO
    END IF
  END IF
  RETURN
!     ------------------------------------------------------------------
END SUBROUTINE B2SSCAL_NODIFF

!  Differentiation of b2scopy in reverse (adjoint) mode, forward sweep (with options context noISIZE r8):
!   gradient     of useful results: sx sy
!   with respect to varying inputs: sx sy
SUBROUTINE B2SCOPY_FWD(n, sx, sxb, incx, sy, syb, incy)
  USE B2MOD_TYPES
  USE B2MOD_MATH_DIFF
  IMPLICIT NONE
  INTEGER :: n, incx, incy
  REAL(kind=r8) :: sx(0:*), sy(0:*)
!     ------------------------------------------------------------------
!     B2SCOPY (BLAS routine) performs the vector operation y:=x.
!     x and y are d.p. real vectors described by (n,sx,incx) and
!     (n,sy,incy). BLAS storage conventions.
!     Undefined if incy.eq.0.
!     ------------------------------------------------------------------
  INTEGER :: i, ix, iy
  EXTERNAL XERRAB
  REAL(kind=r8) :: sxb(0:*)
  REAL(kind=r8) :: syb(0:*)
!     ------------------------------------------------------------------
  IF (incy .EQ. 0) THEN
    CALL PUSHCONTROL2B(0)
  ELSE IF (incx .EQ. 1 .AND. incy .EQ. 1) THEN
    DO i=0,n-1
      CALL PUSHREAL8(sy(i), r8/8)
      sy(i) = sx(i)
    END DO
    CALL PUSHCONTROL2B(1)
  ELSE
    ix = IBASE(n, incx)
    iy = IBASE(n, incy)
    DO i=0,n-1
      CALL PUSHREAL8(sy(iy), r8/8)
      sy(iy) = sx(ix)
      CALL PUSHINTEGER4(ix)
      ix = ix + incx
      CALL PUSHINTEGER4(iy)
      iy = iy + incy
    END DO
    CALL PUSHCONTROL2B(2)
  END IF
END SUBROUTINE B2SCOPY_FWD

!  Differentiation of b2scopy in reverse (adjoint) mode, backward sweep (with options context noISIZE r8):
!   gradient     of useful results: sx sy
!   with respect to varying inputs: sx sy
SUBROUTINE B2SCOPY_BWD(n, sx, sxb, incx, sy, syb, incy)
  USE B2MOD_TYPES
  USE B2MOD_MATH_DIFF
  IMPLICIT NONE
  INTEGER :: n, incx, incy
  REAL(kind=r8) :: sx(0:*), sy(0:*)
  REAL(kind=r8) :: sxb(0:*), syb(0:*)
  INTEGER :: i, ix, iy
  EXTERNAL XERRAB
  INTEGER*4 :: branch
  CALL POPCONTROL2B(branch)
  IF (branch .NE. 0) THEN
    IF (branch .EQ. 1) THEN
      DO i=n-1,0,-1
        CALL POPREAL8(sy(i), r8/8)
        sxb(i) = sxb(i) + syb(i)
        syb(i) = 0.D0
      END DO
    ELSE
      DO i=n-1,0,-1
        CALL POPINTEGER4(iy)
        CALL POPINTEGER4(ix)
        CALL POPREAL8(sy(iy), r8/8)
        sxb(ix) = sxb(ix) + syb(iy)
        syb(iy) = 0.D0
      END DO
    END IF
  END IF
END SUBROUTINE B2SCOPY_BWD

SUBROUTINE B2SCOPY_NODIFF(n, sx, incx, sy, incy)
  USE B2MOD_TYPES
  USE B2MOD_MATH_DIFF
  IMPLICIT NONE
  INTEGER :: n, incx, incy
  REAL(kind=r8) :: sx(0:*), sy(0:*)
!     ------------------------------------------------------------------
!     B2SCOPY (BLAS routine) performs the vector operation y:=x.
!     x and y are d.p. real vectors described by (n,sx,incx) and
!     (n,sy,incy). BLAS storage conventions.
!     Undefined if incy.eq.0.
!     ------------------------------------------------------------------
  INTEGER :: i, ix, iy
  EXTERNAL XERRAB
!     ------------------------------------------------------------------
  IF (incy .EQ. 0) THEN
    CALL XERRAB('b2scopy--error, incy.eq.0')
  ELSE IF (incx .EQ. 1 .AND. incy .EQ. 1) THEN
    DO i=0,n-1
      sy(i) = sx(i)
    END DO
  ELSE
    ix = IBASE(n, incx)
    iy = IBASE(n, incy)
    DO i=0,n-1
      sy(iy) = sx(ix)
      ix = ix + incx
      iy = iy + incy
    END DO
  END IF
  RETURN
!     ------------------------------------------------------------------
END SUBROUTINE B2SCOPY_NODIFF

!  Differentiation of b2saxpy in reverse (adjoint) mode, forward sweep (with options context noISIZE r8):
!   gradient     of useful results: sx sy
!   with respect to varying inputs: sx sy
SUBROUTINE B2SAXPY_FWD(n, sa, sx, incx, sy, syb, incy)
  USE B2MOD_TYPES
  USE B2MOD_MATH_DIFF
  IMPLICIT NONE
  INTEGER :: n, incx, incy
  REAL(kind=r8) :: sa, sx(0:*), sy(0:*)
!     ------------------------------------------------------------------
!     B2SAXPY (BLAS routine) performs the vector operation y:=y+sa*x.
!     x and y are d.p. real vectors, described by (n,sx,incx) and
!     (n,sy,incy). BLAS storage conventions.
!     Return without effects if sa.eq.0.
!     Undefined if incy.eq.0.
!     ------------------------------------------------------------------
  INTEGER :: i, ix, iy
  EXTERNAL XERRAB
  REAL(kind=r8) :: syb(0:*)
!     ------------------------------------------------------------------
  IF (incy .EQ. 0) THEN
    CALL PUSHCONTROL2B(0)
  ELSE IF (sa .EQ. 0.0_R8) THEN
    CALL PUSHCONTROL2B(1)
  ELSE IF (incx .EQ. 1 .AND. incy .EQ. 1) THEN
    DO i=0,n-1
      CALL PUSHREAL8(sy(i), r8/8)
      sy(i) = sy(i) + sa*sx(i)
    END DO
    CALL PUSHCONTROL2B(2)
  ELSE
    ix = IBASE(n, incx)
    iy = IBASE(n, incy)
    DO i=0,n-1
      CALL PUSHREAL8(sy(iy), r8/8)
      sy(iy) = sy(iy) + sa*sx(ix)
      CALL PUSHINTEGER4(ix)
      ix = ix + incx
      CALL PUSHINTEGER4(iy)
      iy = iy + incy
    END DO
    CALL PUSHCONTROL2B(3)
  END IF
END SUBROUTINE B2SAXPY_FWD

!  Differentiation of b2saxpy in reverse (adjoint) mode, backward sweep (with options context noISIZE r8):
!   gradient     of useful results: sx sy
!   with respect to varying inputs: sx sy
SUBROUTINE B2SAXPY_BWD(n, sa, sx, sxb, incx, sy, syb, incy)
  USE B2MOD_TYPES
  USE B2MOD_MATH_DIFF
  IMPLICIT NONE
  INTEGER :: n, incx, incy
  REAL(kind=r8) :: sa, sx(0:*), sy(0:*)
  REAL(kind=r8) :: sxb(0:*), syb(0:*)
  INTEGER :: i, ix, iy
  EXTERNAL XERRAB
  INTEGER*4 :: branch
  CALL POPCONTROL2B(branch)
  IF (branch .GE. 2) THEN
    IF (branch .EQ. 2) THEN
      DO i=n-1,0,-1
        CALL POPREAL8(sy(i), r8/8)
        sxb(i) = sxb(i) + sa*syb(i)
      END DO
    ELSE
      DO i=n-1,0,-1
        CALL POPINTEGER4(iy)
        CALL POPINTEGER4(ix)
        CALL POPREAL8(sy(iy), r8/8)
        sxb(ix) = sxb(ix) + sa*syb(iy)
      END DO
    END IF
  END IF
END SUBROUTINE B2SAXPY_BWD

SUBROUTINE B2SAXPY_NODIFF(n, sa, sx, incx, sy, incy)
  USE B2MOD_TYPES
  USE B2MOD_MATH_DIFF
  IMPLICIT NONE
  INTEGER :: n, incx, incy
  REAL(kind=r8) :: sa, sx(0:*), sy(0:*)
!     ------------------------------------------------------------------
!     B2SAXPY (BLAS routine) performs the vector operation y:=y+sa*x.
!     x and y are d.p. real vectors, described by (n,sx,incx) and
!     (n,sy,incy). BLAS storage conventions.
!     Return without effects if sa.eq.0.
!     Undefined if incy.eq.0.
!     ------------------------------------------------------------------
  INTEGER :: i, ix, iy
  EXTERNAL XERRAB
!     ------------------------------------------------------------------
  IF (incy .EQ. 0) THEN
    CALL XERRAB('b2saxpy--error, incy.eq.0')
  ELSE IF (sa .NE. 0.0_R8) THEN
    IF (incx .EQ. 1 .AND. incy .EQ. 1) THEN
      DO i=0,n-1
        sy(i) = sy(i) + sa*sx(i)
      END DO
    ELSE
      ix = IBASE(n, incx)
      iy = IBASE(n, incy)
      DO i=0,n-1
        sy(iy) = sy(iy) + sa*sx(ix)
        ix = ix + incx
        iy = iy + incy
      END DO
    END IF
  END IF
  RETURN
!     ------------------------------------------------------------------
END SUBROUTINE B2SAXPY_NODIFF

FUNCTION B2S2MIN_NODIFF(n1, n2, sx, inc1x, inc2x) RESULT (b2s2min)
  USE B2MOD_TYPES
  USE B2MOD_MATH_DIFF
  IMPLICIT NONE
  REAL(kind=r8) :: b2s2min
  INTEGER :: n1, n2, inc1x, inc2x
  REAL(kind=r8) :: sx(*)
!     ------------------------------------------------------------------
!     COMPUTE MINIMUM VALUE IN THE D.P. REAL MATRIX DESCRIBED BY
!     (N1, N2, SX, INC1X, INC2X). BLAS**2 STORAGE CONVENTIONS.
!     N1 AND N2 MUST BE POSITIVE.
!     ------------------------------------------------------------------
  INTEGER :: i, i2
  EXTERNAL smin, XERRAB
  REAL(kind=r8) :: t, smin
  INTRINSIC MIN
  REAL(kind=r8) :: y1
  INTEGER :: arg1
!     ------------------------------------------------------------------
  IF (n1 .LT. 1 .OR. n2 .LT. 1) THEN
    CALL XERRAB('b2s2min: invalid n1/n2 values')
!        - - - - - - -
!     ---------------------------------------------------BJB-83/04/06---
  ELSE IF (n1*inc1x .EQ. inc2x) THEN
    arg1 = n1*n2
    b2s2min = smin(arg1, sx, inc1x)
    RETURN
!        - - - - - - -
  ELSE
    i2 = IBASE1(n2, inc2x)
    t = smin(n1, sx(i2), inc1x)
    i2 = i2 + inc2x
    DO i=2,n2
      y1 = smin(n1, sx(i2), inc1x)
      IF (t .GT. y1) THEN
        t = y1
      ELSE
        t = t
      END IF
      i2 = i2 + inc2x
    END DO
    b2s2min = t
    RETURN
!        - - - - - - -
  END IF
END FUNCTION B2S2MIN_NODIFF

