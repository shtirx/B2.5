!        Generated by TAPENADE     (INRIA, Ecuador team)
!  Tapenade 3.16 (develop) - 23 Jul 2024 17:41
!
!  Differentiation of diff in reverse (adjoint) mode, forward sweep (with options context noISIZE r8):
!   gradient     of useful results: funv dfun fun
!   with respect to varying inputs: funv dfun fun
!   Plus diff mem management of: geo.fchc:in geo.fcht:in geo.fcqalf:in
!                geo.fcqbet:in geo.vxvol:in
!
!
!
!
!
!
!
!
!
!
!
!-----------------------------------------------------------------------
!.specification
SUBROUTINE DIFF_FWD(ncv, nfc, nvx, mode, geo, geob, mpg, mpgb, fun, funv&
& , dfun)
  USE B2MOD_TYPES
  USE B2US_GEO_DIFF
  USE B2US_MAP_DIFF
  IMPLICIT NONE
!   ..input arguments
  INTEGER, INTENT(IN) :: ncv, nfc, nvx, mode
  TYPE(GEOMETRY), INTENT(IN) :: geo
  TYPE(MAPPING), INTENT(IN) :: mpg
  REAL(kind=r8) :: fun(ncv), funv(nvx)
!   ..output arguments
  REAL(kind=r8) :: dfun(nfc, 0:1)
!-----------------------------------------------------------------------
!.documentation
!
!     DIFF computes differences of a cell centered quantity fun on
!     faces of a cell.
!     dfun(:,0) will hold poloidal differences
!     dfun(:,1) will hold radial differences
!
!     The routine is a simple interface to DIFF_P and DIFF_R, that will
!     compute the actual poloidal and radial differences
!
!-----------------------------------------------------------------------
!.declarations
!
!   ..procedures
  EXTERNAL DIFF_P_NODIFF, DIFF_R_NODIFF
  EXTERNAL DIFF_P_FWD, DIFF_R_FWD
  TYPE(MAPPING_DIFF) :: mpgb
  TYPE(GEOMETRY_DIFF) :: geob
!
!-----------------------------------------------------------------------
!.computation      
!
!   ..test mode
!
!   ..interpolate fun to cell vertices, using volume weighted averaging
  IF (mode .EQ. 0) THEN
    CALL INTVERTEX_FWD(ncv, nvx, mpg, geo%vxvol, fun, funv)
    CALL PUSHCONTROL1B(0)
  ELSE
    CALL PUSHCONTROL1B(1)
  END IF
!
!   ..compute poloidal and radial differences
  CALL DIFF_P_FWD(ncv, nfc, nvx, 1, geo, mpg, mpgb, fun, funv, dfun(:, 0&
&           ))
  CALL DIFF_R_FWD(ncv, nfc, nvx, 1, geo, mpg, mpgb, fun, funv, dfun(:, 1&
&           ))
!
!   ..return
!
!-----------------------------------------------------------------------
!.end diff
!
END SUBROUTINE DIFF_FWD

!  Differentiation of diff in reverse (adjoint) mode, backward sweep (with options context noISIZE r8):
!   gradient     of useful results: funv dfun fun
!   with respect to varying inputs: funv dfun fun
!   Plus diff mem management of: geo.fchc:in geo.fcht:in geo.fcqalf:in
!                geo.fcqbet:in geo.vxvol:in
!
!
!
!
!
!
!
!
!
!
!
!-----------------------------------------------------------------------
!.specification
SUBROUTINE DIFF_BWD(ncv, nfc, nvx, mode, geo, geob, mpg, mpgb, fun, funb&
& , funv, funvb, dfun, dfunb)
  USE B2MOD_TYPES
  USE B2US_GEO_DIFF
  USE B2US_MAP_DIFF
  IMPLICIT NONE
  INTEGER, INTENT(IN) :: ncv, nfc, nvx, mode
  TYPE(GEOMETRY), INTENT(IN) :: geo
  TYPE(GEOMETRY_DIFF) :: geob
  TYPE(MAPPING), INTENT(IN) :: mpg
  TYPE(MAPPING_DIFF) :: mpgb
  REAL(kind=r8) :: fun(ncv), funv(nvx)
  REAL(kind=r8) :: funb(ncv), funvb(nvx)
  REAL(kind=r8) :: dfun(nfc, 0:1)
  REAL(kind=r8) :: dfunb(nfc, 0:1)
  EXTERNAL DIFF_P_NODIFF, DIFF_R_NODIFF
  EXTERNAL DIFF_P_BWD, DIFF_R_BWD
  INTEGER*4 :: branch
  CALL DIFF_R_BWD(ncv, nfc, nvx, 1, geo, mpg, mpgb, fun, funb, funv, &
&           funvb, dfun(:, 1), dfunb(:, 1))
  CALL DIFF_P_BWD(ncv, nfc, nvx, 1, geo, mpg, mpgb, fun, funb, funv, &
&           funvb, dfun(:, 0), dfunb(:, 0))
  CALL POPCONTROL1B(branch)
  IF (branch .EQ. 0) THEN
    CALL INTVERTEX_BWD(ncv, nvx, mpg, geo%vxvol, fun, funb, funv, funvb)
    funvb = 0.D0
  END IF
END SUBROUTINE DIFF_BWD

!
!
!
!
!
!
!
!
!
!
!
!-----------------------------------------------------------------------
!.specification
SUBROUTINE DIFF_NODIFF(ncv, nfc, nvx, mode, geo, mpg, fun, funv, dfun)
  USE B2MOD_TYPES
  USE B2US_GEO_DIFF
  USE B2US_MAP_DIFF
  IMPLICIT NONE
!   ..input arguments
  INTEGER, INTENT(IN) :: ncv, nfc, nvx, mode
  TYPE(GEOMETRY), INTENT(IN) :: geo
  TYPE(MAPPING), INTENT(IN) :: mpg
  REAL(kind=r8) :: fun(ncv), funv(nvx)
!   ..output arguments
  REAL(kind=r8) :: dfun(nfc, 0:1)
!-----------------------------------------------------------------------
!.documentation
!
!     DIFF computes differences of a cell centered quantity fun on
!     faces of a cell.
!     dfun(:,0) will hold poloidal differences
!     dfun(:,1) will hold radial differences
!
!     The routine is a simple interface to DIFF_P and DIFF_R, that will
!     compute the actual poloidal and radial differences
!
!-----------------------------------------------------------------------
!.declarations
!
!   ..procedures
  EXTERNAL DIFF_P_NODIFF, DIFF_R_NODIFF
!
!-----------------------------------------------------------------------
!.computation      
!
!   ..test mode
  CALL XERTST(0 .LE. mode .AND. mode .LE. 1, &
&       'diff--faulty argument mode')
!
!   ..interpolate fun to cell vertices, using volume weighted averaging
  IF (mode .EQ. 0) CALL INTVERTEX_NODIFF(ncv, nvx, mpg, geo%vxvol, fun, &
&                                  funv)
!
!   ..compute poloidal and radial differences
  CALL DIFF_P_NODIFF(ncv, nfc, nvx, 1, geo, mpg, fun, funv, dfun(:, 0))
  CALL DIFF_R_NODIFF(ncv, nfc, nvx, 1, geo, mpg, fun, funv, dfun(:, 1))
!
!   ..return
  RETURN
!
!-----------------------------------------------------------------------
!.end diff
!
END SUBROUTINE DIFF_NODIFF

