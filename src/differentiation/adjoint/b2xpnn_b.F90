!        Generated by TAPENADE     (INRIA, Ecuador team)
!  Tapenade 3.16 (develop) - 23 Jul 2024 17:41
!
!  Differentiation of b2xpnn in reverse (adjoint) mode (with options context noISIZE r8):
!   gradient     of useful results: na nn
!   with respect to varying inputs: na
!
!
!
!
!
!
!
!
!
!
!
SUBROUTINE B2XPNN_B(ncv, ns, na, nab, nn, nnb)
  USE B2MOD_TYPES
  USE B2MOD_CONSTANTS
  USE B2MOD_B2CMPA_DIFF
  IMPLICIT NONE
  INTEGER :: ncv, ns
  REAL(kind=r8) :: na(ncv, 0:ns-1), nn(ncv)
  REAL(kind=r8) :: nab(ncv, 0:ns-1), nnb(ncv)
!   ..common blocks
!     ------------------------------------------------------------------
!     B2XPNN computes the total density of hydrogenic neutrals, nn:
!       nn = (sum is, is_neutral(is).and.zn(is).eq.1 :: na(,is)) .
!     ------------------------------------------------------------------
  INTEGER :: is
  INTRINSIC NINT
!     ------------------------------------------------------------------
!$$$  call subini ('b2xpnn')
  INTEGER*4 :: branch
  DO is=0,ns-1
    IF (is_neutral(is) .AND. NINT(zn(is)) .EQ. 1) THEN
      CALL PUSHCONTROL1B(1)
    ELSE
      CALL PUSHCONTROL1B(0)
    END IF
  END DO
  DO is=ns-1,0,-1
    CALL POPCONTROL1B(branch)
    IF (branch .NE. 0) nab(:, is) = nab(:, is) + nnb
  END DO
END SUBROUTINE B2XPNN_B

!
!
!
!
!
!
!
!
!
!
!
SUBROUTINE B2XPNN_NODIFF(ncv, ns, na, nn)
  USE B2MOD_TYPES
  USE B2MOD_CONSTANTS
  USE B2MOD_B2CMPA_DIFF
  IMPLICIT NONE
  INTEGER :: ncv, ns
  REAL(kind=r8) :: na(ncv, 0:ns-1), nn(ncv)
!   ..common blocks
!     ------------------------------------------------------------------
!     B2XPNN computes the total density of hydrogenic neutrals, nn:
!       nn = (sum is, is_neutral(is).and.zn(is).eq.1 :: na(,is)) .
!     ------------------------------------------------------------------
  INTEGER :: is
  INTRINSIC NINT
!     ------------------------------------------------------------------
!$$$  call subini ('b2xpnn')
  nn = 0.0_R8
  DO is=0,ns-1
    IF (is_neutral(is) .AND. NINT(zn(is)) .EQ. 1) nn = nn + na(:, is)
  END DO
!$$$  call subend ()
  RETURN
!     ------------------------------------------------------------------
END SUBROUTINE B2XPNN_NODIFF

