!        Generated by TAPENADE     (INRIA, Ecuador team)
!  Tapenade 3.16 (develop) - 23 Jul 2024 17:41
!
!  Differentiation of b2ttia in reverse (adjoint) mode (with options context noISIZE r8):
!   gradient     of useful results: ti tauia lnlam rz2 ne2
!   with respect to varying inputs: ti tauia lnlam rz2 ne2
!
!
!
!
!
!
!
!
!
!
!
!-----------------------------------------------------------------------
!.specification
!
SUBROUTINE B2TTIA_B(ncv, ns, ti, tib, rz2, rz2b, ne2, ne2b, lnlam, &
& lnlamb, tauia, tauiab)
  USE B2MOD_TYPES
  USE B2MOD_CONSTANTS
  USE B2MOD_B2CMPA_DIFF
! csc The following are not necessary for computation but are needed
!     for adjoint AD to avoid side-effect variables
  USE B2MOD_AD_DIFF, ONLY : ncall_b2ttia
  USE B2MOD_SUBSYS
  IMPLICIT NONE
!   ..input arguments (unchanged on exit)
  INTEGER :: ncv, ns
  REAL(kind=r8) :: ti(ncv), ne2(ncv), lnlam(ncv), rz2(ncv, 0:ns-1)
  REAL(kind=r8) :: tib(ncv), ne2b(ncv), lnlamb(ncv), rz2b(ncv, 0:ns-1)
!   ..output arguments (unspecified on entry)
  REAL(kind=r8) :: tauia(ncv, 0:ns-1)
  REAL(kind=r8) :: tauiab(ncv, 0:ns-1)
!   ..common blocks
!-----------------------------------------------------------------------
!.documentation
!
!  1. purpose
!
!     B2TTIA computes the classical Braginskii time of the ion-atom collision.
!
!
!  3. description (see also routine b2cdca)
!
!     This routine computes the classical Braginskii time of the ion-atom collision.
!     This coefficient is differed from Balescu coefficient by a factor sqrt(2).
!     - tauia: the classical Braginskii time of the ion-atom collision.
!
!  4. references
!
!     /1/ R. Balescu: Transport Processes in Plasmas, Vol. 1.
!     North Holland, Amsterdam, 1988.
!*
!-----------------------------------------------------------------------
!.declarations
!
!   ..global named constants
!   ..local variables
  INTEGER :: icv, is
  REAL(kind=r8) :: ctaup, t0, t1
  REAL(kind=r8) :: t0b
!   ..procedures
  INTRINSIC SQRT
  EXTERNAL XERTST, SFILL_NODIFF
  EXTERNAL B2XVSG
!   ..initialization
!
!-----------------------------------------------------------------------
!.documentation-internal
!
!     Description of some local variables:
!
!     lnlam is the log of the plasma parameter.
!
!     Description of some local procedures:
!
!
!-----------------------------------------------------------------------
!.computation
!
! ..preliminaries
!   ..subprogram start-up calls
!   ..test nCv, ns
!   ..extensive tests on first few calls
  REAL(kind=r8) :: tempb
  REAL(kind=r8) :: temp
  REAL(kind=r8) :: temp0
  REAL(kind=r8) :: temp1
  REAL(kind=r8) :: temp2
  REAL(kind=r8) :: tempb0
  REAL(kind=r8) :: tempb1
  REAL(kind=r8) :: tempb2
  INTEGER*4 :: branch
!   ..compute ctaup
!     Following Braginskii                                           !srv 11.07.99
!     The mathematical expression is:
!       t0 = (3/(4*sqrt(pi)))*(4*pi*eps0/qe**2)**2*ev*sqrt(ev)       !srv delete sqrt(2)
!       ctaup = t0*sqrt(mp)/lnlam
!     The numerical value in SI units is: ctaup = 1.4743e13/lnlam.
!     The following calculation of t0 is designed to avoid overflow
!     even for IEEE single precision arithmetic.
  t1 = 4*pi*eps0/qe/qe
!srv delete sqrt(2)
  t0 = 3/(4*SQRT(pi))*t1*(t1*ev*SQRT(ev))
!/lnlam           !srv 01.07.09    it is taken below
  ctaup = t0*SQRT(mp)
!
! ..compute tauia
  DO is=0,ns-1
    IF (.NOT.is_neutral(is)) THEN
      CALL PUSHCONTROL1B(1)
    ELSE
      CALL PUSHCONTROL1B(0)
    END IF
  END DO
  DO is=ns-1,0,-1
    CALL POPCONTROL1B(branch)
    IF (branch .EQ. 0) THEN
      tauiab(:, is) = 0.D0
    ELSE
      DO icv=ncv,1,-1
        t0 = ti(icv)/ev
        temp = lnlam(icv)*ne2(icv)
        temp0 = rz2(icv, is)*temp
        temp1 = t0/temp0
        temp2 = SQRT(t0)
        tempb = ctaup*SQRT(am(is))*tauiab(icv, is)
        tauiab(icv, is) = 0.D0
        tempb0 = temp2*tempb/temp0
        IF (t0 .EQ. 0.D0) THEN
          t0b = tempb0
        ELSE
          t0b = temp1*tempb/(2.0*temp2) + tempb0
        END IF
        tempb1 = -(temp1*tempb0)
        rz2b(icv, is) = rz2b(icv, is) + temp*tempb1
        tempb2 = rz2(icv, is)*tempb1
        lnlamb(icv) = lnlamb(icv) + ne2(icv)*tempb2
        ne2b(icv) = ne2b(icv) + lnlam(icv)*tempb2
        tib(icv) = tib(icv) + t0b/ev
      END DO
    END IF
  END DO
END SUBROUTINE B2TTIA_B
!srv 16.06.08 }

!
!
!
!
!
!
!
!
!
!
!
!-----------------------------------------------------------------------
!.specification
!
SUBROUTINE B2TTIA_NODIFF(ncv, ns, ti, rz2, ne2, lnlam, tauia)
  USE B2MOD_TYPES
  USE B2MOD_CONSTANTS
  USE B2MOD_B2CMPA_DIFF
! csc The following are not necessary for computation but are needed
!     for adjoint AD to avoid side-effect variables
  USE B2MOD_AD_DIFF, ONLY : ncall_b2ttia
  USE B2MOD_SUBSYS
  IMPLICIT NONE
!   ..input arguments (unchanged on exit)
  INTEGER :: ncv, ns
  REAL(kind=r8) :: ti(ncv), ne2(ncv), lnlam(ncv), rz2(ncv, 0:ns-1)
!   ..output arguments (unspecified on entry)
  REAL(kind=r8) :: tauia(ncv, 0:ns-1)
!   ..common blocks
!-----------------------------------------------------------------------
!.documentation
!
!  1. purpose
!
!     B2TTIA computes the classical Braginskii time of the ion-atom collision.
!
!
!  3. description (see also routine b2cdca)
!
!     This routine computes the classical Braginskii time of the ion-atom collision.
!     This coefficient is differed from Balescu coefficient by a factor sqrt(2).
!     - tauia: the classical Braginskii time of the ion-atom collision.
!
!  4. references
!
!     /1/ R. Balescu: Transport Processes in Plasmas, Vol. 1.
!     North Holland, Amsterdam, 1988.
!*
!-----------------------------------------------------------------------
!.declarations
!
!   ..global named constants
!   ..local variables
  INTEGER :: icv, is
  REAL(kind=r8) :: ctaup, t0, t1
!   ..procedures
  INTRINSIC SQRT
  EXTERNAL XERTST, SFILL_NODIFF
  EXTERNAL B2XVSG
!   ..initialization
!
!-----------------------------------------------------------------------
!.documentation-internal
!
!     Description of some local variables:
!
!     lnlam is the log of the plasma parameter.
!
!     Description of some local procedures:
!
!
!-----------------------------------------------------------------------
!.computation
!
! ..preliminaries
!   ..subprogram start-up calls
  CALL SUBINI('b2ttia')
!   ..test nCv, ns
  CALL XERTST(0 .LT. ncv, 'faulty argument nCv')
  CALL XERTST(1 .LE. ns, 'faulty argument ns')
!   ..extensive tests on first few calls
  IF (ncall_b2ttia .LT. 3) THEN
!    ..test state
    CALL B2XVSG(ncv, ti, 1, 'ti', '.gt.')
    CALL B2XVSG(ncv, ne2, 1, 'ne2', '.gt.')
  END IF
!   ..compute ctaup
!     Following Braginskii                                           !srv 11.07.99
!     The mathematical expression is:
!       t0 = (3/(4*sqrt(pi)))*(4*pi*eps0/qe**2)**2*ev*sqrt(ev)       !srv delete sqrt(2)
!       ctaup = t0*sqrt(mp)/lnlam
!     The numerical value in SI units is: ctaup = 1.4743e13/lnlam.
!     The following calculation of t0 is designed to avoid overflow
!     even for IEEE single precision arithmetic.
  t1 = 4*pi*eps0/qe/qe
!srv delete sqrt(2)
  t0 = 3/(4*SQRT(pi))*t1*(t1*ev*SQRT(ev))
!/lnlam           !srv 01.07.09    it is taken below
  ctaup = t0*SQRT(mp)
!
! ..compute tauia
  DO is=0,ns-1
    IF (.NOT.is_neutral(is)) THEN
      DO icv=1,ncv
        t0 = ti(icv)/ev
!srv 01.07.09
        tauia(icv, is) = ctaup/lnlam(icv)*SQRT(am(is))*t0*SQRT(t0)/(rz2(&
&         icv, is)*ne2(icv))
      END DO
    ELSE
      tauia(:, is) = 0.0_R8
    END IF
  END DO
!
! ..return
  ncall_b2ttia = ncall_b2ttia + 1
  CALL SUBEND()
  RETURN
!
!-----------------------------------------------------------------------
!.end b2ttia
!
END SUBROUTINE B2TTIA_NODIFF
!srv 16.06.08 }

