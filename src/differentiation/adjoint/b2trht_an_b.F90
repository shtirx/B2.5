!        Generated by TAPENADE     (INRIA, Ecuador team)
!  Tapenade 3.16 (develop) - 23 Jul 2024 17:41
!
!  Differentiation of b2trht_an in reverse (adjoint) mode (with options context noISIZE r8):
!   gradient     of useful results: *nal c_ht_an ti nz2n hci0_al
!                zetap zeffne
!   with respect to varying inputs: *nal c_ht_an ti nz2n hci0_al
!                zetap zeffne
!   Plus diff mem management of: nal:in
!
!
!
!
!
!
!
!
!
!
!
!-----------------------------------------------------------------------
!.specification
!
SUBROUTINE B2TRHT_AN_B(nnucl, icv, pit, zetap, zetapb, zeffne, zeffneb, &
& ti, tib, c_ht_an, c_ht_anb, nz2n, nz2nb, rmd2, hci0_al, hci0_alb)
  USE B2MOD_TYPES
  USE B2MOD_SUBSYS
  USE B2MOD_CONSTANTS
  USE B2MOD_ZHFRTF_DIFF, ONLY : nal, nalb
  IMPLICIT NONE
!   ..input arguments (unchanged on exit)
  INTEGER :: nnucl, icv
  REAL(kind=r8) :: pit, zetap, zeffne, ti
  REAL(kind=r8) :: zetapb, zeffneb, tib
  REAL(kind=r8) :: c_ht_an(0:nnucl-1), nz2n(0:nnucl-1), rmd2(0:nnucl-1)
  REAL(kind=r8) :: c_ht_anb(0:nnucl-1), nz2nb(0:nnucl-1)
!   ..output arguments 
  REAL(kind=r8) :: hci0_al(0:nnucl-1)
  REAL(kind=r8) :: hci0_alb(0:nnucl-1)
!-----------------------------------------------------------------------
!.documentation
!
!  1. purpose
!
!     B2TRHT_AN computes the ion heat conductivity coefficient using
!     the analytic method.
!
!  3. description 
!
!     This routine computes:
!     -hci0_al: the ion heat conductivity coefficient 
!
!-----------------------------------------------------------------------
!.declarations
!   ..local variables
  INTEGER :: j
!
!-----------------------------------------------------------------------
!.computation
!
!   ..subprogram start-up calls
  REAL(kind=r8) :: temp
  REAL(kind=r8) :: tempb
  REAL(kind=r8) :: temp0
  REAL(kind=r8) :: temp1
  REAL(kind=r8) :: tempb0
  REAL(kind=r8) :: tempb1
  DO j=nnucl-1,0,-1
    temp = mp*rmd2(j)*(zeffne*zeffne)*(nz2n(j)*nz2n(j))
    temp0 = nal(icv, j)
    temp1 = zetap*ti*c_ht_an(j)
    tempb = pit**2*hci0_alb(j)/temp
    hci0_alb(j) = 0.D0
    tempb0 = temp0**2*tempb
    nalb(icv, j) = nalb(icv, j) + 2*temp0*temp1*tempb
    tempb1 = -(mp*rmd2(j)*temp1*temp0**2*tempb/temp)
    zeffneb = zeffneb + 2*zeffne*nz2n(j)**2*tempb1
    nz2nb(j) = nz2nb(j) + 2*nz2n(j)*zeffne**2*tempb1
    zetapb = zetapb + ti*c_ht_an(j)*tempb0
    tib = tib + zetap*c_ht_an(j)*tempb0
    c_ht_anb(j) = c_ht_anb(j) + zetap*ti*tempb0
  END DO
END SUBROUTINE B2TRHT_AN_B
!
!

!
!
!
!
!
!
!
!
!
!
!
!-----------------------------------------------------------------------
!.specification
!
SUBROUTINE B2TRHT_AN_NODIFF(nnucl, icv, pit, zetap, zeffne, ti, c_ht_an&
& , nz2n, rmd2, hci0_al)
  USE B2MOD_TYPES
  USE B2MOD_SUBSYS
  USE B2MOD_CONSTANTS
  USE B2MOD_ZHFRTF_DIFF, ONLY : nal
  IMPLICIT NONE
!   ..input arguments (unchanged on exit)
  INTEGER :: nnucl, icv
  REAL(kind=r8) :: pit, zetap, zeffne, ti
  REAL(kind=r8) :: c_ht_an(0:nnucl-1), nz2n(0:nnucl-1), rmd2(0:nnucl-1)
!   ..output arguments 
  REAL(kind=r8) :: hci0_al(0:nnucl-1)
!-----------------------------------------------------------------------
!.documentation
!
!  1. purpose
!
!     B2TRHT_AN computes the ion heat conductivity coefficient using
!     the analytic method.
!
!  3. description 
!
!     This routine computes:
!     -hci0_al: the ion heat conductivity coefficient 
!
!-----------------------------------------------------------------------
!.declarations
!   ..local variables
  INTEGER :: j
!
!-----------------------------------------------------------------------
!.computation
!
!   ..subprogram start-up calls
  CALL SUBINI('b2trht_an')
!   ..main
!   ..sums using analytical coefficient
  DO j=0,nnucl-1
    hci0_al(j) = pit**2*(zetap/zeffne**2)*ti/mp*c_ht_an(j)/rmd2(j)*nal(&
&     icv, j)**2/nz2n(j)**2
  END DO
!
  CALL SUBEND()
  RETURN
!
END SUBROUTINE B2TRHT_AN_NODIFF
!
!

