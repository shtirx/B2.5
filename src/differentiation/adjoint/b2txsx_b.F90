!        Generated by TAPENADE     (INRIA, Ecuador team)
!  Tapenade 3.16 (develop) - 23 Jul 2024 17:41
!
!  Differentiation of b2txsx in reverse (adjoint) mode (with options context noISIZE r8):
!   gradient     of useful results: fun funsx
!   with respect to varying inputs: fun funsx
!
!
!
!
!
!
!
!
!
!
!
SUBROUTINE B2TXSX_B(ncv, nfc, geo, mpg, fcvol, fcs, fun, funb, funsx, &
& funsxb)
  USE B2MOD_TYPES
  USE B2US_GEO_DIFF
  USE B2US_MAP_DIFF
  USE B2MOD_SUBSYS
  IMPLICIT NONE
  INTEGER, INTENT(IN) :: ncv, nfc
  TYPE(GEOMETRY), INTENT(IN) :: geo
  TYPE(MAPPING), INTENT(IN) :: mpg
  REAL(kind=r8) :: fcvol(nfc, 2), fcs(nfc), fun(ncv), funsx(nfc)
  REAL(kind=r8) :: funb(ncv), funsxb(nfc)
!     ------------------------------------------------------------------
!     B2TXSX computes the integrals over cell x-faces of a function
!     that is provided on the cells.
!!!   (Reconsider specification and use of this routine for the case
!     of a non-orthogonal grid.)
!     ------------------------------------------------------------------
  INTEGER :: ifc
!     ------------------------------------------------------------------
!   ..test nCv, nFc
  REAL(kind=r8) :: tempb
  DO ifc=nfc,1,-1
    tempb = fcs(ifc)*geo%fcqalf(ifc, 0)*funsxb(ifc)/(fcvol(ifc, 1)+fcvol&
&     (ifc, 2))
    funsxb(ifc) = 0.D0
    funb(mpg%fccv(ifc, 1)) = funb(mpg%fccv(ifc, 1)) + fcvol(ifc, 2)*&
&     tempb
    funb(mpg%fccv(ifc, 2)) = funb(mpg%fccv(ifc, 2)) + fcvol(ifc, 1)*&
&     tempb
  END DO
END SUBROUTINE B2TXSX_B

!
!
!
!
!
!
!
!
!
!
!
SUBROUTINE B2TXSX_NODIFF(ncv, nfc, geo, mpg, fcvol, fcs, fun, funsx)
  USE B2MOD_TYPES
  USE B2US_GEO_DIFF
  USE B2US_MAP_DIFF
  USE B2MOD_SUBSYS
  IMPLICIT NONE
  INTEGER, INTENT(IN) :: ncv, nfc
  TYPE(GEOMETRY), INTENT(IN) :: geo
  TYPE(MAPPING), INTENT(IN) :: mpg
  REAL(kind=r8) :: fcvol(nfc, 2), fcs(nfc), fun(ncv), funsx(nfc)
!     ------------------------------------------------------------------
!     B2TXSX computes the integrals over cell x-faces of a function
!     that is provided on the cells.
!!!   (Reconsider specification and use of this routine for the case
!     of a non-orthogonal grid.)
!     ------------------------------------------------------------------
  INTEGER :: ifc
!     ------------------------------------------------------------------
  CALL SUBINI('b2txsx')
!   ..test nCv, nFc
  CALL XERTST(0 .LT. ncv .AND. 0 .LT. nfc, 'faulty argument nCv,nFc')
!   ..compute funsx
  DO ifc=1,nfc
    funsx(ifc) = fcs(ifc)*geo%fcqalf(ifc, 0)*(fcvol(ifc, 2)*fun(mpg%fccv&
&     (ifc, 1))+fcvol(ifc, 1)*fun(mpg%fccv(ifc, 2)))/(fcvol(ifc, 1)+&
&     fcvol(ifc, 2))
  END DO
!   ..return
  CALL SUBEND()
  RETURN
!     ------------------------------------------------------------------
END SUBROUTINE B2TXSX_NODIFF

