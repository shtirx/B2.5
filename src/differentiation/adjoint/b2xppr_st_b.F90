!        Generated by TAPENADE     (INRIA, Ecuador team)
!  Tapenade 3.16 (develop) - 23 Jul 2024 17:41
!
!
!
!
!
!
!
!
!
!
!
!
SUBROUTINE B2XPPR_ST_NODIFF(nx, ny, ns, ne, na, te, ti, pr)
  USE B2MOD_TYPES
  USE B2MOD_EXTERNAL_DIFF
  IMPLICIT NONE
  INTEGER :: nx, ny, ns
  REAL(kind=r8) :: ne(-1:nx, -1:ny), na(-1:nx, -1:ny, 0:ns-1), te(-1:nx&
& , -1:ny), ti(-1:nx, -1:ny), pr(-1:nx, -1:ny)
!     ------------------------------------------------------------------
!     B2XPPR_ST computes the total pressure, pr:
!       pr(,) = ne(,)*te(,) + (sum is :: na(,,is)*ti(,)) .
!     The routines only works on structured grids.
!     ------------------------------------------------------------------
  INTEGER :: n2, ix, iy, is
  EXTERNAL SFILL_NODIFF
!     ------------------------------------------------------------------
!$$$  call subini ('b2xppr_st')
  n2 = (nx+2)*(ny+2)
  CALL SFILL_NODIFF(n2, 0.0_R8, pr, 1)
  pr = ne*te
  DO is=0,ns-1
    DO iy=-1,ny
      DO ix=-1,nx
        pr(ix, iy) = pr(ix, iy) + na(ix, iy, is)*ti(ix, iy)
      END DO
    END DO
  END DO
  DO is=0,ns_ext-1
    DO iy=-1,ny
      DO ix=-1,nx
        pr(ix, iy) = pr(ix, iy) + na_ext(ix, iy, is)*ta_ext(ix, iy, is)
      END DO
    END DO
  END DO
!$$$  call subend ()
  RETURN
!     ------------------------------------------------------------------
END SUBROUTINE B2XPPR_ST_NODIFF

