!        Generated by TAPENADE     (INRIA, Ecuador team)
!  Tapenade 3.16 (develop) - 23 Jul 2024 17:41
!
!
!
!
!
!
!
!
!
!
!
!
MODULE B2MOD_BOUNDARY_SOURCES_DIFF
!srv 26.02.18 {
  USE B2MOD_TYPES
  IMPLICIT NONE
  REAL(kind=r8), ALLOCATABLE, SAVE :: bv_po(:), bv_te(:), bv_ti(:), &
& bv_tn(:), bv_ua(:, :), bv_na(:, :)
!

CONTAINS
!
  SUBROUTINE ALLOC_B2MOD_BOUNDARY_SOURCES(ncv, nsd)
    IMPLICIT NONE
    INTEGER :: ncv, nsd
!
    ALLOCATE(bv_po(ncv))
    ALLOCATE(bv_te(ncv))
    ALLOCATE(bv_ti(ncv))
    ALLOCATE(bv_tn(ncv))
    ALLOCATE(bv_ua(ncv, 0:nsd-1))
    ALLOCATE(bv_na(ncv, 0:nsd-1))
!
    bv_po = 0.0_R8
    bv_te = 0.0_R8
    bv_ti = 0.0_R8
    bv_tn = 0.0_R8
    bv_ua = 0.0_R8
    bv_na = 0.0_R8
!
    WRITE(*, *) 'Allocated space for b2mod_boundary_sources'
    RETURN
!
  END SUBROUTINE ALLOC_B2MOD_BOUNDARY_SOURCES

!
  SUBROUTINE DEALLOC_B2MOD_BOUNDARY_SOURCES()
    IMPLICIT NONE
!
    DEALLOCATE(bv_po)
    DEALLOCATE(bv_te)
    DEALLOCATE(bv_ti)
    DEALLOCATE(bv_tn)
    DEALLOCATE(bv_ua)
    DEALLOCATE(bv_na)
!
    RETURN
  END SUBROUTINE DEALLOC_B2MOD_BOUNDARY_SOURCES

!
  SUBROUTINE READ_BOUNDARY_SOURCES(ncv, ns, switch, itim, mpg)
    USE B2MOD_TYPES
    USE B2MOD_SWITCHES_DIFF
    USE B2MOD_NUMERICS_NAMELIST_DIFF
    USE B2US_MAP_DIFF
    IMPLICIT NONE
    TYPE(SWITCHES), INTENT(IN) :: switch
    TYPE(MAPPING), INTENT(IN) :: mpg
    INTEGER :: ncv, ns, itim
    INTEGER :: i, is, icv
    LOGICAL :: b2news_solving(4)
    CHARACTER(len=36) :: filename
    LOGICAL :: lexist
    CHARACTER(len=4) :: chitim
    INTRINSIC ABS
    INTRINSIC MOD
    INTRINSIC ANY
    EXTERNAL FIND_FILE
    INTRINSIC TRIM
    INTEGER :: abs0
!
!      Check equations to be solved
    b2news_solving(1:4) = switch%no_solve .EQ. 0
    DO i=1,4
      IF (switch%no_solve .GE. 0.) THEN
        abs0 = switch%no_solve
      ELSE
        abs0 = -switch%no_solve
      END IF
      b2news_solving(i) = b2news_solving(i) .OR. (switch%no_solve .LT. 0&
&       .AND. MOD(abs0, 2**i) .GE. 2**(i-1))
    END DO
    CALL READ_B2MOD_NUMERICS_NAMELIST(ncv, ns, switch%nsmin, switch%&
&                               nsmax, mpg%nnreg, mpg%cvonclosedsurface&
&                               , switch%b2mndt_style, .false.)
!
!      Read MMS inputs
!
    IF (b2news_solving(2) .AND. ANY(solveco(0:ns-1, 0:mpg%nnreg(0)))) &
&   THEN
      IF (switch%use_mms .EQ. 1) THEN
        filename = 'bv_na_us.dat'
        CALL FIND_FILE(filename, lexist)
        CALL XERTST(lexist, 'File '//TRIM(filename)//' not found')
        OPEN(70, file=trim(filename)) 
        READ(70, '(1x,e24.16)') ((bv_na(icv, is), icv=1,ncv), is=0,ns-1)
        CLOSE(70) 
      ELSE IF (switch%use_mms .EQ. 2) THEN
        WRITE(chitim, '(i4.4)') itim
        filename = 'bv_na_us'//chitim//'.dat'
        CALL FIND_FILE(filename, lexist)
        CALL XERTST(lexist, 'File '//TRIM(filename)//' not found')
        OPEN(70, file=trim(filename)) 
        READ(70, '(1x,e24.16)') ((bv_na(icv, is), icv=1,ncv), is=0,ns-1)
        CLOSE(70) 
      END IF
    END IF
!
    IF (b2news_solving(1) .AND. ANY(solvemo(0:ns-1, 0:mpg%nnreg(0)))) &
&   THEN
      IF (switch%use_mms .EQ. 1) THEN
        filename = 'bv_ua_us.dat'
        CALL FIND_FILE(filename, lexist)
        CALL XERTST(lexist, 'File '//TRIM(filename)//' not found')
        OPEN(70, file=trim(filename)) 
        READ(70, '(1x,e24.16)') ((bv_ua(icv, is), icv=1,ncv), is=0,ns-1)
        CLOSE(70) 
      ELSE IF (switch%use_mms .EQ. 2) THEN
        WRITE(chitim, '(i4.4)') itim
        filename = 'bv_ua_us'//chitim//'.dat'
        CALL FIND_FILE(filename, lexist)
        CALL XERTST(lexist, 'File '//TRIM(filename)//' not found')
        OPEN(70, file=trim(filename)) 
        READ(70, '(1x,e24.16)') ((bv_ua(icv, is), icv=1,ncv), is=0,ns-1)
        CLOSE(70) 
      END IF
    END IF
!
    IF (b2news_solving(4)) THEN
      IF (ANY(solveee(0:mpg%nnreg(0)))) THEN
        IF (switch%use_mms .EQ. 1) THEN
          filename = 'bv_te_us.dat'
          CALL FIND_FILE(filename, lexist)
          CALL XERTST(lexist, 'File '//TRIM(filename)//' not found')
          OPEN(70, file=trim(filename)) 
          READ(70, '(1x,e24.16)') (bv_te(icv), icv=1,ncv)
          CLOSE(70) 
        ELSE IF (switch%use_mms .EQ. 2) THEN
          WRITE(chitim, '(i4.4)') itim
          filename = 'bv_te_us'//chitim//'.dat'
          CALL FIND_FILE(filename, lexist)
          CALL XERTST(lexist, 'File '//TRIM(filename)//' not found')
          OPEN(70, file=trim(filename)) 
          READ(70, '(1x,e24.16)') (bv_te(icv), icv=1,ncv)
          CLOSE(70) 
        END IF
      END IF
      IF (ANY(solveei(0:mpg%nnreg(0)))) THEN
        IF (switch%use_mms .EQ. 1) THEN
          filename = 'bv_ti_us.dat'
          CALL FIND_FILE(filename, lexist)
          CALL XERTST(lexist, 'File '//TRIM(filename)//' not found')
          OPEN(70, file=trim(filename)) 
          READ(70, '(1x,e24.16)') (bv_ti(icv), icv=1,ncv)
          CLOSE(70) 
        ELSE IF (switch%use_mms .EQ. 2) THEN
          WRITE(chitim, '(i4.4)') itim
          filename = 'bv_ti_us'//chitim//'.dat'
          CALL FIND_FILE(filename, lexist)
          CALL XERTST(lexist, 'File '//TRIM(filename)//' not found')
          OPEN(70, file=trim(filename)) 
          READ(70, '(1x,e24.16)') (bv_ti(icv), icv=1,ncv)
          CLOSE(70) 
        END IF
      END IF
    END IF
!
    IF (b2news_solving(3) .AND. ANY(solvepo(0:mpg%nnreg(0)))) THEN
      filename = 'bv_po_us.dat'
      CALL FIND_FILE(filename, lexist)
      CALL XERTST(lexist, 'File '//TRIM(filename)//' not found')
      OPEN(70, file=trim(filename)) 
      READ(70, '(1x,e24.16)') (bv_po(icv), icv=1,ncv)
      CLOSE(70) 
    END IF
!
    IF (switch%tn_style .EQ. 2) THEN
      IF (ANY(solveen(0:mpg%nnreg(0)))) THEN
        IF (switch%use_mms .EQ. 1) THEN
          filename = 'bv_tn_us.dat'
          CALL FIND_FILE(filename, lexist)
          CALL XERTST(lexist, 'File '//TRIM(filename)//' not found')
          OPEN(70, file=trim(filename)) 
          READ(70, '(1x,e24.16)') (bv_tn(icv), icv=1,ncv)
          CLOSE(70) 
        ELSE IF (switch%use_mms .EQ. 2) THEN
          WRITE(chitim, '(i4.4)') itim
          filename = 'bv_tn_us'//chitim//'.dat'
          CALL FIND_FILE(filename, lexist)
          CALL XERTST(lexist, 'File '//TRIM(filename)//' not found')
          OPEN(70, file=trim(filename)) 
          READ(70, '(1x,e24.16)') (bv_tn(icv), icv=1,ncv)
          CLOSE(70) 
        END IF
      END IF
    END IF
!
    RETURN
  END SUBROUTINE READ_BOUNDARY_SOURCES
!

END MODULE B2MOD_BOUNDARY_SOURCES_DIFF
!srv 26.02.18 }

